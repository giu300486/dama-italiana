# Changelog

Tutte le modifiche significative al progetto sono documentate qui.

Il formato è basato su [Keep a Changelog 1.1.0](https://keepachangelog.com/en/1.1.0/) e il progetto adotta [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **Task 4.11** — End-to-end Java API match test (acceptance autoritativa Fase 4, branch `feature/4-core-server-skeleton`):
  - **1 file di test nuovo** (`SoloMatchEndToEndTest.java`, **3 test top-level**), nessun file di produzione toccato. Verifica end-to-end che il `MatchManager` orchestri match fino a stato terminale via API Java pura usando il vero `ItalianRuleEngine`.
  - **Test 1 — `playsRandomGameUntilTerminal`**: setup `InMemoryMatchRepository` + `ItalianRuleEngine` reale + Mockito mocks bus/stomp + `Clock.fixed`. Helper `playRandomGame(manager, engine, match)` loop con `Random(seed=42L)` + safety cap `MAX_PLIES=500`: finché `match.status() == ONGOING`, sender = WHITE→ALICE / BLACK→BOB derivato da `match.state().sideToMove()`, mossa random-pick uniforme da `engine.legalMoves(state)`. Asserisce: status FINISHED, `events.size() == pliesPlayed + 1` (1 MoveApplied per ply + 1 MatchEnded finale; 0 rejection perché si pesca solo da `legalMoves`), tutti gli eventi sono `MoveApplied | MatchEnded`, ultimo evento `MatchEnded`, sequence number strict monotonic da 0 (FR-COM-04 verification).
  - **Test 2 — `replayFromEventsReconstructsCurrentState`**: applica 8 ply tramite `playFixedNumberOfPlies`, poi `replayFromInitial(engine, events)` ripiega la lista filtrando `MoveApplied` e applicando ognuno via `engine.applyMove(state, applied.move())` partendo da `GameState.initial()`. Asserisce `replayed.equals(match.state())` — prova event-log fidelity: la storia degli eventi è autoritativa per ricostruire lo stato del match (preview di F6 reconnection/replay).
  - **Test 3 — `eventsBroadcastOnBusAndStomp`**: bootstrap `AnnotationConfigApplicationContext.register(CoreServerConfiguration.class, BroadcastTestConfig.class, CollectingListener.class)`. **`BroadcastTestConfig @Configuration`** static inner espone `BufferingStompPublisher @Bean @Primary` per spostare l'autowire di `MatchManager` da `LoggingStompPublisher @Component` (default core-server) a `BufferingStompPublisher` test-scope (stesso pattern che F6 userà per `WebSocketStompPublisher @Primary`). **`CollectingListener @Component`** static inner con `@EventListener void on(MatchEventEnvelope)` + `synchronizedList<MatchEvent>`. Flusso: `createMatch + playRandomGame`, poi `busEvents = listener.received()`, `stompEvents = publisher.published().stream().map(p -> (MatchEvent) p.payload()).toList()`. Asserisce `stompEvents.containsExactlyElementsOf(busEvents)` (stesso ordine, stessi oggetti — verifica che `MatchManager.emit` attraversi entrambi i canali sincronicamente FIFO), e `publisher.published().allSatisfy(p -> p.topic().equals("/topic/match/" + match.id()))` (verifica `MATCH_TOPIC_PREFIX` SPEC §11.4).
  - **Determinismo**: `RANDOM_SEED = 42L` su tutti e 3 i test → run riproducibili anche cross-machine. `MAX_PLIES = 500` ceiling difensivo: in Italian la stalemate=loss garantisce terminazione, il cap serve solo a fallire con `AssertionError` esplicativo invece di loopare in eternità in caso di bug futuro.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 29.6s, **145/145 test verdi** (142 + 3 nuovi), JaCoCo gate `All coverage checks have been met` (38 classi invariate vs Task 4.10 — solo nuovi test), SpotBugs 0 issues, Spotless OK (76 file kept clean — Spotless ha riformattato Javadoc/error-message multilinea del test durante l'`apply`).

- **Task 4.10** — `TournamentEngine` skeleton (Fase 4, branch `feature/4-core-server-skeleton`):
  - **9 file prod nuovi** in `com.damaitaliana.core.tournament` + **1 file prod modificato** (`package-info.java`) + **1 file di test nuovo**.
  - **`TournamentEngine`** interface 4 metodi: `createTournament(TournamentSpec)`, `registerParticipant(TournamentId, UserRef)`, `startTournament(TournamentId)` (Fase 4 stub), `findById(TournamentId)`. **`TournamentSpec(name, format, timeControl, maxParticipants)`** record con validation null/blank-name + `maxParticipants >= 2`. **`TournamentFormat`** enum 2 valori (`SINGLE_ELIMINATION`/`ROUND_ROBIN`) parallelo a `Tournament` sealed permits.
  - **`TournamentEngineImpl @Service final class`**: ctor injection di `TournamentRepository`; concurrency via `ConcurrentHashMap<TournamentId, Object> locks` + `synchronized (lockFor(id))` su `registerParticipant`. `createTournament` genera `TournamentId.random()` + costruisce variante via `switch(spec.format())` + persiste status `CREATED` + ritorna immutabile. `registerParticipant` valida `NoSuchElementException` se id sconosciuto, `IllegalStateException` se status != CREATED ("registration closed"), `IllegalArgumentException` se partecipante duplicato; helper `withParticipants(current, roster)` switcha su `Tournament` sealed e crea il record aggiornato preservando gli altri campi. `startTournament` lancia `UnsupportedOperationException("...deferred to Fase 8 (single-elimination) / Fase 9 (round-robin) — PLAN-fase-4 §4.10 risk R-2")`.
  - **`BracketGenerator`** + **`RoundRobinScheduler`** + **`TieBreakerPolicy`** interface contracts. **`StubBracketGenerator implements BracketGenerator`** + **`StubRoundRobinScheduler implements RoundRobinScheduler`** plain classes (no `@Component` — F8/F9 reali registrano impl `@Component` senza conflitto): ogni metodo throws UOE con message "deferred to Fase 8" / "deferred to Fase 9". **`NoOpTieBreakerPolicy @Component final class`**: ritorna `List.copyOf(tied)` (input invariato + null-guards) — fallback Fase 4 prima della policy reale Fase 9.
  - **Roster-cap decision**: `Tournament` records (Task 4.3) NON hanno `maxParticipants` (data shape già solidificata); aggiungerlo sarebbe deviazione retroattiva. Soluzione: `maxParticipants` resta solo su `TournamentSpec` (input), enforcement deferred a Fase 8 start-time validation; documentato in Javadoc di `TournamentEngine.registerParticipant`.
  - **2 deviazioni documentate vs PLAN-fase-4 §4.10**:
    1. **`@ConditionalOnMissingBean` NON usata** su `NoOpTieBreakerPolicy` — vive in `spring-boot-autoconfigure`, violerebbe CLAUDE.md §8.7-8.8 (transport-agnostic invariant). Soluzione: plain `@Component`; F9 introdurrà policy reale marcata `@Primary` o rimuoverà NoOp. Stesso pattern di `LoggingStompPublisher` (Task 4.6).
    2. **`StubBracketGenerator` / `StubRoundRobinScheduler` non `@Component`** — il PLAN listava espressamente `@Component` solo per `NoOpTieBreakerPolicy`. Non registrare i due stub come bean elimina la necessità di marcare gli impl reali F8/F9 con `@Primary` o rimuovere gli stub.
  - **`TournamentEngineSkeletonTest`** in `com.damaitaliana.core.tournament`, **24 test in 8 nested class**:
    - `CreateTournament` (5): SINGLE_ELIMINATION→EliminationTournament+CREATED+empty participants+name/tc, ROUND_ROBIN→RoundRobinTournament, persistence (`repo.findById` post-create), distinct calls→distinct ids, null spec→NPE.
    - `RegisterParticipant` (6): append+persist (1 partecipante via repo lookup), multi-registration preserves insertion order ALICE/BOB/CARLO, double-registration→IAE "already registered", unknown id→NoSuchElementException con message, format variant preserved, null args→NPE × 2.
    - `StartTournament` (2): throws UnsupportedOperationException con message "deferred", null id→NPE.
    - `FindById` (2): returns persisted, returns Optional.empty per unknown.
    - `StubGenerators` (2): `BracketGenerator.generate` throws UOE con "deferred to Fase 8", `RoundRobinScheduler.schedule` throws UOE con "deferred to Fase 9".
    - `NoOpPolicy` (3): returns input unchanged via `containsExactlyElementsOf`, returns empty per empty input, null args→NPE × 2.
    - `TournamentSpecValidation` (4): blank name→IAE "name must not be blank", maxParticipants<2→IAE "maxParticipants must be >= 2", null fields→NPE × 3.
    - 1 test top-level `registrationClosedAfterStartIsTreatedAsIllegalState`: registra una IN_PROGRESS-snapshot direttamente via `repo.save(...)` (no engine path, perché startTournament throws in F4) poi prova a registrare → IAE "registration closed". Simula la transition F8/F9 senza poterla fare via engine.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 28.7s, **142/142 test verdi** (118 + 24 nuovi), JaCoCo gate `All coverage checks have been met` (38 classi analizzate vs 32 di Task 4.9, +6 prod: `TournamentFormat` + `TournamentSpec` + `NoOpTieBreakerPolicy` + `StubBracketGenerator` + `StubRoundRobinScheduler` + `TournamentEngineImpl`; le 4 interface bytecode-zero non entrano nel bundle), SpotBugs 0 issues, Spotless OK (75 file kept clean — Spotless ha riformattato Javadoc in 5 file durante l'`apply`).

- **Task 4.9** — Anti-cheat tests + counter behavior end-to-end via `MatchManager` (Fase 4, branch `feature/4-core-server-skeleton`):
  - **1 file di test nuovo** (`AntiCheatTest.java`, 3 test che coprono A4.4 — FR-COM-01, SPEC §9.8.3, ADR-040). Nessun nuovo file di produzione: la logica anti-cheat era già stata scritta in Task 4.7 (counter su `Match`) + Task 4.8 (`applyAntiCheatCheck` in `MatchManager` con threshold `ILLEGAL_MOVES_FORFEIT_THRESHOLD = 5`). Task 4.9 chiude la copertura end-to-end.
  - **Setup comune**: `InMemoryMatchRepository` reale + Mockito mocks `MatchEventBus` + `StompCompatiblePublisher` + `Clock.fixed` deterministico. Test 1 e 2 usano `ItalianRuleEngine` reale (NOT_YOUR_TURN sequences); test 3 stub-Mockito di `RuleEngine` perché serve interleaving illegali tra WHITE e BLACK senza che alcuno faccia una mossa legale (impossibile con engine reale: ogni mossa legale flippa sideToMove).
  - **Test 1 — `fiveConsecutiveIllegalMovesForfeitsThePlayerAndAwardsTheOpponent`**: WHITE plays 1 legal → BLACK turn; WHITE invia 5 mosse di seguito (tutte NOT_YOUR_TURN); dopo la 5ª, WHITE counter=5, match→FINISHED, eventi totali=7 (1 MoveApplied + 5 MoveRejected NOT_YOUR_TURN + 1 MatchEnded BLACK_WINS+FORFEIT_ANTI_CHEAT); BLACK counter resta 0.
  - **Test 2 — `aLegalMoveResetsTheCounterAndAvoidsForfeit`**: WHITE plays legal → 4 NOT_YOUR_TURN (counter=4) → BLACK plays legal → WHITE plays legal (counter resetta a 0) → 4 NOT_YOUR_TURN (counter=4); match still ONGOING (≤4 < 5 threshold).
  - **Test 3 — `eachPlayerHasItsOwnCounterAndOnlyTheOffenderForfeits`** (Mockito-stubbed engine throws IllegalMoveException su ogni applyMove): WHITE invia 4 mosse mentre è suo turno → 4 ILLEGAL_MOVE rejections (engine throws), WHITE counter=4 (BLACK counter=0); BLACK invia 4 mosse mentre è ancora WHITE turn (engine non viene mai chiamato, turn check fallisce prima) → 4 NOT_YOUR_TURN rejections, BLACK counter=4 (WHITE counter ancora 4); WHITE invia la 5ª mossa illegale → counter=5, forfait WHITE, MatchEnded BLACK_WINS+FORFEIT_ANTI_CHEAT, BLACK counter resta a 4 (sotto threshold).
  - **Coverage**: A4.4 ora ✅ COVERED end-to-end. JaCoCo branches `applyAntiCheatCheck` ramo "≥ threshold → emit MatchEnded + status FINISHED" coperti.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 27.3s, **118/118 test verdi** (115 + 3 nuovi), JaCoCo gate `All coverage checks have been met` (32 classi invariato vs Task 4.8 — solo nuovi test, nessuna nuova classe prod), SpotBugs 0 issues, Spotless OK.

- **Task 4.8** — `MatchManager @Service` (Fase 4, branch `feature/4-core-server-skeleton`):
  - **2 file prod nuovi** (`MatchManager.java`, `MatchNotFoundException.java`) + **1 file prod modificato** (`CoreServerConfiguration.java` con `@Bean RuleEngine ruleEngine() { return new ItalianRuleEngine(); }` factory) + **4 file di test nuovi** (26 test totali).
  - **`MatchManager`** `@Service final class` in `com.damaitaliana.core.match`: DI di `MatchRepository`, `RuleEngine` (da `shared`), `MatchEventBus`, `StompCompatiblePublisher` via ctor pubblico `@Autowired` 4-arg + ctor package-private 5-arg per visible-for-tests con `Clock` deterministico. Concurrency: `ConcurrentHashMap<MatchId, Object> matchLocks` + `synchronized (lockFor(matchId))` su ogni write op (FR-COM-04 strict monotonic validation cannot fail under contention). Helper `emit(<E extends MatchEvent> E event)` esegue `repo.appendEvent + bus.publish + stompPublisher.publishToTopic("/topic/match/" + matchId, event)` per ogni evento applicato (i 3 canali sempre attraversati). Helper `terminalResult(GameStatus)` e `terminalReason(GameStatus)` mappano lo stato del `RuleEngine` shared (5 valori non-ONGOING) ai tipi wire core-server `MatchResult` + `EndReason`.
  - **API pubblica (6 metodi)**:
    - `createMatch(white, black, timeControl)` → Match status=ONGOING (lobby/`WAITING` management F6 — F4 starts ongoing immediato), `MatchId.random` + `GameState.initial` + sequenceNo=-1.
    - `findById(MatchId)` → delega lock-free.
    - `eventsSince(MatchId, fromSeq)` → delega lock-free.
    - `applyMove(MatchId, sender, Move)` → flow: lookup (MatchNotFoundException) → colorOf (IllegalArgumentException se non-partecipante) → status==ONGOING? else MoveRejected MATCH_NOT_ONGOING senza counter bump → sideToMove==senderColor? else MoveRejected NOT_YOUR_TURN + counter+1 + antiCheatCheck → try ruleEngine.applyMove (catch IllegalMoveException → MoveRejected ILLEGAL_MOVE + counter+1 + antiCheatCheck) → success → MoveApplied + match.state(newState) + match.clearIllegalMoves(senderColor) → se newState terminal emit MatchEnded(terminalResult, terminalReason) + match.status(FINISHED).
    - `resign(MatchId, who)` ONGOING-only → Resigned + MatchEnded(opposite winner, RESIGN) + FINISHED.
    - `offerDraw(MatchId, from)` ONGOING + no-pending-offer-only → DrawOffered + pendingDrawOfferFrom set.
    - `respondDraw(MatchId, responder, accept)` ONGOING + has-pending + responder!=offerer-only → accept: DrawAccepted + MatchEnded(DRAW, DRAW_AGREEMENT) + FINISHED + pending cleared / decline: DrawDeclined + match prosegue ONGOING + pending cleared.
  - **Anti-cheat** (FR-COM-01, SPEC §9.8.3, ADR-040): `applyAntiCheatCheck` valuta `match.consecutiveIllegalMoves(senderColor) >= 5` (`ILLEGAL_MOVES_FORFEIT_THRESHOLD` package-private constant), in caso emette MatchEnded(FORFEIT_ANTI_CHEAT, opposite winner). MATCH_NOT_ONGOING NON triggera anti-cheat (stale client retry post-clean-termination non è "cheating" nello stesso senso).
  - **`MatchNotFoundException extends RuntimeException`** unchecked con `serialVersionUID = 1L` per lookup falliti — F6 REST controller la mapperà a 404.
  - **`CoreServerConfiguration` updated**: aggiunto `@Bean RuleEngine ruleEngine() { return new ItalianRuleEngine(); }` factory. `shared` è dependency-pure (CLAUDE.md §8.7: no Spring annotations) — necessario `@Bean` invece di `@Component` su `ItalianRuleEngine`.
  - **4 file di test** in `com.damaitaliana.core.match` (accesso package-private al ctor 5-arg con Clock):
    - **`MatchManagerTurnTest`** (8 test, real `ItalianRuleEngine` + Mockito mocks bus/stomp): findById empty, createMatch persists ONGOING + initial state + clock-fixed startedAt, unknown-match→MatchNotFoundException, non-participant→IllegalArgumentException, wrong-turn→MoveRejected NOT_YOUR_TURN+counter bump+state unaffected, finished-match→MoveRejected MATCH_NOT_ONGOING senza counter bump, broadcast bus+stomp via `verify`, eventsSince delegate.
    - **`MatchManagerValidationTest`** (6 test, mix real engine + Mockito-stubbed engine): legal move flips sideToMove, legal-move-resets-counter pattern, illegal-move-replay-from-empty-square→MoveRejected ILLEGAL_MOVE, terminal-WHITE_WINS via stubbed engine→MatchEnded WHITE_WINS+CHECKMATE_LIKE+FINISHED, terminal-DRAW_REPETITION via stubbed engine→MatchEnded DRAW+DRAW_REPETITION, IllegalMoveException da engine→MoveRejected ILLEGAL_MOVE+counter.
    - **`ResignFlowTest`** (6 test): white resign→MatchEnded BLACK_WINS+RESIGN, black resign→MatchEnded WHITE_WINS+RESIGN, transitions to FINISHED, finished-match-resign→IllegalStateException, non-participant→IllegalArgumentException, unknown-match→MatchNotFoundException.
    - **`DrawFlowTest`** (6 test, copre A4.8 4 scenari + 2 edge): offer→DrawOffered+pending state, accept→DrawAccepted+MatchEnded DRAW+DRAW_AGREEMENT+FINISHED+pending cleared, decline→DrawDeclined+ONGOING+pending cleared+re-offer ok, doppia-offer-pending→IllegalStateException "draw offer is already pending", responder==offerer→IllegalStateException "cannot respond to their own draw offer", no-pending-offer→IllegalStateException "No pending draw offer".
  - **Issue Spring DI risolto**: `MatchManager` ha 2 ctor (4-arg public, 5-arg package-private per Clock). Spring 6+ non sa quale scegliere autonomously ("No default constructor found"); soluzione: `@Autowired` esplicito sul ctor 4-arg pubblico per disambiguare. Alternative considerate (single ctor con default Clock, ctor 5-arg pubblico) scartate per pulizia API.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 26.8s, **115/115 test verdi** (89 + 26 nuovi: 8 turn + 6 validation + 6 resign + 6 draw), JaCoCo gate `All coverage checks have been met` (32 classi analizzate vs 30 di Task 4.7, +2: `MatchManager` + `MatchNotFoundException`), SpotBugs 0 issues, Spotless OK.

- **Task 4.7** — `Match` state machine + anti-cheat counter (Fase 4, branch `feature/4-core-server-skeleton`):
  - **1 file prod modificato** (`Match.java`: +1 import `Color`, +1 import `EnumMap`, +1 field, +5 metodi, +1 helper privato, Javadoc class-level aggiornato per riflettere lo stato post-4.7) + **1 file di test nuovo** (`MatchStateMachineTest.java`, 26 test in 2 nested class).
  - **State-machine validation** in `void status(MatchStatus newStatus)` package-private setter: valida la transizione contro un set chiuso `WAITING→ONGOING` / `ONGOING→FINISHED` / `ONGOING→ABORTED` (PLAN-fase-4 §4.7); ogni altra transizione (terminali FINISHED/ABORTED → qualsiasi, regressioni `ONGOING→WAITING`, salti `WAITING→FINISHED|ABORTED`, identity `X→X`) lancia `IllegalStateException` con message `"invalid match status transition: FROM -> TO"` e NON modifica lo state (asserito dai test: dopo throw, `match.status()` resta invariato). Helper `private static boolean isLegalTransition(MatchStatus from, MatchStatus to)` con switch expression + `case FINISHED, ABORTED -> false` (sealed-style esaustivo sull'enum).
  - **Anti-cheat counter** (FR-COM-01, SPEC §9.8.3, ADR-040 a Task 4.13): nuovo field `private final EnumMap<Color, Integer> consecutiveIllegalMoves` inizializzato in ctor con `{WHITE: 0, BLACK: 0}`. Public read accessor `int consecutiveIllegalMoves(Color who)` con null-guard, ritorna value diretto dall'EnumMap (sempre non-null perché ctor inizializza entrambi i colori). Package-private mutators `void recordIllegalMove(Color who)` (`merge(who, 1, Integer::sum)`) e `void clearIllegalMoves(Color who)` (`put(who, 0)`) — saranno chiamati dal `MatchManager` di Task 4.8 sui flussi MoveRejected/MoveApplied. Counter held in-memory only (transient, non serializzato, non parte del wire shape) — recovery F6 reconnection resetta a zero per scelta benevola (ADR-040).
  - **Class Javadoc aggiornato**: rimossa frase stale "the state-machine wiring stays in Task 4.8 — Match itself is intentionally a thin data carrier with structural validation only", sostituita con spiegazione che (i) il data shape è stato anticipato a 4.3, (ii) state-machine validation + anti-cheat counter aggiunti in 4.7, (iii) il "wiring" (decisione su quando chiamare quale mutator) resta a `MatchManager` 4.8.
  - **`MatchStateMachineTest`** in `com.damaitaliana.core.match` (per accedere ai mutator package-private): `@Nested StatusTransitions` (10 metodi @Test, di cui 3 `@ParameterizedTest @EnumSource(MatchStatus.class)` — terminale-FINISHED → 4 cases, terminale-ABORTED → 4 cases, identity → 4 cases — totale 19 esecuzioni); `@Nested AntiCheatCounter` (7 test). Helper `newMatchWithStatus(MatchStatus)` costruisce `Match` via il ctor 10-arg con `GameState(Board.initial(), Color.WHITE, 0, List.of(), GameStatus.ONGOING)`.
  - **Backward compat**: nessuna modifica al ctor di `Match` (l'EnumMap è inizializzato all'interno, non passato come arg), quindi `MatchRepositoryContractTest`/`MatchValueTypesTest` continuano a usare lo stesso ctor signature 10-arg senza modifiche.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 20.8s, **89/89 test verdi** (63 + 26 nuovi), JaCoCo gate `All coverage checks have been met` (30 classi analizzate, invariato vs Task 4.6 — `Match` era già nel bundle, l'aggiunta di metodi non cambia il count, il coverage interno aumenta), SpotBugs 0 issues, Spotless OK.

- **Task 4.6** — STOMP-compatible publisher (Fase 4, branch `feature/4-core-server-skeleton`):
  - **2 file prod nuovi** in `com.damaitaliana.core.stomp` + **2 file test** (1 helper test-scope + 1 nuovo test class) + **1 dep aggiunta** a `core-server/pom.xml`.
  - **`StompCompatiblePublisher`** interface single-method `void publishToTopic(String topic, Object payload)`. Contratto "given a topic and a payload, deliver to whoever is subscribed on the transport" — `core-server` NON sa nulla di WebSocket: l'impl reale `WebSocketStompPublisher` arriva da F6 (server, Internet) e F7 (client LAN host) con wrapping di Spring `SimpMessagingTemplate`. Spec del contratto: throw `NullPointerException` su null args (fail-fast nei test); le impl transport-side NON DEVONO throw su transport failure (broadcast best-effort — l'autoritativo è già persistito via `MatchRepository`).
  - **`LoggingStompPublisher @Component`** `final class` SLF4J fallback: `LoggerFactory.getLogger(LoggingStompPublisher.class)` + `publishToTopic` valida null + `LOGGER.info("[stomp] {} <- {}", topic, payload)` (parameterized SLF4J, evita allocazione String se INFO disabled). Default impl quando nessun trasporto reale è cablato — usato dai test core-server e dai run JVM standalone server/client prima dell'install del publisher reale.
  - **`BufferingStompPublisher`** (test-scope, in `core-server/src/test/java/com/damaitaliana/core/stomp/` — NOT in production jar) `final class` con record nested `Published(String topic, Object payload)` con null validation + `Collections.synchronizedList<Published> buffer`. `publishToTopic` add atomico (synchronizedList lock interno); `published()` ritorna `List.copyOf(buffer)` sotto `synchronized(buffer)` block esplicito (iteration safety richiesta dal JavaDoc di `Collections.synchronizedList`); `clear()` per fixture reset. Sarà iniettato dai `MatchManager` test di Task 4.8 via `@TestConfiguration` o registrazione manuale per asserire A4.10 ("ogni evento è pubblicato sul topic `/topic/match/{id}` via `StompCompatiblePublisher.publishToTopic`").
  - **`LoggingStompPublisherTest`** (3 test): smoke 3-call no-throw con `String`/`StringBuilder`/`int` payloads, null topic NPE, null payload NPE. Log output NON è asserito — capturing SLF4J richiederebbe nuova test dep `logback-test` o equivalente, anti-pattern #13.
  - **`BufferingStompPublisherTest`** (6 test): FIFO collect 3-call, fresh-publisher empty list, `clear()` semantics, snapshot immutability vs further writes (snapshot pre-write ha size 1, snapshot post-write ha size 2), `Published` record null-topic guard, null-payload guard.
  - **2 deviazioni documentate vs PLAN-fase-4 §4.6**:
    1. **`@ConditionalOnMissingBean` NON usato** — vive in `spring-boot-autoconfigure`, aggiungerla violerebbe CLAUDE.md §8.7-8.8 (transport-agnostic invariant) e anti-pattern #13 (dep fuori SPEC §6). Soluzione: plain `@Component`; F6 (server) e F7 (LAN host) marcheranno il loro `WebSocketStompPublisher @Component @Primary` per prendere precedenza nell'autowiring quando entrambi i bean coesistono — il logging publisher resta registered ma orphaned (Spring non multicast attraverso bean unrelated dello stesso tipo, solo l'autowire target riceve chiamate da `MatchManager`).
    2. **Aggiunto `slf4j-api`** come dep produzione a `core-server/pom.xml` (no version tag — gestita dal Spring Boot BOM del parent, già citato nel commento "manages SLF4J, Logback, ..."). `slf4j-api` è autorizzato esplicitamente da SPEC §6 line 330 (`Logging | SLF4J + Logback`) e mandato da CLAUDE.md §4.1 ("Solo SLF4J") — NON è "nuova dep outside SPEC §6" ma enabling di una scelta già approvata, non ancora attivata in `core-server` perché nessuno dei task precedenti aveva bisogno di logging. La binding (logback-classic) NON è inclusa qui: i moduli runtime (server F6, client F7) la portano via le loro `spring-boot-starter` deps.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 27.7s, **63/63 test verdi** (54 + 9 nuovi), JaCoCo gate `All coverage checks have been met` (30 classi analizzate vs 29 di Task 4.5, +1: `LoggingStompPublisher`; `StompCompatiblePublisher` interface bytecode-zero, `BufferingStompPublisher` in test scope NON entra nel bundle prod), SpotBugs 0 issues, Spotless OK.

- **Task 4.5** — Internal event bus su Spring `ApplicationEventPublisher` (Fase 4, branch `feature/4-core-server-skeleton`):
  - **3 file prod nuovi** in `com.damaitaliana.core.eventbus` + **1 file di test**.
  - **`MatchEventBus`** interface single-method (`void publish(MatchEvent event)`). Semantica documentata: FIFO per publisher thread (Spring default multicaster sincrono — `publish` ritorna solo dopo che tutti i listener hanno processato l'evento), per-match FIFO segue per costruzione perché il `MatchManager` di Task 4.8 serializzerà le scritture per match. Constraint CLAUDE.md §8.8: in-process only — la distribuzione cross-JVM viaggia sul publisher STOMP (Task 4.6), non su questo bus.
  - **`MatchEventEnvelope extends ApplicationEvent`** wrapper Spring (`source` = bus instance, `payload` = `MatchEvent`). Listener registrano una single signature `@EventListener void on(MatchEventEnvelope env)` e fanno pattern-match su `env.payload()` — il sealed `MatchEvent` garantisce esaustività compile-time. `private static final long serialVersionUID = 1L` + `private final transient MatchEvent payload`: il `transient` documenta l'intent (l'envelope NON è serializzato Java-mechanism — il bus è in-process) e silenzia SpotBugs `SE_BAD_FIELD`; il wire layer F6/F7 serializza il `MatchEvent` payload via Jackson, non l'envelope Spring.
  - **`SpringMatchEventBus @Component`** `final class implements MatchEventBus`, ctor injection di `ApplicationEventPublisher` (auto-injected — la `ApplicationContext` stessa lo implementa). `publish` valida null-event poi `publisher.publishEvent(new MatchEventEnvelope(this, event))`.
  - **`SpringMatchEventBusTest`** (4 test): `eventListenerReceivesAllEventsInPublishOrder` (4 eventi `DrawOffered/Declined/Offered/Accepted` pubblicati su un match → listener riceve in ordine), `publishingFromSingleThreadDeliversFifoPerMatch` (eventi interleaved su 2 match, filter per matchId verifica per-match FIFO), `busBeanIsTheSpringImplementation` (`getBean(MatchEventBus.class)` è `SpringMatchEventBus`), `publishingNullEventThrowsNullPointerException` (copre il `Objects.requireNonNull(event)` guard). Listener bean `CollectingListener static final class @Component` con `@EventListener` + `received` come `synchronizedList`. Helper `newContext()` `AnnotationConfigApplicationContext.register(CoreServerConfiguration.class) + register(CollectingListener.class) + refresh()`, try-with-resources per closure.
  - **Deviazione documentata vs PLAN-fase-4 §4.5**: il PLAN chiedeva `@SpringJUnitConfig(CoreServerConfiguration.class)`, che vive in `spring-test` artifact. `spring-test` NON è dep diretta di `core-server` (lo è solo via `spring-boot-starter-test` in `client`/`server`); aggiungerla è "nuova dipendenza" per CLAUDE.md §8 anti-pattern #13 (richiede approvazione esplicita). Soluzione: stesso pattern di `CoreServerConfigurationTest` (Task 4.1) — `AnnotationConfigApplicationContext` diretto. Funzionalmente equivalente per A4.9 (event multicast + FIFO check). **Nessun cambio a `core-server/pom.xml`**. La deviazione è reviewable in REVIEW Fase 4.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 17.1s, **54/54 test verdi** (50 + 4 nuovi), JaCoCo gate `All coverage checks have been met` (29 classi analizzate vs 27 di Task 4.4, +2: envelope + bus impl; l'interface `MatchEventBus` è bytecode-zero), SpotBugs 0 issues, Spotless OK (51 file kept clean — Spotless ha riformattato 4 nuovi file durante l'`apply` per line-wrap Javadoc a 100 col Google Java Style).

- **Task 4.4** — In-memory adapters per i repository ports (Fase 4, branch `feature/4-core-server-skeleton`):
  - **3 adapter `@Component` `final class`** in `com.damaitaliana.core.repository.inmemory` (SPEC §7.2 "Adapter in-memory built-in (per LAN host)"). Riusati dal LAN host F7 via `CoreServerConfiguration` component scan; il modulo `server` di F6 fornirà la controparte JPA contro tabelle `matches`/`match_events` (SPEC §8.4).
  - **`InMemoryMatchRepository`**: `ConcurrentHashMap<MatchId, Match>` per snapshot (save = put + return same ref, findById = `Optional.ofNullable`, findByStatus + findByPlayer = iterazione su `snapshots.values()` con filtro + `List.copyOf` per immutability). Event log append-only via `ConcurrentHashMap<MatchId, List<MatchEvent>>` con valori `Collections.synchronizedList(new ArrayList<>())`; `appendEvent` esegue validation + append sotto `synchronized (log)` block esplicito così che strict monotonic check (FR-COM-04, SPEC §7.5) e l'add siano atomici sotto contesa: `expected = log.size()`, se `event.sequenceNo() != expected` → `IllegalStateException` con message che cita matchId + expected + actual; altrimenti `log.add(event)`. `currentSequenceNo` ritorna `-1L` se match unknown o log vuoto, `(long) log.size() - 1L` altrimenti (sotto lock). `eventsSince(matchId, fromSeq)` ritorna `List.of()` per match unknown, altrimenti sotto lock costruisce ArrayList col suffix dove `e.sequenceNo() > fromSeq` e ritorna `List.copyOf(suffix)`. Il Fase 6 JPA adapter sostituirà `synchronized` con `UNIQUE(match_id, sequence_no)` constraint + pessimistic row locking — stesso contratto, transport diverso.
  - **`InMemoryTournamentRepository`**: stesso pattern, solo CRUD su `ConcurrentHashMap<TournamentId, Tournament>` (no event log: tournament-level events sono F8/F9 — PLAN-fase-4 §1 "out of scope tournament events").
  - **`InMemoryUserRepository`**: due `ConcurrentHashMap` (`Long → UserRef` id-indexed e `String → UserRef` username-indexed); helper public `register(UserRef)` popola entrambi gli indici (gli utenti anonimi LAN — `id == ANONYMOUS_LAN_ID` — vengono indicizzati solo per username, l'id-side resta vuoto); `findById(long)` early-return `Optional.empty()` per `UserRef.ANONYMOUS_LAN_ID` (-1L) per documentazione del port (SPEC §11.1: anonymous LAN users not addressable by id); `findByUsername` semplice lookup. Lifecycle utenti completo (registration BCrypt cost ≥ 12, JWT) arriva in F5 con JPA adapter nel modulo `server`.
  - **`InMemoryMatchRepositoryTest`** (`final class` package-private in `com.damaitaliana.core.repository` per accedere all'abstract `MatchRepositoryContractTest` package-private): override `createRepository()` ritorna `new InMemoryMatchRepository()`. **Attiva i 13 contract test** scritti in 4.3 — la voce CHANGELOG di 4.3 li stimava 12, il count reale è 13 (l'extra è `currentSequenceNoIsMinusOneForUnknownMatch`). Pattern riusabile in F6: `JpaMatchRepositoryTest` estenderà la stessa classe abstract con `@Transactional` boundary + `createRepository()` su impl JPA → stesse 13 asserzioni contro due adapter eterogenei.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 20.8s, **50/50 test verdi** (37 + 13 contract activations), JaCoCo gate `All coverage checks have been met` (27 classi analizzate vs 24 di Task 4.3, +3 prod adapter), SpotBugs 0 issues, Spotless OK (47 file kept clean — niente reformat necessario).

- **Task 4.3** — Repository ports + `Match` data class + `Tournament` sealed shape (Fase 4, branch `feature/4-core-server-skeleton`):
  - **`Match` data class anticipato da Task 4.7** (state machine) per dipendenza di tipo: `MatchRepository.save(Match)` richiede il tipo concreto. Scope ridotto in F4: dati only (mutable: public getters + package-private setters). Fields: `MatchId id`, `UserRef white`/`black`, `TimeControl timeControl`, `Instant startedAt`, nullable `TournamentMatchRef tournamentRef`, mutable `GameState state`, `long currentSequenceNo` (≥-1 invariant), `MatchStatus status`, nullable `UserRef pendingDrawOfferFrom`. Optional accessors per i campi nullable (CLAUDE.md §4.1: `Optional` solo per ritorni). Anti-cheat counter, transitions di stato e wiring eventi restano a Task 4.8 (`MatchManager`).
  - **`Tournament` sealed interface** in `com.damaitaliana.core.tournament` con permits `EliminationTournament` + `RoundRobinTournament`. Common: `id()`, `name()`, `status()`, `participants()`, `timeControl()`. F4 ship solo data shape — bracket generation, scheduling, standings, tie-breaker deferred a F8 (`EliminationTournament`) / F9 (`RoundRobinTournament` + `TieBreakerPolicy`). Records validano null + `name.isBlank()` + `participants = List.copyOf(...)` per immutability.
  - **`MatchRepository` port** in `com.damaitaliana.core.repository` (no JPA, plain Java per CLAUDE.md §8.8) con 7 metodi: `save(Match)`, `findById(MatchId): Optional`, `findByStatus(MatchStatus): List`, `findByPlayer(UserRef): List` (matches con user come bianco o nero, equality via `UserRef.equals`), `appendEvent(MatchEvent)` (validazione strict monotonicity, `IllegalStateException` se `event.sequenceNo() != currentSequenceNo + 1` — FR-COM-04), `eventsSince(MatchId, fromSeq): List` (suffix in ascending order, `-1L` = full log), `currentSequenceNo(MatchId): long` (`-1` se nessun event o match unknown). Per-match write serialization è responsabilità del caller (`MatchManager` userà lock per match).
  - **`TournamentRepository` port** skeleton: `save`, `findById`, `findByStatus`. Adapter in-memory a Task 4.4; JPA adapter a F6.
  - **`UserRepository` port** skeleton: `findById(long): Optional<UserRef>`, `findByUsername(String): Optional<UserRef>`. Read-only (full lifecycle utenti: registration BCrypt cost ≥12 + JWT a F5 nel modulo `server`). Per `UserRef.ANONYMOUS_LAN_ID` lookup non significativo — implementations possono returnare `Optional.empty()`.
  - **`MatchRepositoryContractTest`** (`abstract class`, package-private — JUnit 5 skip automatico in discovery): 12 test che coprono il contratto del port. Sub-package: save+findById roundtrip, save overwrites same-id, findByStatus empty/multi, findByPlayer (white|black|none), currentSequenceNo `-1` pre-event + post-event monotonic, appendEvent rejects non-monotonic seq + first event must be `0`, eventsSince suffix semantics + empty for unknown match. Concrete subclasses (Task 4.4 in-memory, F6 JPA) forniscono `protected abstract MatchRepository createRepository()`. **Riusabile** per JPA in F6: stesso file, due adapter testati identicamente contro le stesse asserzioni.
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 5:53 min, **37/37 test verdi** (invariato vs 4.2 — i contract test sono skipped in attesa del concrete sub di Task 4.4), JaCoCo gate `All coverage checks have been met` (24 classi analizzate vs 21 di Task 4.2), SpotBugs 0 issues, Spotless OK.

- **Task 4.2** — Domain value types (records, enums) + MatchEvent sealed hierarchy (Fase 4, branch `feature/4-core-server-skeleton`):
  - **Value types in `com.damaitaliana.core.match`** (8 file): `MatchId(UUID)` con factory `random()`/`of(String)`; `UserRef(long, String, String)` con sentinel `ANONYMOUS_LAN_ID = -1L`, factory `anonymousLan(username)` (LAN no-auth, SPEC §11.1) e `authenticated(id, username, displayName)` (server-side, SPEC §8.4 `users` table), helper `isAnonymous()`, validation null/blank; `TimeControl(preset, initialMillis, incrementMillis)` come metadato del match (no clock running in F4 — clock + `FORFEIT_TIMEOUT` arrivano F6); `TimeControlPreset` enum 4 valori (BLITZ_5_3, RAPID_15_10, CLASSICAL_30_30, UNLIMITED) con factory `asTimeControl()`; enum `MatchStatus` (WAITING/ONGOING/FINISHED/ABORTED), `MatchResult` (WHITE_WINS/BLACK_WINS/DRAW/UNFINISHED), `EndReason` 7 valori (CHECKMATE_LIKE/RESIGN/DRAW_AGREEMENT/DRAW_REPETITION/DRAW_FORTY_MOVES/FORFEIT_ANTI_CHEAT/FORFEIT_TIMEOUT), `RejectionReason` 4 valori (NOT_YOUR_TURN/ILLEGAL_MOVE/MATCH_NOT_ONGOING/MATCH_NOT_FOUND).
  - **Value types in `com.damaitaliana.core.tournament`** (3 file): `TournamentId(UUID)` (parallelo MatchId), `TournamentMatchRef(TournamentId, roundNo, matchIndex)` con validation 0-indexed, `TournamentStatus` enum (CREATED/IN_PROGRESS/FINISHED).
  - **MatchEvent sealed hierarchy in `com.damaitaliana.core.match.event`** (10 file): `MatchEvent` sealed interface con 9 permits — `MoveApplied`, `MoveRejected`, `DrawOffered`, `DrawAccepted`, `DrawDeclined`, `Resigned`, `MatchEnded`, `PlayerDisconnected`, `PlayerReconnected` — common method `matchId()`/`sequenceNo()`/`timestamp()`. Ogni record valida null + `sequenceNo ≥ 0`; `MatchEnded` valida che `result != UNFINISHED`. Shape SPEC §8.3 + estensione minima: `MoveRejected` usa enum `RejectionReason` invece di String per wire-stability; `MatchEnded` aggiunge `EndReason reason` accanto a `MatchResult result` per discriminare il meccanismo (deviation documentata in Javadoc dei tipi e in PLAN-fase-4 §4.2, reviewable in REVIEW Fase 4).
  - **`core-server/pom.xml`** — aggiunti 2 deps `<scope>test</scope>` per smoke serializzazione: `jackson-databind` + `jackson-datatype-jsr310` (entrambe Spring Boot BOM-managed). **Production code NON dipende da Jackson** — rispetta CLAUDE.md §8.8 (transport-agnostic). Il wire layer (F6 server / F7 client) porterà il proprio ObjectMapper + Module per il sealed `Move` di `shared` e il non-record `Board`.
  - **`MatchValueTypesTest`** (15 test in 3 `@Nested`): `MatchIdValidation` (random uniqueness, of UUID parsing, malformed string rejection, null check), `UserRefValidation` (anonymousLan factory, authenticated factory id≥0, blank/null username/displayName checks), `TimeControlValidation` (preset.asTimeControl canonical values, unlimited factory, negative validation, null preset, all presets non-negative).
  - **`TournamentValueTypesTest`** (7 test in 2 `@Nested`): `TournamentIdValidation` + `TournamentMatchRefValidation`.
  - **`MatchEventSealedTest`** (4 test): `exhaustiveSwitchOverAllNonMoveAppliedVariants` (compile-time exhaustiveness check su 9 permits — omettere un caso non compila), `allVariantsExposeMatchIdSequenceAndTimestamp` (common interface), `matchEndedRejectsUnfinishedResult`, `rejectsNegativeSequenceNo`. Pattern `case Variant unused ->` per evitare unused-variable warning.
  - **`EventSerializationTest`** (9 test): 8 roundtrip Jackson default per le varianti che dipendono solo da core-server + JDK types (`MoveRejected`, `DrawOffered`, `DrawAccepted`, `DrawDeclined`, `Resigned`, `MatchEnded`, `PlayerDisconnected`, `PlayerReconnected`); `MoveApplied` constructor-only smoke (full Jackson roundtrip deferred a F6 — `Board` non-record + `Move` sealed richiedono custom Module). Test mapper config: `JavaTimeModule` per `Instant`, `disable(MapperFeature.AUTO_DETECT_IS_GETTERS)` per evitare che Jackson detecti `UserRef.isAnonymous()` come bean is-getter "anonymous" (causerebbe unknown-field error in deser).
  - `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 5:22 min, **37/37 test verdi** (+36 vs Task 4.1), JaCoCo gate `All coverage checks have been met` (21 classi analizzate vs 1 di Task 4.1), SpotBugs 0 issues, Spotless OK (auto-applied 2 volte).

- **Task 4.1** — Module setup + sub-packages + `CoreServerConfiguration` (Fase 4, branch `feature/4-core-server-skeleton`):
  - **Rimosso** `core-server/src/test/java/.../CoreServerSmokeTest.java` (vestigial F0, scritto come placeholder con commento "Will be removed in Fase 4 once real tournament/match-manager tests exist").
  - **Creati 7 `package-info.java`** per i sotto-package introdotti dal PLAN-fase-4 §4.1: `com.damaitaliana.core.match` (aggregate Match + MatchManager + value types; ospita anti-cheat counter transient — ADR-040 a Task 4.13), `match.event` (sealed MatchEvent hierarchy SPEC §8.3, Jackson-serializable per FR-COM-02 — verifica a Task 4.2), `tournament` (sealed Tournament + stub generators che lanciano `UnsupportedOperationException` finché F8/F9), `repository` (ports interfaces, no JPA per CLAUDE.md §8.8), `repository.inmemory` (`ConcurrentHashMap` + sync list + `AtomicLong` per sequence monotonico FR-COM-04), `eventbus` (su Spring `ApplicationEventPublisher` — ADR-038 a Task 4.13), `stomp` (publisher port, impl reali in F6 server / F7 client). Ogni Javadoc cita constraint + roadmap deferral.
  - **Creato `CoreServerConfiguration`** in `com.damaitaliana.core` come root Spring Java config: `@Configuration @ComponentScan(basePackages = "com.damaitaliana.core")`. Riusabile via `@Import(CoreServerConfiguration.class)` o parent `@ComponentScan` da `client` (modalità LAN host F7) e `server` (Internet F6). In F4 il component scan non scova ancora @Component concreti — saranno introdotti dai Task 4.4 (in-memory adapters), 4.5 (`SpringMatchEventBus`), 4.6 (`LoggingStompPublisher`), 4.8 (`MatchManager`), 4.10 (`TournamentEngineImpl`).
  - **Sostituito smoke test** con `CoreServerConfigurationTest#shouldBootstrapEmptyContextWithoutErrors`: avvia `AnnotationConfigApplicationContext(CoreServerConfiguration.class)` con try-with-resources, asserisce `getBean(CoreServerConfiguration.class) != null` + `getBeanDefinitionCount() > 0` (almeno la config bean stessa).
  - **Spotless apply** ha riformattato 8 file `package-info.java` + il test (line-wrap a 100 col Google Java Style, `{@code ...}` di fine riga inglobato sulla riga successiva).
  - **Verify**: `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 3:27 min, **1/1 test verde** (`CoreServerConfigurationTest`, 16s incl. boot Spring context), JaCoCo gate `All coverage checks have been met` (1 classe analizzata, parent default `haltOnFailure=false` minimum=0.00 — il gate ≥80% di Task 4.13 attiverà la stretta), SpotBugs 0 issues, Spotless OK.

### Changed

- **`CLAUDE.md` — anti-pattern #15 (vincolo design system F3.5 cross-fase)** (2026-05-02, su `develop` pre-Fase 4): aggiunto in §8 il divieto di introdurre nuovi stili visuali ad-hoc nelle fasi successive a F3.5. Tutte le schermate UI di F6 (lobby online), F7 (LAN host/discovery/chat), F8 (tornei eliminazione), F9 (classifiche/scheduler) e F11 (replay/profilo) DEVONO riusare il design system v2: `ThemeService.applyTheme(scene)`, token `-color-bg-*`/`-color-text-on-*`, classi utility `screen-root`/`card-elevated`/`popover`/`button-primary|secondary`/`label-display*`, fonts Playfair + Inter, texture wood, `BoardRenderer`/`BoardCellNode`/`PieceNode`, animation system (`MoveAnimator`/`ParticleEffects`/`AnimationOrchestrator`), `AudioService` per audio cue, asset CC0/CC-BY/OFL con audit `CREDITS.md`. Riferimenti normativi: SPEC §13.2 (v2.2), ADR-034/035/037. Estensioni del design system sono task dedicati con ADR, non improvvisazioni per fase. Aggiunta inoltre nota cross-fase in §7 "Avvio sessione" che rimanda a #15 quando la fase aggiunge schermate UI. Memoria di progetto associata: `memory/project_ui_restyling_constraint.md` (richiesta utente 2026-05-02 di propagare il vincolo in CLAUDE.md per ricordarlo a ogni avvio sessione).

### Added

- **Task 3.5.15** — Documentation + ADR + CHANGELOG + TRACEABILITY (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - **`ARCHITECTURE.md`**: 4 nuovi ADR. **ADR-034** (visual rework "videogame premium wood") documenta il rationale del riscrivere `theme-light.css` con vocabolario duale `bg-*/text-on-*`, tipografia bi-classe Playfair display + Inter UI, classi pulsante primary/secondary, texture Poly Haven applicata via `BackgroundImage`, pezzi composti `Group` con gradient/ring/gloss/marker, e single-source-of-truth `ThemeService` (tracing del finding F-001 / Task 3.5.14a). **ADR-035** (architettura `AudioService`): interface testabile + impl `JavaFxAudioService` single MediaPlayer music + `EnumMap<Sfx,MediaPlayer>` cache, threading runOnFxThread con catch IllegalStateException, persistence via UserPreferences v2, JaCoCo exclusion sull'impl. **ADR-036** (jpackage Windows MSI): pipeline opt-in profilo `installer`, prerequisito WiX 3.x, `--win-upgrade-uuid` stabile per upgrade in-place, fallback `APP_IMAGE`, AppIconGenerator pattern. **ADR-037** (asset licensing strategy): CC0 visual+audio, OFL 1.1 fonts, `CREDITS.md` come single source of truth con audit trail e esclusioni dettagliate.
  - **`README.md`**: sezione "Build installer Windows (Fase 3.5+)" già presente (Task 3.5.12), aggiunta nota dedicata "Demo cliente" che documenta il flusso target (doppio-click `.msi` → install → Start menu shortcut → no prerequisito Java).
  - **`tests/TRACEABILITY.md`**: nuova sezione "Acceptance criteria di Fase 3.5 (PLAN-fase-3.5.md §2.2)" con mapping completo A3.5.1..A3.5.21 → test class/method o output di build. Riga aggiuntiva FR-SP/FR-RUL invariate (rework solo visual, niente nuovi FR coperti).
  - **`AI_CONTEXT.md`**: stato avanzato a Task 3.5.15 completato; "Prossimo task" → Task 3.5.16 (REVIEW Fase 3.5).
  - **Nessun nuovo test** prodotto in 3.5.15 (per design del piano: solo documentazione).

- **Task 3.5.14** — Manual demo run + visual review (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - **MSI buildato 2 volte** via profilo opt-in `installer` con WiX 3.14 freshly-installed e `PATH=...` workaround inline (WiX non auto-aggiunto al PATH di sistema dall'installer; documentato in README). Output: `client/target/jpackage/Dama Italiana-0.3.5.msi` 152 MB col JRE bundled, `--win-upgrade-uuid 9d8c4a02-...` per upgrade in-place automatico tra build successive.
  - **Installazione su Win 11 locale** OK (shortcut Start menu, niente prerequisito Java, lancio dal shortcut → splash → main menu in <2s).
  - **8 schermate review**: 7/8 catturate in `tests/visual-review/{main-menu,sp-setup,board-game,save-dialog,load-screen,settings,rules}.png` come baseline visuale; **splash** non screenshot-able (durata ~1.5s) ma look verificato a occhio dall'utente — non bloccante.
  - **Gameplay full vs IA Esperto** verificato: tutti gli A3.5.x audio/animazione/visual PASS — easing OUT_BACK overshoot, particle splash su cattura, raggi dorati su promozione, glow halo cattura obbligatoria, music shuffle 30%, SFX MOVE/CAPTURE/PROMOTION/VICTORY|DEFEAT su rispettivi eventi, sliders Volume musica + Effetti reagiscono in tempo reale.
  - **Finding F-001 [REQUIREMENT_GAP, High]** emerso e risolto in **Task 3.5.14a** (vedi sotto).
  - `tests/TEST-PLAN-fase-3.5.md §7` finalizzato esito **PASS** (con i 7 PNG baseline checked-in). A3.5.4 PASS post-fix Task 3.5.14a, A3.5.5..15 PASS direttamente.

### Fixed

- **Task 3.5.14a** — Wood-theming del save dialog (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - `SaveDialogController` creava il proprio `Stage` modale con `new Scene(root)` senza chiamare `ThemeService.applyTheme(scene)` — il dialog renderizzava con la chrome JavaFX di default (sfondo bianco, font sistema), in violazione di A3.5.4 (8 schermate ridisegnate).
  - **Fix**: iniettata dipendenza `ThemeService` nel costruttore @Autowired (ora 4-arg) + visible-for-tests (5-arg); chiamata `applyTheme(scene)` immediatamente dopo `new Scene(root)`. Pattern già usato da `SceneRouter`.
  - Test aggiornati: `SaveDialogControllerTest` + `FxmlLoadingSmokeTest` mockano `ThemeService`; `SaveDialogSpringContextTest` invariato (autowiring risolve ThemeService come `@Component` esistente).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, 321 test totali (invariato vs 3.5.13), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

### Added

- **Task 3.5.13** — Audit FXML smoke breakage post-rework: 0 test rotti dalle modifiche FXML/layout dei task 3.5.6 / 3.5.9 / 3.5.10 / 3.5.11. Pattern dei test (lookup by `fx:id`/styleClass + asserzioni sullo stato del controller) intenzionalmente robusto a refactor visivi. **Deviazione vs PLAN**: stimati 5-15 fix necessari, valore reale 0. Verificato `mvn -pl client verify` (322 test) + `mvn clean verify` root (BUILD SUCCESS 19:48 min, 673/673 verdi).

- **Task 3.5.12** — `jpackage` Windows MSI installer (vedi ADR-036). Profilo Maven `installer` opt-in con pipeline 3-step (`maven-dependency-plugin` stage runtime deps + `maven-resources-plugin` stage self-jar + `org.panteleyev:jpackage-maven-plugin:1.6.5`). Icona `app-icon.ico` multi-size generata via nuovo `AppIconGenerator` test-scope (PNG-in-ICO Vista+). README sezione "Build installer Windows (Fase 3.5+)" aggiunta. Verifica end-to-end deferred a Task 3.5.14 (WiX install + manual demo).

- **Task 3.5.11** — Restyling coordinato 5 schermate restanti (SP setup, Save dialog, Load screen, Settings, Rules) con i token v2 già definiti. Niente nuovi token CSS se non `.screen-root` (backdrop wood) e una regola context-scoped per il fill dei display label dentro card/popover (flip a dark-roast su cream, evita fail WCAG AA). 4 CTAs primary highlight (gradient gold). Deviazione documentata: paragraphs delle rules restano Inter (Playfair italic ridurrebbe leggibilità).

- **Task 3.5.10** — Main menu restyling: 5-card grid (`.main-menu-card`) con cornice in legno scura + dropshadow + hover glow gold; titoli card in `.main-menu-card-title` Playfair 22px bold dark-roast (gold troppo basso contrasto su cream); icon Ikonli ricolorate warm brown #6B4423; backdrop `.main-menu-root` riusa la stessa texture wood scura della board.

- **Task 3.5.9** — Splash screen restyling: rimossi FontIcon checkerboard + ProgressIndicator spinner; aggiunto sottotitolo italics + ProgressBar barber-pole gold orizzontale; backdrop riusa `frame.jpg` di Task 3.5.6; nuova chiave i18n `splash.subtitle` IT/EN. Loading time invariato (1.5s).

- **Task 3.5.8** — Animation polish (juicy easing + particle effects + glow halo): `MoveAnimator` switch da `Interpolator.EASE_OUT` a `Interpolator.SPLINE(0.34, 1.56, 0.64, 1)` (out-back, overshoot leggero); nuovo `ParticleEffects` con `captureSplash` (8-12 Circle marrone/grigie, 350ms ParallelTransition translate radiale + fade + shrink), `promotionGlow` (8-12 Line gold radiali, 600ms expand+fade), `mandatoryGlow` (DropShadow Timeline cycle 1200ms su pezzo che deve catturare). `AnimationOrchestrator` invoca i nuovi effetti.

- **Task 3.5.7** — `PieceNode` redesign 3D-look: composizione `Group(Circle bg radial gradient + Circle ring scanalato + Circle gloss highlight + Text king-marker)`. Marker dame: gold su bianco, deep red su nero. DropShadow ombra. `PieceNodeTest` 5 test rendering uomo/dama bianco/nero + marker presence.

- **Task 3.5.6** — `BoardCellNode` redesign con texture legno: `BackgroundImage` chiaro/scuro alternato (Poly Haven CC0 da Task 3.5.1); cornice `Region` decorativa con texture wood scura + padding 24px; highlight legali via DropShadow gold (sostituisce overlay verde piatto).
  - **`SinglePlayerController` ctor +4° arg `AudioService`** required (Spring auto-wires; tests aggiornati con mock `Mockito.mock(AudioService.class)`). Estratto helper `dispatchMoveSfx(move, preMovePiece)` invocato dentro `finalizeMove` dopo `applyMove`.
  - **3 canali indipendenti**:
    - `Sfx.CAPTURE` se `move.isCapture()`, altrimenti `Sfx.MOVE` (sempre uno per ogni mossa applicata, sia umana che IA — feedback acustico simmetrico per "videogame premium" SPEC §13.4).
    - `Sfx.PROMOTION` quando `preMovePiece.kind == MAN && postMovePiece.kind == KING` (rilevata via `state.board().at(move.from())` pre-apply vs `at(move.to())` post-apply).
    - `Sfx.VICTORY` / `Sfx.DEFEAT` su `state.status().isWin()` confrontando `winner == game.humanColor()`. Draws (DRAW_REPETITION/DRAW_FORTY_MOVES/DRAW_AGREEMENT) restano silenti.
  - **`Sfx.ILLEGAL` in `onCellClicked`**: ramo "click su empty/enemy con `selected != null` e nessun match precedente" (target legale, deselect, reselect friendly) → `audioService.playSfx(Sfx.ILLEGAL)` + `deselect()`. Click senza selezione attiva non genera ILLEGAL (esperienza UX neutrale).
  - **Tests** 9 nuovi (totale +9):
    - `SinglePlayerControllerTest` 5: `simpleHumanMoveFiresMoveSfx`, `illegalClickWhileSelectionActiveFiresIllegalSfx`, `clickOnEmptySquareWithoutSelectionDoesNotFireIllegalSfx`, `reselectingFriendlyPieceDoesNotFireIllegalSfx`, `deselectingSameSquareDoesNotFireIllegalSfx`.
    - `SinglePlayerE2ETest` 4 con setup hand-built `SerializedGameState`: `captureMoveFiresCaptureSfxNotMoveSfx` (W22 cattura B18→W15), `promotionMoveFiresMoveAndPromotionSfx` (W5→W1 rank 7), `capturingTheLastOpposingPieceFiresVictorySfx` (W22 cattura unico B18 → WHITE_WINS, humanColor=WHITE → VICTORY+CAPTURE), `aiWinningMoveFiresDefeatSfxFromHumanPerspective` (driven via `AiTurnService` mock con `CompletableFuture.completedFuture(captureMove)`, humanColor=BLACK → CAPTURE+DEFEAT).
  - 6 ctor call sites aggiornati nei test files (5 in `SinglePlayerControllerTest`, 1 in `SinglePlayerE2ETest`) per il nuovo arg `AudioService`.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **310 test totali** (+9 vs 3.5.4 follow-up), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

### Changed

- **Task 3.5.4 follow-up — audio formato pipeline (2026-05-01)**: dopo verifica empirica del 2026-05-01, **JavaFX Media su Windows non decodifica OGG Vorbis** — `MediaException — Unrecognized file signature!`. Tutti i 3 pack Kenney usati in Task 3.5.1 distribuiscono solo `.ogg`, niente `.wav`. **Soluzione (Opzione 2 approvata dall'utente)**: pipeline pure-Java OGG→WAV in fase di asset acquisition.
  - **Master OGG preservati** in `client/src/main/resources/assets/audio/sfx-master/` (audit trail Task 3.5.1, ~70 KB totali, sorgente di verità).
  - **WAV PCM 16-bit signed LE** committati in `client/src/main/resources/assets/audio/sfx/` (~600 KB, caricati a runtime da `Sfx.resourcePath()`).
  - **Tool**: `com.damaitaliana.client.buildtools.OggToWavConverter` (`client/src/test/java/`), main Java basato sull'API low-level JOrbis (Page/Packet/StreamState/SyncState/DspState/Block, modellato su `com.jcraft.jorbis.DecodeExample`). La rotta via `javax.sound.sampled` SPI è stata tentata ma ha mostrato un bug con clip <400 ms (`pcm.read()` ritorna 0 prematuramente per buffer interno underrun); switch al low-level JOrbis dà output deterministico per tutte le 6 SFX.
  - **Dipendenze build-only** aggiunte a `client/pom.xml` con `<scope>test</scope>` (NON shippate nell'installer demo MSI di Task 3.5.14): `com.googlecode.soundlibs:jorbis:0.0.17.4` (~85 KB, decoder OGG Vorbis pure-Java) + `com.googlecode.soundlibs:vorbisspi:1.0.3.3` (~15 KB, registra `AudioFileReader` SPI in `javax.sound.sampled`). Entrambe LGPL — non ridistribuendole non si attiva nessun obbligo. **Eccezione SPEC §6** documentata in `AI_CONTEXT.md` come "tooling di asset pipeline" (CLAUDE.md §8 anti-pattern #13 grey area: build-time only).
  - **Sfx.java** aggiornato: ogni `resourcePath()` da `.ogg` → `.wav` (i 6 file MOVE/CAPTURE/PROMOTION/ILLEGAL/VICTORY/DEFEAT). `SfxTest` assertion `.endsWith(".ogg")` → `.wav`.
  - **Music binari finalmente committati** in `client/src/main/resources/assets/audio/music/`: 1 MP3 (`calm_piano_1.mp3`, 6.30 MB) + 3 WAV PCM (`first_light_particles.wav` 24.12 MB, `at_home_orchestral.wav` 12.50 MB, `peaceful_forest.wav` 21.23 MB). Totale ~64 MB. Niente re-encoding necessario: JavaFX Media legge MP3 e WAV PCM nativamente. `JavaFxAudioService.MUSIC_TRACKS` aggiornato con le estensioni reali.
  - **Test**: `SfxPlaybackSmokeTest` (1 test) sostituisce il temporaneo `OggCodecSmokeTest`; verifica che ogni `Sfx.resourcePath()` carichi cleanly via `javax.sound.sampled.AudioSystem.getAudioInputStream` (encoding=PCM_SIGNED, channels∈[1,2], frameLength>0). Headless-friendly, niente JavaFX boot.
  - **CREDITS.md** riscritto sezione Audio: nuova sotto-sezione "Pipeline format" che documenta il workflow OGG-master/WAV-runtime + comando `mvn exec:java` per ri-converte; tabella SFX ora ha colonne separate "Master OGG" e "WAV PCM"; sezione Music documenta as-downloaded format (MP3+WAV).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **301 test totali** (+1 `SfxPlaybackSmokeTest`, -1 `OggCodecSmokeTest`), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

### Added

- **Task 3.5.4** — AudioService implementazione + Settings integration (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - **`UserPreferences` schema v2** (`CURRENT_SCHEMA_VERSION = 2`): 4 nuovi campi audio `int musicVolumePercent` + `int sfxVolumePercent` + `boolean musicMuted` + `boolean sfxMuted` con default `30/70/false/false` (SPEC §13.4); compact canonical constructor con clamp 0-100; static `@JsonCreator fromJson()` con boxed `Integer`/`Boolean` per detect missing keys nei file v1 → defaults SPEC; 4 nuovi `with*` setters; `withSchemaVersion(int)` package-private per migration in-memory.
  - **`PreferencesService.load()` migrazione v1→v2 trasparente**: ramo dedicato `version == 1 → withSchemaVersion(CURRENT)` (audio defaults riempiti dal `@JsonCreator`); `version > CURRENT || sconosciuto → defaults()`. Test pinning: `migratesSchemaV1FileFillingAudioDefaults`.
  - **`JavaFxAudioService` full MediaPlayer wiring**: SFX bus = `EnumMap<Sfx, MediaPlayer>` cache lazy (un player per cue, riusato `seek(Duration.ZERO) + play()`); music bus = single `MediaPlayer` driven da `MusicPlaylist`, `setOnEndOfMedia` callback dispone il player corrente e ne crea uno nuovo per il prossimo brano shufflato. Music tracks discovery: lista hard-coded `MUSIC_TRACKS` (4 path da CREDITS.md), filter via `getClass().getResource() != null` → assenti tollerati (warn log, music silente se 0). Threading: helper `runOnFxThread()` con catch `IllegalStateException` (toolkit non inizializzato → run inline) per resilienza unit-test. Persistence: ogni `setVolume`/`setMuted` invoca `persistAudioPreferences()` (`load + with* + save`); failure swallowed con WARN log. Mute semantics: `setMuted(MUSIC, true)` dispone il player; `false` ri-avvia se `musicRequested==true`. `@PreDestroy shutdown()` dispone tutti i players. **JaCoCo `JavaFxAudioService.class` exclusion** aggiunta in `client/pom.xml` (stesso pattern di `JavaFxUserPromptService` per JavaFX-bound impl, l'interface `AudioService` è la testable abstraction).
  - **Auto-start music** in `JavaFxApp.start()` dopo `primaryStage.show()` con try/catch RuntimeException (continua senza audio se boot media fallisce).
  - **Settings UI**: `settings.fxml` aggiunta sezione Audio con 2 `Slider` (0-100, majorTickUnit=25, showTickMarks/Labels) + 2 `CheckBox` mute. `SettingsController` ctor +6° arg `AudioService`; `populateAudio()` legge volume/muted dall'audioService; 4 nuovi `@FXML` handlers (`onMusicVolumeChanged`/`onSfxVolumeChanged`/`onMusicMutedChanged`/`onSfxMutedChanged`) chiamano `audioService.setVolume`/`setMuted` e disabilitano lo slider corrispondente quando muto.
  - **i18n** keys IT/EN: `settings.section.audio`, `settings.audio.{music,sfx,muted}`.
  - **Music asset binari** (4 OpenGameArt CC0 tracks da CREDITS.md: calm_piano_1, first_light_particles, at_home_orchestral, peaceful_forest) **non committati in questo task**: `MUSIC_TRACKS` punta ai loro path attesi, l'AudioService tollera l'assenza (warn log "Music bus has no playable tracks"). Download+re-encode ffmpeg WAV→OGG @96kbps è un commit follow-up dedicato.
  - **Tests aggiornati**: `JavaFxAudioServiceTest` riscritto da 3 a 8 test (init from prefs, defaults SPEC §13.4, setVolume clamp+persist, setMuted toggle+persist, persist failure swallow, mute-while-playing safe, idempotent stop, idempotent stop pre-play); `PreferencesServiceTest` +1 test migrazione; `SettingsControllerTest` ctor signature aggiornata; `UiScalingServiceTest` ctor 9-arg.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **300 test totali** (+5 vs 3.5.3), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

### Fixed

- **Task 3.5.2 readability follow-up #2** (post manual user test 2026-05-01, secondo bug segnalato): `.button-secondary` aveva `-fx-background-color: transparent` per design "wood-frame outline / no fill" — ma posizionato dentro un `.card-elevated` (cream parchment `#F0E0C4`) con `-fx-text-fill: -color-text-on-dark` (cream stesso) il testo risultava invisibile (cream-on-cream, ~1.0:1 contrast). Caso concreto: bottone "Indietro" su `board-view.fxml` annidato in `card-elevated`. Richiesta utente: "il colore di sfondo dovrebbe essere allineato a quello degli altri". Fix: `.button-secondary` ora usa `-color-bg-surface` (deep walnut `#3D2817`) come fill di default, identico a `.button` standard, mantenendo la differenziazione tramite `-fx-border-width: 1.5` (vs 1 del default) e bordo che vira oro su hover. Hover/pressed allineati con derive ±8% sull'`-color-bg-surface` invece dei rgba gold-tinted. Contrast text/bg ora `#F0E0C4 / #3D2817` ≈ 11:1, ampiamente WCAG AAA. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS.

- **Task 3.5.2 readability follow-up** (post manual user test 2026-05-01): `theme-light.css` aveva selettori troppo larghi `.card .label` / `.card-elevated .label` / `.popover .label` che matchavano anche il `.label` interno del `Button` JavaFX (struttura skin del controllo), sovrascrivendo `-fx-text-fill` del bottone (cream `#F0E0C4`) con `-color-text-on-light` (dark-roast `#2A1F15`) → testo dark-roast su sfondo deep-walnut del pulsante → invisibile (~1.5:1 contrast). Fix: ristretti i selettori alle utility class esplicite `.card-elevated .label-title, .label-subtitle, .label-secondary` (idem per `.card` e `.popover`); il commento sopra il blocco documenta il rationale. Inoltre aggiunto styling completo del `MenuBar` + `MenuItem` + `ContextMenu` + `Tooltip` (mai stilizzati per il tema dark — i default JavaFX assumono ambiente light): menu bar `bg-surface` deep walnut con label cream, dropdown `bg-elevated` cream con label dark-roast e hover `accent-gold`. Tutti i 5 stati interattivi (hover/focused/showing/disabled). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **281 test totali** invariato.

### Changed

- **SPEC.md v2.2** (2026-04-30) — pull-forward per esigenza **demo cliente Win 10/11**, applicato post-approvazione PLAN-fase-3.5 §10. CR-F3.5-001..005:
  - **§13.2 Design system**: riscritta intera sezione con direzione estetica "videogame premium wood". Token color rifatti (palette warm wood: dark-roast / cream / gold antique / deep red); aggiunti `-font-family-display` (Playfair Display serif) + token motion `-easing-out-back`; aggiunta sotto-sezione 13.2.1 (asset visuali tavola: texture legno + cornice frame) + 13.2.3 (asset licensing CC0/CC-BY con CREDITS.md inventory). Inter resta UI font.
  - **§13.3 Animazioni**: separata in due blocchi. "Animazioni base (Fase 3)" preservato (250/200/500/800ms OUT_QUAD). "Animazioni avanzate (Fase 3.5)" aggiunge: mossa con OUT_BACK (overshoot juicy), particle puff su cattura (8-12 nodi, 350ms), raggi dorati radiali su promozione (8-12, 600ms), glow halo DropShadow esterno cycle 1200ms su cattura obbligatoria. Niente camera shake (escluso per non distrarre).
  - **§13.4 Audio**: separato SFX (preservato) + music ambient (Fase 3.5). Music: 3-5 tracce orchestrali soft, playlist random shuffle, default 30%, mutabile, persistence in `config.json` (`musicVolumePercent`/`sfxVolumePercent`/`musicMuted`/`sfxMuted`). Stile dichiarato "tranquillo da concentrazione, no battle/heroic".
  - **§16 Fase 3.5** (nuova): mini-fase intermedia tra F3 e F4, pull-forward parziale di F11, con acceptance "cliente apre installer Win, gioca demo SP completa, percepisce stile videogame premium + audio coerente".
  - **§16 Fase 11**: rimosse "Audio + animazioni avanzate" (anticipate F3.5) e jpackage Windows (anticipato F3.5). Restano in F11 jpackage Mac+Linux + dark toggle + replay + profilo + report + monitoring + docker.
  - **Frontmatter**: bump 2.1 → 2.2 con storico versioni inline.
  - **Decisione utente** (2026-04-30, in-thread): "PLAN ok, SPEC ok, branch+tag ok, parti".

- **SPEC.md v2.1** (2026-04-30) — clarifiche post-REVIEW Fase 3, applicate a chiusura del flusso CR (CLAUDE.md §1):
  - **§4.1 FR-SP-06**: aggiunta nota di granularità undo/redo: una coppia (mossa umana + risposta IA) annullata come unità singola, depth limitata solo dalla memoria. Allinea SPEC alla semantica fissata in PLAN-fase-3 §7.13 (stop point opzione A) e implementata in Task 3.24 (commit `74de2af`).
  - **§13.5 Accessibilità**: aggiunta annotazione "*Implementazione per fase*" che documenta esplicitamente quanto è in F3 (`accessibleText` inglese univoco, scaling 100/125/150, keyboard nav board) e cosa è **deferred F11** (localizzazione IT/EN dei testi `accessibleText`, pattern daltonismo). Risolve il REQUIREMENT_GAP latente che la REVIEW Fase 3 ha evidenziato come finding F-005 (DOC_GAP).
  - **§16 Fase 11**: aggiunto bullet "Deferral consci da Fase 3" che enumera le voci formalmente deferred dalla REVIEW Fase 3: a11y i18n, daltonismo, misura formale 60 FPS (NFR-P-01 / AC §17.2.3), verifica tool-based contrasto WCAG AA su entrambi i temi (NFR-U-04 / AC §17.2.7).
  - **Frontmatter**: bump versione 2.0 → 2.1 con storico versioni inline.
  - **Decisione utente** (2026-04-30, in-thread): "S1 ok, S2 ok, S3 ok, S4 ok". Nessuna SPEC change request lasciata pendente.

### Notes

- **TEST sotto-fase Fase 3 — DEBITO ESTINTO il 2026-04-30** come **Task 3.5.0** dentro Fase 3.5 (deviazione una-tantum dal workflow CLAUDE.md §2 autorizzata in fase di chiusura F3). Output:
  - `tests/TEST-PLAN-fase-3.md` finalizzato (commit pending Task 3.5.0): popolate sezioni 2 (composizione test 46 file × 280 test, coverage 74.18%/60.95% line/branch, SAST clean), 3 (corpus regole F1 48 + tactical F2 5 invariati), 4 (naming convention con esempi), 5 (tag JUnit `slow`+`performance` ereditati da F2), 6 (TRACEABILITY history Task 3.23 + Task 3.24 + F-003 closure), 8 (regression cross-modulo full-tag), 9 (debiti F3-D1..F3-D6), 10 (closure checklist 7/8 voci spuntate; voce manuale §7.4 esplicitamente deferita all'utente come prerequisito di Task 3.5.6).
  - **Regressione full-tag** `mvn clean verify` (root, con `slow`+`performance`) eseguita su commit `fc7b68c`: BUILD SUCCESS in **16:25 min**, **673/673 test verdi** (shared 391 + core-server 1 + client 280 + server 1, 0 failures / 0 errors / 0 skipped). Gating F2 `AiTournamentSimulationTest#campionWinsAtLeast95OutOf100AgainstPrincipiante` PASS (885.2s wall-clock, soglia ≥ 95/100). Gate JaCoCo, Spotless, SpotBugs, Maven Enforcer tutti verdi.
  - **Voce residua deliberatamente aperta**: §7.4 (log validazione manuale A3.3) — l'utente eseguirà la procedura `mvn -pl client javafx:run` partita full vs Esperto **prima dell'inizio del Task 3.5.6** di F3.5 (visual reskinning della board), perché valida la UI corrente "gestionale" di F3 prima di sostituirla.

### Added

- **Task 3.5.3** — Skeleton package audio (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - Nuovo package `com.damaitaliana.client.audio` con 6 file di produzione: **`package-info.java`** (overview SPEC §13.4 + ADR-035 + scope F3.5: SFX one-shot + ambient music random-shuffle); **`AudioBus.java`** (enum a 2 valori MUSIC/SFX per le due bus indipendenti); **`Sfx.java`** (enum a 6 valori MOVE/CAPTURE/PROMOTION/ILLEGAL/VICTORY/DEFEAT con `resourcePath()` che mappa a `/assets/audio/sfx/{move,capture,promotion,illegal,win,lose}.ogg` — i 6 file CC0 committati in Task 3.5.1; deviazione dal PLAN-fase-3.5 §3.5.3 originale che listava 4 valori — espansione a 6 motivata dalla coerenza con gli asset acquisiti, documentata in `AI_CONTEXT.md`); **`MusicPlaylist.java`** (final class con `next()` e `reset()`, deck con shuffle deterministico — `RandomGenerator` injectable, anti-back-to-back-repeat across reshuffle boundary swap, lista immutabile via `List.copyOf`); **`AudioService.java`** (interface con 7 metodi: playMusicShuffle/stopMusic/playSfx/setVolume/volume/setMuted/isMuted); **`JavaFxAudioService.java`** (`@Component` skeleton — tutti i no-op log DEBUG con TODO Task 3.5.4, defaults SPEC §13.4 30/70 per music/SFX volume, isMuted=false). Nessuna logica reale di playback — solo i tipi e la firma; Task 3.5.4 cabla `MediaPlayer` JavaFX + `PreferencesService` persistence.
  - **14 nuovi test** in `client/src/test/java/com/damaitaliana/client/audio/`: `AudioBusTest` (1 test, values), `SfxTest` (4 test: 6 enum values + classpath resource path format + uniqueness + bundled-on-classpath check via getResource), `MusicPlaylistTest` (6 test: empty rejection + single-track loop + cycle through all + no back-to-back across reshuffle + reset + size), `JavaFxAudioServiceTest` (3 test: SPEC default volumes 30/70, isMuted=false, no-op methods don't throw).
  - **Bean Spring**: `JavaFxAudioService` resolvabile come `AudioService` autowired bean — Task 3.5.4 lo inietterà in `SettingsController` (sliders + toggles) e Task 3.5.5 lo inietterà in `SinglePlayerController` (SFX dispatch).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **295 test totali** (+14 vs Task 3.5.2: 1+4+6+3 nuovi), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task 3.5.2** — Design tokens v2 (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - **`theme-light.css`** riscritto con palette **wood premium** SPEC §13.2 (Fase 3.5): `bg-primary #2A1F15` (dark roast), `bg-surface #3D2E20` (deep walnut), `bg-elevated #F5E6C8` (cream parchment), `border-subtle #6B4423` (warm brown), `border-frame #4A2E18` (dark frame), `text-on-dark #F0E0C4` (cream), `text-on-light #2A1F15` (dark roast), `text-secondary #8B6F4E` (muted brown), `accent-gold #C9A45C` (antique gold), `accent-gold-hover #DDB874`, `accent-deep-red #8B3A3A`. Damiera fallback (pre-texture in 3.5.6): `board-light #E8C99A`, `board-dark #6B4423`. Pezzi: `piece-white #F0E0C4`, `piece-black #2A1F15`. Marker dame: gold su bianco, deep red su nero. Highlight legali e mandatory entrambi gold. Sostituiti i token vecchi `text-primary`/`accent`/`accent-hover` con il nuovo vocabolario duale `text-on-dark`/`text-on-light` per gestire le superfici miste (sfondo dark + card cream).
  - **Tipografia**: aggiunta font-family chain serif per display titles `"Playfair Display", "Cormorant Garamond", "Georgia", serif` referenziata da `.label-display` + variants `.label-display-md` (32 px) / `.label-display-lg` (48 px) per hero titles in antique gold. Inter resta UI base.
  - **Nuove classi pulsante**: `.button-primary` (gradient `linear-gradient(to bottom, accent-gold-hover, accent-gold)` + bevel `innershadow rgba(255,255,255,0.25) + dropshadow rgba(0,0,0,0.30)` + hover glow `dropshadow accent-gold radius 16` per WCAG AA su gold dark-roast text); `.button-secondary` ridisegnato (transparent fill, wood-frame outline 1.5 px, hover `rgba(201,164,92,0.10)` + border accent-gold). Default `.button` rimappato a wood-surface frame neutro per CTA secondari/menu items.
  - **Card / popover / list-cell / text-field**: aggiornati per gestire l'ambiente cream del bg-elevated (foreground dark-roast); selected list-cell usa accent-gold con dark-roast text. Stati interattivi (default + hover + pressed + focused + disabled) preservati su tutti i componenti per SPEC §13.2.2.
  - **`components.css`** rimappato al nuovo vocabolario: `board-cell-selected` ora `innershadow accent-gold`; `piece` stroke `text-on-light`; `piece-king` stroke `accent-gold`; `cell-focused-keyboard` border `accent-gold`; `move-history-row-current` background `derive(bg-surface, 10%)` (era -5% — rovesciato perché il nuovo bg-surface è dark e serve schiarire per evidenziare). Token `-color-board-light/dark` mantenuti come fallback prima del wiring `BackgroundImage` (Task 3.5.6).
  - **`theme-dark.css`** stub aggiornato con stesso vocabolario light per evitare token drift quando F11 attiverà il toggle: `bg-primary #1A120A` (deeper roast), valori coerenti per tutti i token. Stub-only — F11 farà tuning con contrast-checker.
  - **Font binari committati** in `client/src/main/resources/fonts/`: `InterVariable.ttf` (859 KB, weight axis Thin..Black, da rsms/inter master), `PlayfairDisplay-Variable.ttf` (294 KB, weight axis Regular..Black, da google/fonts ofl/playfairdisplay). Entrambi SIL OFL 1.1 con `Inter-OFL.txt` (4.4 KB) e `PlayfairDisplay-OFL.txt` (4.5 KB) committati alongside per OFL compliance (l'unico obbligo OFL: redistribuire la license text).
  - **`ThemeService`**: rimossi i 2 constants `INTER_REGULAR_PATH`/`INTER_SEMIBOLD_PATH` (file mai esistiti), aggiunti `INTER_VARIABLE_PATH` e `PLAYFAIR_DISPLAY_VARIABLE_PATH`. `ensureFontsLoaded()` ora carica entrambi via `Font.loadFont`. Javadoc aggiornato per riflettere font-family chain dual (UI + display) e bundling F3.5. `fonts/README.md` riscritto: i font ora sono **bundled** (non più "optional"), tabella delle famiglie + sorgenti + license, fallback chain documentata per il caso edge in cui il classpath è strippato.
  - **`CREDITS.md`** esteso con sezione **Fonts** (Inter + Playfair Display, OFL 1.1, sorgenti GitHub raw, autori). Frontmatter `license-policy` aggiornato per esplicitare la doppia regola: CC0 per visual+audio, OFL per fonts (per CLAUDE.md §1 / PLAN §3.2 "asset CC-BY/OFL").
  - **`ThemeServiceTest`**: 9 test (era 8) — rinominato `interFontResourcesAreOptionalAndAbsenceIsAcceptable` → `fontBinariesArePresentAndLoadOnFirstApply` (i font ora sono bundled, l'asserzione di "absence acceptable" non si applica più). Riscritto `themeLightDefinesAllSpecColorTokens` → `themeLightDefinesWoodPremiumColorTokens` con 20 token check sul palette F3.5. Riscritto `themeDarkStubDefinesDarkPalette` → `themeDarkStubDefinesDarkWoodPalette`. Aggiunti `themeLightDefinesPrimaryAndSecondaryButtonAndDisplayLabel` (5 stati `.button-primary` + `.button-secondary` + `.label-display`) e `themeLightDefinesFontFamilyChainsForUiAndDisplay` (entrambe le chain Inter UI + Playfair display).
  - **Stop point §7.2 PLAN-fase-3.5**: post-Task 3.5.2 si attende approvazione utente prima di procedere col restyling delle 8 schermate (Tasks 3.5.6, 3.5.9-3.5.11). I tasks intermedi audio (3.5.3-3.5.5) sono visivamente indipendenti.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 1:08, **281 test totali** (+1 vs Task 3.24/3.5.1: il +2 nuovi test sui display label + button primary, -1 test rinominato), JaCoCo client gate ✅ (haltOnFailure=true, bundle 63 classi), SpotBugs 0 High, Spotless OK.

- **Task 3.5.1** — Asset acquisition + CREDITS.md (Fase 3.5, branch `feature/3.5-visual-polish-and-audio`):
  - **Visual textures** (3 file, 8.32 MB totali) in `client/src/main/resources/assets/textures/`: `board_dark.jpg` (Poly Haven `dark_wooden_planks` 2K JPG diffuse, 2.75 MB), `board_light.jpg` (Poly Haven `oak_wood_planks` 2K JPG diffuse, 2.61 MB), `frame.jpg` (Poly Haven `wood_table_worn` 2K JPG diffuse, 2.96 MB). Tutti CC0 1.0 (verifica licenza per asset 2026-05-01).
  - **SFX** (6 file, ~70 KB totali) in `client/src/main/resources/assets/audio/sfx/`: `move.ogg` (Kenney `impactWood_light_000`, 266 ms), `capture.ogg` (Kenney `impactWood_heavy_000`, 313 ms), `promotion.ogg` (Kenney `confirmation_002`, 539 ms), `illegal.ogg` (Kenney `error_001`, 165 ms), `win.ogg` (Kenney `jingles_PIZZI07`, 1.32 s), `lose.ogg` (Kenney `jingles_PIZZI03`, 1.15 s). Durate **verificate via mutagen** (Python OGG metadata reader) su download diagnostico in temp (no commit del temp).
  - **Music** (4 tracce: Calm Piano 1 / First Light Particles / At Home Orchestral / Peaceful Forest da OpenGameArt CC0): selezioni e URL definitivi documentati in `CREDITS.md`. **Binari deferred a Task 3.5.4** (richiede ffmpeg per re-encoding WAV→OGG; il setup ffmpeg portable in 3.5.1 ha avuto download non completato e si è scelto di delegare il re-encoding all'integrazione AudioService dove serve naturalmente).
  - **`CREDITS.md`** in root del repo: inventario completo asset CC0 con frontmatter (license-policy, last-updated), sezioni Visual / Music / SFX (per ogni asset: local file, source page URL, direct file URL, license, author, durata o size on disk), audit trail della verifica, lista esclusioni (Patreon-locked walnut_veneer, 404 wood_planks_027/028, freepd.com chiuso 2025, tutto CC-BY).
  - **Stop point §7.1 PLAN-fase-3.5 chiuso**: shortlist iniziale presentata 2026-05-01, dubbio utente su durate SFX risolto via probe mutagen su download diagnostico (smentito definitivamente "1 minuto" → file più lungo è jingles_SAX07 a 1.74 s; pizzicato max 1.32 s), pacchetto definitivo approvato dall'utente in-thread.
  - **Asset esclusi durante ricerca** (per trasparenza, vedi `CREDITS.md`): walnut_veneer (Wood Vault Patreon), wood_planks_027/028 (404 — il primo agent visual aveva allucinato gli ID; correzione via WebSearch), freepd.com (sito chiuso 2025), incompetech default (CC-BY non CC0), Mixkit/Pixabay (custom license non CC0), pizzicato jingles <1 s (troppo corti come fanfara), Hit/Sax/Steel/8-Bit jingles (mood non aderente a SPEC §13.4 "videogame premium classico, no battle/heroic").

- **Task 3.24** — Undo/redo della coppia umana+IA (Fase 3, fix REVIEW finding F-001 opzione A, branch `feature/3-ui-singleplayer`):
  - `SinglePlayerController`: `Deque<GameState> undoStack`/`redoStack`, push del pre-state SOLO sulle mosse umane (filtrato da `sideThatMoved == game.humanColor()` in `finalizeMove`); le mosse IA non pushano (il loro pre-state è già parte della coppia). API pubblica: `undoPair()` / `redoPair()` / `canUndo()` / `canRedo()` / `undoState()` (observable).
  - Nuovo `client/controller/UndoState.java`: pattern matchea `AiThinkingState`, ma usa `BiConsumer<Boolean, Boolean>` listener per i due flag compound `(canUndo, canRedo)` settati insieme via `set(boolean, boolean)`. Niente JavaFX BooleanProperty per restare testabile sul thread del test.
  - `MoveHistoryViewModel.replaceWithHistory(List<Move>)`: ricostruisce le row da una history arbitraria alternando WHITE/BLACK partendo da WHITE (Italian Draughts ADR-013, primo turno sempre bianco).
  - `BoardViewController`: aggiunti `@FXML MenuItem undoMenuItem`/`redoMenuItem` con accelerator `Ctrl+Z` / `Ctrl+Y` (cross-platform via `KeyCombination.SHORTCUT_DOWN`); listener su `gameController.undoState()` aggiorna il `setDisable` dei menu item; deleghe `onUndo()` / `onRedo()` al controller.
  - `client/src/main/resources/fxml/board-view.fxml`: aggiunti i 2 `MenuItem` in cima al menu Partita.
  - i18n IT/EN: `board.menu.undo` ("Annulla mossa" / "Undo move"), `board.menu.redo` ("Ripeti mossa" / "Redo move").
  - 16 nuovi test: 9 unit in `SinglePlayerControllerTest` (push solo human, redo dopo undo, redo cleared on new move, busy guard durante AI thinking, observable propagation), 3 in `BoardViewControllerTest` (delegation onUndo/onRedo, no-op when controller null), 3 in `MoveHistoryViewModelTest` (`replaceWithHistory*`), 1 E2E in `SinglePlayerE2ETest#undoRedoCycleRestoresAndReappliesHumanMove`.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **280 test totali** (+16 vs Task 3.23), JaCoCo line 74.18% / branch 60.95%, SpotBugs 0 High, Spotless OK.
  - Risolve REVIEW-fase-3 finding **F-001 [REQUIREMENT_GAP, High]** (commit `74de2af`).

- **REVIEW Fase 3 closure** (commit `6eeb332`, file `reviews/REVIEW-fase-3.md`): tutti i 5 findings RESOLVED.
  - **F-001 High** (FR-SP-06 Undo/redo non implementato) → opzione A, Task 3.24, commit `74de2af`.
  - **F-002 Medium** (A3.3 partita end-to-end light) → opzione B, procedura manuale documentata in `tests/TEST-PLAN-fase-3.md` §7 (commit `603306f`).
  - **F-003 / F-004 / F-005 Low** (DOC_GAP: TRACEABILITY drift, ADR-031 wording, a11y i18n deferral comment) → singolo commit `docs(claude)` `59bedc7`.
  - **CR-001 WITHDRAWN** (rimasta PENDING solo finché F-001 non era deciso; opzione A → no SPEC change).

- **Task 3.23** — Documentazione, ADR, TRACEABILITY, README, CHANGELOG (Fase 3, chiusura sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `ARCHITECTURE.md`: appendiati 5 ADR per le decisioni di design emerse durante l'IMPLEMENTA: **ADR-029** (bootstrap JavaFX con Spring Boot DI non-web — `ClientApplication` Spring main → `Application.launch(JavaFxApp.class)` con `JavaFxAppContextHolder` come bridge singleton), **ADR-030** (architettura schermate FXML + `setControllerFactory(applicationContext::getBean)` con controller `@Component @Scope("prototype")` per evitare reset di `@FXML` al re-load — vedi anche memory `feedback_spring_ui_tests`), **ADR-031** (schema saves v1: JSON con `schemaVersion`, `kind`, `aiLevel`, `humanColor`, `currentState` riusando `SerializedGameState` 4-FID-list e `moves` future-proof, rifiuto `schemaVersion` ignote via `UnknownSchemaVersionException` tipata), **ADR-032** (autosave atomico tramite write-temp + `Files.move(tmp, target, ATOMIC_MOVE, REPLACE_EXISTING)` con fallback `REPLACE_EXISTING` solo per FS che non supportano `ATOMIC_MOVE` + `SinglePlayerAutosaveTrigger` failure-tolerant che swallowa `UncheckedIOException` con log WARN), **ADR-033** (localizzazione: `MessageSource` Spring + `LocaleService` bridge + `I18n` wrapper con `[code]` placeholder per chiavi mancanti; default locale Italian fallback chain, encoding UTF-8 esplicito; cambio lingua richiede riavvio in F3, runtime dynamic in F11; chiavi gerarchiche `screen.element.role`).
  - `client/.../package-info.java`: aggiunti i 4 file mancanti per chiudere la copertura A3.15: `ui` (top-level container con riferimenti incrociati ai sotto-package), `ui.save` (save dialog + load screen, MiniatureRenderer riusato per i diagrammi delle regole), `ui.settings` (settings screen con UiScalingService global re-apply), `ui.rules` (FR-RUL-01..05). I package-info di `app`, `controller`, `i18n`, `persistence`, `ui.menu`, `ui.setup`, `ui.board`, `ui.board.animation`, `ui.splash` erano già stati scritti task-by-task durante l'IMPLEMENTA.
  - `tests/TRACEABILITY.md`: aggiunte righe per **FR-SP-01..09** (avvio SP, 3 livelli, scelta colore, highlight legali, highlight obbligatorie, undo/redo, multi-slot save, autosave + recovery, cronologia FID), **FR-RUL-02..05** (navigazione 7 sezioni, ≥5 diagrammi su ≥4 sezioni, mini-animazioni, localizzazione testi regole), **NFR-M-02** (gate client 60% con 9 esclusioni esplicite, line 73.94% / branch 60.55%), **NFR-U-01** (LocalizationE2ETest 87 chiavi IT+EN), **NFR-U-04** (light mode WCAG manual), **AC §17.1.9** (multi-slot save), **AC §17.1.12** (rules screen). Nuova sezione "Acceptance criteria di Fase 3 (PLAN-fase-3.md §2.2)" con tutte le 21 voci A3.1..A3.21 mappate ai test/output corrispondenti.
  - `README.md`: aggiunta sezione **"Eseguire il client (Fase 3+)"** con `mvn -pl client javafx:run`, prerequisito `mvn install` parent+shared+core-server, tabella storage utente (`~/.dama-italiana/{config.json, saves/<slot>.json, saves/_autosave.json}`), nota toggle lingua IT/EN richiede riavvio in F3 (ADR-033), nota fast loop `mvn -pl client verify -DexcludedGroups=slow,performance` e Headless guard `Assumptions.assumeTrue(fxToolkitReady)`. Stato del progetto aggiornato a **"Fase 3 — Client UI single-player"**.
  - `AI_CONTEXT.md`: stato avanzato a "Fase 3 — IMPLEMENTA completa, REVIEW pending"; "Prossimo task" → REVIEW Fase 3.
  - **Nessun nuovo test** prodotto (per design del piano: documentazione + traceability).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **264 test totali** (invariato vs Task 3.22), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Fase 3 summary** — Client UI single-player completo (sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`, da Task 3.1 a Task 3.23):
  - **Bootstrap & infrastructure** (Task 3.1-3.5): Spring Boot non-web + `Application.launch`, design system (`theme-light.css`, font Inter optional, Ikonli), localizzazione IT/EN (87 chiavi, MessageSource + LocaleService + I18n + bundle parity), preferences service (`config.json`), splash con autosave detection.
  - **Main flows** (Task 3.6-3.13): main menu 6-card (3 wired, 3 disabled), single-player setup modal, BoardRenderer + side panel, click-to-select interaction con highlight legal-target/pulse-mandatory, animation engine (translate + fade + promotion overlay), move history pane FID-encoded, status pane (player + side-to-move + endgame banner), AI integration async via virtual thread executor.
  - **Persistence** (Task 3.14-3.16): SaveService multi-slot + autosave atomico + AutosaveService + recovery prompt al riavvio.
  - **Auxiliary screens** (Task 3.17-3.19): settings (lingua, scaling 100/125/150, tema light placeholder), rules screen 7 sezioni con 6 diagrammi su 4 sezioni, 3 mini-animazioni regole (FR-RUL-04 opzionale).
  - **Quality** (Task 3.20-3.23): accessibility (accessibleText, keyboard navigation, scaling globale via UiScalingService), TestFX/E2E single-player tests (LocalizationE2ETest, SinglePlayerE2ETest, SaveLoadE2ETest, RulesScreenE2ETest), JaCoCo gate 60% line+branch (haltOnFailure=true) con 9 esclusioni esplicite, documentazione (5 ADR, package-info, TRACEABILITY, README, CHANGELOG).
  - Risultato finale: **264 test JUnit5** sul modulo `client` (di cui 10 E2E nuovi in Task 3.21), 0 skipped, 0 failed. Coverage bundle dopo esclusioni: line 73.94%, branch 60.55%, instructions 73.42%. SpotBugs 0 High. Spotless verde.
  - Pendenti per la sotto-fase REVIEW (Task 3.24+): `mvn clean verify` (root) check di regressione su `shared` (corpus 48 + gating IA F2 slow), revisione manuale degli AC end-to-end (`mvn -pl client javafx:run` partita completa vs Esperto, save/load round-trip, autosave recovery), eventuali findings BLOCKER/REQUIREMENT_GAP/BUG/SECURITY.

- **Task 3.22** — Coverage gate JaCoCo client + cleanup (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `client/pom.xml`: configurate due execution `jacoco-maven-plugin` (override del `pluginManagement` parent). `jacoco-report` rebound a fase `verify` con `<excludes>` per filtrare le classi pure-JavaFX dal denominatore del bundle. `jacoco-check` rebound a fase `verify` con `haltOnFailure=true`, gli stessi `<excludes>` (necessario duplicarli — gli `excludes` su `prepare-agent` agiscono sull'instrumentation, ma le classi non instrumentate continuano a comparire al 0% nel report) e regola `BUNDLE` ≥ 60% line + branch (NFR-M-02).
  - **Esclusioni** documentate nel POM con commenti, tre famiglie: (1) JavaFX/Spring bootstrap glue: `JavaFxApp.class` (JavaFX `Application` subclass), `JavaFxAppContextHolder.class` (Spring/JavaFX bridge), `ClientApplication.class` (Spring Boot main); (2) JavaFX Alert wrapper: `JavaFxUserPromptService.class` (impl di `UserPromptService`, reachable solo su FX thread vivo — l'interface è quella mockata dai test); (3) anonymous cell-factory inner classes: `MoveHistoryView$MoveHistoryCell`, `LoadScreenController$MiniatureCell` (named TableCell), `RulesController$1` (anonymous ListCell), `SettingsController$1` (anonymous StringConverter), `SplashController$1` (anonymous javafx.concurrent.Task) — tutte istanziate dal FX runtime, no business logic.
  - Bundle dopo esclusioni: **62 classi** (-9 vs il bundle senza esclusioni), **lines 73.94% (1285/1738), branches 60.55% (284/469), instructions 73.42%**. Verifica negativa: senza le esclusioni il branch ratio è 57.4% e il gate fallisce con `BUILD FAILURE` (`branches covered ratio is 0.57, but expected minimum is 0.60`).
  - `ClientSmokeTest` già rimosso in Task 3.1 e sostituito da `ClientBootstrapTest` (4 test reali su Spring boot + SceneRouter + ClientProperties); nessun cleanup ulteriore richiesto.
  - **Nessun nuovo test** prodotto in Task 3.22 (per design del piano: "solo gate").
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **264 test totali** (invariato vs Task 3.21), JaCoCo client gate ✅ (haltOnFailure=true), SpotBugs 0 High, Spotless OK.

- **Task 3.21** — TestFX E2E single-player tests (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `client/i18n/LocalizationE2ETest` (4 test): pinning della contract controller→bundle. Lista hard-coded di 87 chiavi i18n referenziate dai controller (menu, setup, board chrome, save dialog, load screen, settings, rules) verificate `isNotBlank().doesNotStartWith("[")` in IT e EN tramite `MessageSourceConfig.buildMessageSource()`. Spot-check distinguibilità IT/EN su `app.title` ("Dama Italiana" vs "Italian Draughts") + `menu.rules.title` ("Regole" vs "Rules"). Placeholder check su `status.player.color` (`{0}`/`{1}`) e `setup.name.default` (`{0}`) → `MessageFormat` substitution funziona in entrambi i locale.
  - `client/controller/SinglePlayerE2ETest` (3 test): copertura dei flussi UI single-player con `BoardRenderer` Mockito-mocked (animation path short-circuita a `finalizeMove` sincrono, `renderer.pieceAt` ritorna null → no JavaFX runtime). `manCannotCaptureKingInUi` (A3.11/SPEC §3.3): hand-built whiteMan@FID22 + blackKing@FID18, click sulla pedina, `ArgumentCaptor` su `highlightLegalTargets` verifica che `kingSquare` non sia tra gli highlight. `promotionStopsSequenceInUi` (A3.12/SPEC §3.5): whiteMan@FID5 → 1 (rank 7), dopo i 2 click `sideToMove==BLACK` + `board.at(to).get().kind()==PieceKind.KING` + `board.at(from).isEmpty()`. `humanFirstMoveAdvancesGameStateAndHistory` (A3.3-light): stato iniziale, prima mossa White (Move dalla `RuleEngine.legalMoves`) → `sideToMove==BLACK` + `history().size==1` + `status.isOngoing()`.
  - `client/persistence/SaveLoadE2ETest` (2 test): copertura A3.4 save/load round-trip. `saveThenLoadResumesAtSameState`: nuova `SinglePlayerGame` PRINCIPIANTE+WHITE+seed 7L, applica due plies via helper `applyOneMove` (stessa tecnica di `AutosaveE2ETest`: `RULES.legalMoves(state).first` + `RULES.applyMove`), persiste `SavedGame.of(snapshot, instant, instant)` su `SaveService` con `slugify(name)`, verifica `saveService.listSlots().slot()` contiene lo slug, ricarica via `saveService.load(slot).orElseThrow` + `SinglePlayerGame.fromSaved(loaded, new SplittableRandom(99L))` e asserta byte-equality su name/level/humanColor/board/sideToMove/halfmoveClock + parità di lunghezza history. `deleteRemovesSlotFromListing`: save → listSlots ha lo slot → `saveService.delete(slot)` → listSlots non lo contiene + `saveService.load(slot).isEmpty()`. Tempdir gestito via `@TempDir` JUnit5.
  - `client/ui/rules/RulesScreenE2ETest` (1 test, A3.10 `openRulesAndNavigateSections`): `@SpringBootTest classes=ClientApplication.class webEnvironment=NONE` + `@TestPropertySource("dama.client.saves-dir=/tmp/test-saves-rules-e2e")`; `Platform.startup` con guard `Assumptions.assumeTrue(fxToolkitReady)` per skip headless; `FXMLLoader.load("/fxml/rules.fxml")` con `setControllerFactory(springContext::getBean)` su FX thread via `runOnFxThread(Callable)` + CountDownLatch. Default `currentSection==SETUP`, poi itera su `RuleSection.ALL` (7 sezioni in display order), `selectSection` su FX thread, asserta `currentSection==section` + `contentChildren().get(0)` è Label il cui text == `i18n.t(section.titleKey())` + `contentChildren.size>=2` (header + ≥1 paragrafo).
  - **Decisione di packaging**: i test sono collocati nei package del modulo SUT (mirroring `AutosaveE2ETest` in `client.persistence`), non sotto un `client.e2e/` separato. La motivazione è preservare la package-private visibility dei "Visible for testing" accessor (`SinglePlayerController.state/onCellClicked/selectedSquare`, `RulesController.currentSection/selectSection/contentChildren`) senza modificare la production API.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **264 test totali** (+10 vs Task 3.20: 4+3+2+1 nuovi), 0 skipped, JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task 3.20** — Accessibility (accessibleText, keyboard nav, scaling globale) (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - **Scaling globale**: nuovo `client/app/UiScalingService.java` (`@Component`) con `applyTo(Scene)` (legge `PreferencesService.load().uiScalePercent()`) e `applyTo(Scene, int)` (preview esplicito); whitelist `{100, 125, 150}` con fallback 100 + log WARN. `SceneRouter.show` invoca `uiScalingService.applyTo(scene)` dopo `themeService.applyTheme(scene)` → la preferenza scaling viene riapplicata su **ogni** navigazione (chiude la limitazione documentata in Task 3.17).
  - `SettingsController` ridisegnato: ctor +`UiScalingService` arg, `onScalingChanged` delega a `uiScalingService.applyTo(scene, percent)` (rimossa la copia statica `applyScalingToScene` + `BASE_FONT_SIZE_PX`). Single source of truth per lo scaling.
  - **`accessibleText` su BoardCellNode**: nuovo helper `client/ui/board/CellAccessibleText.java` con format inglese-only (PieceAccessibleText style): light → "Light square (not playable)", dark vuota → "Dark square <FID>, empty", dark con pezzo → "Dark square <FID>, <pieceDesc>" (riusa `PieceAccessibleText.describe`). `BoardCellNode` chiama `setAccessibleText` al ctor e ad ogni `setPiece` per restare in sincrono.
  - **Keyboard navigation board** (NFR-U-03): `BoardRenderer` ora `setFocusTraversable(true)` con `accessibleText="Italian draughts board, 8 by 8 grid"`. Tracking di una `Square keyboardFocus` con CSS class `cell-focused-keyboard` (border accent in `components.css`). Handler `KeyEvent.KEY_PRESSED`: arrow keys muovono il focus di **due passi** sull'asse (next dark cell), Enter dispatcha il click handler sulla cella focalizzata, Esc invoca un `Runnable escapeHandler` opzionale. Auto-focus iniziale a `(0,0)` quando il renderer riceve focus la prima volta. `SinglePlayerController.start` ora wira `renderer.setOnEscape(this::deselect)` → Esc deseleziona pezzi+highlights. `BoardViewController.initialize` chiama `Platform.runLater(boardRenderer::requestFocus)` dopo `gameController.start` per dare focus alla board appena entrati.
  - **16 nuovi test**: `UiScalingServiceTest` 5 (null-scene no-op, applies percent to root via `-fx-font-size: 17.5px` per 125%, unknown percent fallback a 100=14px, `applyTo(scene)` legge prefs e applica 150=21px, `ALLOWED_SCALES==[100,125,150]`); `CellAccessibleTextTest` 4 (light, dark empty, white man, black king); `BoardKeyboardNavigationTest` 7 (focus traversable, initial focus null, RIGHT da (0,0)→(2,0), UP→(0,2), edge clamp, ENTER→click handler, ESC→escape handler — tutti su JavaFX thread con `Platform.startup` + `Assumptions.assumeTrue` per skip headless). `SettingsControllerTest` -1 (rimossa `applyScalingToSceneIsNullSafe` ora coperta da `UiScalingServiceTest#nullSceneIsNoOp`); ctor signature +`UiScalingService` mock.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **254 test totali** (+15 vs Task 3.19), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task 3.19** — Mini-animazioni regole (FR-RUL-04, opzionale §7.6 → incluso) (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `client/ui/rules/RulesAnimations.java` (`@Component`): catalogo statico di 3 animazioni dimostrative con `enum Kind { SIMPLE_CAPTURE, MULTI_CAPTURE, PROMOTION }`. Ogni `Kind` espone `startingPosition(Kind) → SerializedGameState`, `move(Kind) → Move`, `captionKey(Kind) → String`, e `animation(Kind, Function<Square,Node>, double cellSize) → Animation`. Mosse in coordinate FID legali secondo le regole italiane: SIMPLE_CAPTURE = `22x15` saltando `18` (whiteMen [22], blackMen [18]); MULTI_CAPTURE = `26x17x10` saltando `22` poi `14` (whiteMen [26], blackMen [22, 14]); PROMOTION = `5-1` (whiteMen [5], rank 6 → 7). Riusa `AnimationOrchestrator.animateMove` per le slide-and-fade legs e wrappa la PROMOTION in `SequentialTransition(slide, MoveAnimator.promotion(piece, flashOverlay))` (rotate 360° + flash dorato 500 ms su `Rectangle` overlay, SPEC §13.3).
  - `RulesController` esteso: ctor +`RulesAnimations` arg. Per `CAPTURE` (2 widget: SIMPLE_CAPTURE + MULTI_CAPTURE) e `PROMOTION` (1 widget) renderSection costruisce un `VBox` con un `BoardRenderer` 280×280 inizializzato sulla starting position + caption `label-secondary` + pulsante "Riproduci" (`button-secondary`); il button click resetta la board, costruisce l'`Animation` via `animations.animation(...)` con `board::pieceAt` e `board.currentCellSize()` come resolver, disabilita il button durante il play e lo riattiva su `setOnFinished`. `animationKindsFor(RuleSection)` static + visible-for-test mappa CAPTURE → [SIMPLE_CAPTURE, MULTI_CAPTURE], PROMOTION → [PROMOTION], altre sezioni → empty.
  - **i18n** IT/EN: `rules.animation.play` (Riproduci/Play), `rules.animation.{simple_capture,multi_capture,promotion}.caption`.
  - **9 nuovi test**: `RulesAnimationsTest` 8 (SIMPLE_CAPTURE describes man jumping forward, MULTI_CAPTURE chains 2 jumps, PROMOTION moves to rank 7, caption keys unique+scoped, cellSize≤0 throws IAE, simpleCapture builds animation that plays without errors via `setRate(50)` + `setOnFinished` latch su JavaFX thread, MULTI_CAPTURE ritorna `SequentialTransition` con 2 children, PROMOTION ha `SequentialTransition(slide, promo)` con `promo.totalDuration == MoveAnimator.PROMOTION_DURATION == 500ms`); `RulesControllerTest` +1 (`animationKindsFor` returns CAPTURE→[SIMPLE,MULTI] / PROMOTION→[PROMOTION] / others→empty).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **239 test totali** (+9 vs Task 3.18), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task 3.18** — Rules screen (sezioni statiche) (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `client/ui/rules/RulesController.java` (`@Component @Scope("prototype")`): controller backing `rules.fxml`. Ctor `(SceneRouter, I18n, RuleDiagramLoader, MiniatureRenderer)`. `initialize()` popola la `ListView` con le 7 sezioni (`RuleSection.ALL`), cell factory mappa via `i18n.t(section.titleKey())`, listener su `selectedItemProperty` chiama `renderSection(...)` che pulisce il `VBox` di destra e ricostruisce: header `label-subtitle` + paragrafi (split su `\n\n`) + per ogni `RuleDiagram` un `VBox` con `ImageView` (rendered via `MiniatureRenderer.render(position, 220px)`, styleClass `rule-diagram`) + caption `label-secondary`. Default selection: `RuleSection.SETUP`. `selectSection(RuleSection)` visible-for-test bypassa la ListView quando non ancora popolata.
  - `client/ui/rules/RuleSection.java` (record): id + diagramResource. 7 istanze statiche `SETUP/MOVEMENT/CAPTURE/PRECEDENCE/PROMOTION/ENDGAME/NOTATION` in `ALL` (ordine SPEC §3). 4 sezioni hanno diagrammi (`hasDiagrams()` true), 3 no. `titleKey()`/`bodyKey()` derivano `rules.section.<id>.{title,body}`.
  - `client/ui/rules/RuleDiagram.java` (record): wrapper `(captionKey, SerializedGameState position)` per la JSON deserialization.
  - `client/ui/rules/RuleDiagramLoader.java` (`@Component`): `loadFor(RuleSection)` legge classpath JSON via Jackson `TypeReference<List<RuleDiagram>>`; ritorna `List.of()` per sezioni senza diagram resource.
  - `resources/rules/{setup,movement,capture,promotion}.json`: 6 diagrammi totali (setup 1 + movement 1 + capture 2 + promotion 2) — supera il minimum di 5 across 4 sections (FR-RUL-03).
  - `resources/fxml/rules.fxml`: `BorderPane` con header `label-title` + pulsante Indietro, center `SplitPane` (dividerPositions=0.28) con `ListView` a sinistra e `ScrollPane` (`fitToWidth=true`, `card-elevated`) contenente il `VBox#contentBox` a destra.
  - `SceneRouter.fxmlPathFor`: `RULES -> "/fxml/rules.fxml"` (rimosso il placeholder null).
  - `BoardViewController.onShowRules()` + `MenuItem rulesMenuItem` in `board-view.fxml`: rules accessibile in-game (FR-RUL-01) — stop dell'AI + navigation a `SceneId.RULES`. Label localizzato via `board.menu.rules` IT/EN.
  - **i18n** IT/EN: `rules.title`, `rules.section.<id>.{title,body}` × 7 (paragrafi separati da `\n\n`), `rules.diagram.<id>.caption` × 6 (5 inglese + 1 italiano), `board.menu.rules`. Bodies tradotti dal SPEC §3.1-3.6 + §3.8.
  - **16 test nuovi**: `RuleDiagramLoaderTest` 4 (setup loads initial position, capture has 2 diagrams, sezioni senza diagrammi tornano vuote, coverage ≥ 5 diagrammi su ≥ 4 sezioni), `RulesControllerTest` 5 (display order, all 7 sections have IT+EN title+body via direct property bundle read, hasDiagrams flags, selectSection tracks current, onBack naviga MAIN_MENU), `RulesSpringContextTest` 3 (`@SpringBootTest` resolve prototype + isPrototypeScoped + RuleDiagramLoader bean — lezione `feedback_spring_ui_tests`), `RulesFxmlSmokeTest` 3 (default section is SETUP, clicking section updates content, capture section renders 2 ImageView con styleClass `rule-diagram` — tutti su JavaFX thread con `Platform.startup` + `Assumptions.assumeTrue`), `BoardViewControllerTest` +1 (`onShowRules` stops AI then navigates to RULES).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **230 test totali** (+16 vs Task 3.17), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task 3.17** — Settings screen (lingua, scaling UI) (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `client/ui/settings/SettingsController.java` (`@Component @Scope("prototype")`): controller del pannello impostazioni. Ctor `(SceneRouter, I18n, PreferencesService, UserPromptService)`. `initialize()` legge `UserPreferences` corrente e popola `ChoiceBox<Locale>` (Italiano/Inglese, display tramite `StringConverter` con chiavi `locale.italian/english`), `ToggleGroup` scaling 100/125/150 (RadioButton), `ChoiceBox<String>` tema disabilitato con singolo item "Light" + label nota "Dark mode in Fase 11". `onScalingChanged()` applica immediatamente `-fx-font-size` al root della scena (preview live, persistenza solo su Save). `onSave()` chiama `saveSelections(Locale, int)` (visible-for-test, ritorna typed enum `SaveOutcome` SAVED_RESTART_REQUIRED/SAVED_NO_RESTART_NEEDED) che merge in `UserPreferences.withLocale().withUiScalePercent()`, persiste via `PreferencesService.save`, e se il locale è cambiato mostra `prompt.info("settings.toast.locale.restart.*")` (riavvio richiesto, §7.10 opzione A); poi naviga a `SceneId.MAIN_MENU`. `onBack()` naviga senza salvare. `applyScalingToScene(Scene, int)` static + null-safe (riusabile dal global re-apply previsto in Task 3.20). Scaling validato contro la whitelist {100,125,150} con fallback a 100.
  - `resources/fxml/settings.fxml`: VBox `card-elevated` con sezioni Lingua / Scaling / Tema-stub e barra inferiore Indietro/Salva. ToggleGroup `scalingGroup` definito in `<fx:define>` come per `sp-setup.fxml`.
  - `UserPreferences.withUiScalePercent(int)` e `UserPreferences.withThemeId(String)` aggiunti come helper compatibili con `withLocale`/`withFirstLaunch`.
  - `SceneRouter.fxmlPathFor`: `SETTINGS -> "/fxml/settings.fxml"` (rimossa la mappatura placeholder `RULES, SETTINGS -> null`; `RULES` resta `null` fino a Task 3.18).
  - **i18n** IT/EN: `settings.title`, `settings.section.{language,scaling,theme}`, `settings.scaling.{100,125,150}`, `settings.theme.light`, `settings.theme.disabled.note`, `settings.button.save`, `settings.toast.locale.restart.{title,content}`.
  - **9 test nuovi**: `SettingsControllerTest` 6 (locale change persists+prompts restart, locale unchanged no prompt, scaling change persists, invalid scaling falls back to 100, onBack non salva, applyScalingToScene null-safe), `SettingsSpringContextTest` 2 (`@SpringBootTest` resolve prototype + isPrototypeScoped — lezione `feedback_spring_ui_tests`), `SettingsFxmlSmokeTest` 1 (`FXMLLoader.load` su JavaFX thread con `Platform.startup` + `Assumptions.assumeTrue` per skip headless, controller wired via `springContext::getBean`).
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **214 test totali** (+9 vs Task 3.16), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task 3.16** — Autosave + recovery prompt (Fase 3, sotto-fase IMPLEMENTA, branch `feature/3-ui-singleplayer`):
  - `client/persistence/AutosaveService.java` (`@Component`): wrap di `SaveService` vincolato a `AUTOSAVE_SLOT`, espone `writeAutosave/readAutosave/clearAutosave/autosaveExists`. `Clock` e `Supplier<RandomGenerator>` injectable per test (timestamp deterministici e RNG riproducibile).
  - `client/controller/SinglePlayerAutosaveTrigger.java` (`@Component`): impl di `AutosaveTrigger` che inoltra `onMoveApplied(SinglePlayerGame)` a `AutosaveService.writeAutosave`, swallows `UncheckedIOException` con log WARN per non bloccare il game loop. Spring popola automaticamente `Optional<AutosaveTrigger>` di `SinglePlayerController`: ogni mossa applicata produce ora un autosave atomico.
  - `MainMenuController` ridisegnato: nuova firma ctor `(SceneRouter, I18n, AutosaveService, GameSession, UserPromptService)`. `handleAutosavePrompt()` ritorna typed enum `PromptResult` (RESUMED/DISCARDED/SCHEMA_MISMATCH/IO_ERROR/NO_AUTOSAVE); ramo "yes" carica via `AutosaveService.readAutosave`, popola `GameSession`, naviga a `SceneId.BOARD`; ramo "no" cancella via `AutosaveService.clearAutosave`. `UnknownSchemaVersionException` (ADR-031) e `UncheckedIOException` mostrano toast localizzato e cancellano il file.
  - `SplashController` migrato a `AutosaveService.autosaveExists()` (rimosse `Path`/`Files`/`AUTOSAVE_FILENAME` hardcoded).
  - `BoardViewController.terminate()` migrato a `AutosaveService.clearAutosave()` (sostituisce `saveService.delete(AUTOSAVE_SLOT)` diretto).
  - **i18n** IT/EN: `autosave.toast.error.schema.title/content`, `autosave.toast.error.io.title/content`.
  - **22 test nuovi/modificati**: `AutosaveServiceTest` 8, `SinglePlayerAutosaveTriggerTest` 2, `AutosaveE2ETest` 3 (prompt on restart A3.5, clear on terminate, write failure tolerated), `SaveDialogSpringContextTest` +2 context-resolution per `AutosaveService` e `AutosaveTrigger` (lezione `feedback_spring_ui_tests`), `MainMenuControllerTest` riscritto su 8 metodi (3 click + 5 prompt branch), `SplashControllerTest` aggiornato, `BoardViewControllerTest` aggiornato.
  - `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, 205 test totali (+22 vs Task 3.15), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

---

## [0.2.0] — 2026-04-28

Tag git: `v0.2.0`. Chiusura della **Fase 2 — IA nel modulo `shared.ai`** della roadmap (`SPEC.md` §16). Tutte e 4 le sotto-fasi (PIANIFICA / IMPLEMENTA / REVIEW / TEST) chiuse. Gating SPEC §16 Fase 2 ✅ PASSED (Campione ≥ 95/100 vs Principiante in 16:02 min). Branch `feature/2-ai` mergiato `--no-ff` in `develop` e poi in `main`.

### Added

- `plans/PLAN-fase-2.md` (sotto-fase PIANIFICA Fase 2, approvata in blocco 2026-04-28).
- **Fase 2 — IA nel modulo `shared.ai`** (sotto-fase IMPLEMENTA conclusa). Tre livelli funzionanti, virtual-thread cancellabile, transposition table, ~125 nuovi test. Branch di lavoro `feature/2-ai`, sequenza Tasks 2.1 ÷ 2.13 + 2.15.
  - **Task 2.1** — `com.damaitaliana.shared.ai.evaluation`: `Evaluator` interface, `EvaluationTerm` interface, `MaterialTerm` (man=100, king=300 cp per SPEC §12.1), `WeightedSumEvaluator` con record `WeightedTerm` e factory `defaultEvaluator()` (15 test).
  - **Task 2.2** — `MobilityTerm` (×5), `AdvancementTerm` (×2), `EdgeSafetyTerm` (×8), `CenterControlTerm` (×10) — i 4 termini residui SPEC §12.1; `defaultEvaluator()` ora compone tutti e 5 (19 test).
  - **Task 2.3** — `com.damaitaliana.shared.ai.search.MoveOrderer` interface + `StandardMoveOrderer` (capture-first → longer-capture-first → center-destination-first → FID-from-ascending) (7 test).
  - **Task 2.4** — `MinimaxSearch` (negamax con alpha-beta), `SearchResult`, mate-distance scoring `±(MATE_SCORE − plyFromRoot)`. Introdotti anche `CancellationToken` (interface + `never()`), `MutableCancellationToken`, `SearchCancelledException` (deviazione minore vs piano §2.5: necessari per testare `MinimaxSearch.cancellationStopsSearch`) (18 test).
  - **Task 2.5** — `IterativeDeepeningSearch` con PV-first (`PvFirstOrderer` package-private) e graceful cancellation; `CancellationToken` esteso con `deadline(Instant)`/`deadline(Instant, Clock)`/`composite(...)` (27 test).
  - **Task 2.6** — `ZobristHasher` deterministico (seed `0xDA4A172L`), `TranspositionTable` 2^20 entry always-replace, integrazione TT in `MinimaxSearch.negamax` con bound semantics `EXACT`/`LOWER_BOUND`/`UPPER_BOUND` e niente early-return al root. `IterativeDeepeningSearch` con costruttore TT-aware (22 test).
  - **Task 2.7** — `sealed interface AiEngine permits PrincipianteAi, EspertoAi, CampioneAi`, `AiLevel` enum, factory `forLevel(level, rng)`. Esperto e Principiante deviano dal piano usando `IterativeDeepeningSearch` per cooperative cancellation; Campione usa IDS+TT come da piano. Costanti `DEPTH`/`MAX_DEPTH`/`DEFAULT_TIMEOUT`/`NOISE_PROBABILITY` esposte (19 test).
  - **Task 2.8** — `VirtualThreadAiExecutor` su `Executors.newVirtualThreadPerTaskExecutor()`, `Submission` con graceful + hard cancel; deadline + thread-interrupt + manual token composti via `CancellationToken.composite(...)` (9 test).
  - **Task 2.9** — `AiTacticalPositionsTest` con 5 posizioni golden hardcoded in Java (mate-in-1 bianco, mate-in-1 nero via dama, cattura forzata, due-mosse, terminale).
  - **Task 2.10** — `AiTournamentSimulationTest` con sanity quick (5 partite) + gating `@Tag("slow")` 100 partite Campione vs Principiante (≥ 95 vittorie, A2.2). Determinismo via Principiante seed = `42 + gameIndex`.
  - **Task 2.11** — `AiPerformanceTest` `@Tag("performance")` per ogni livello: budget × 1.5 tolerance (PLAN-fase-2 §7.7).
  - **Task 2.13** — `shared/pom.xml` JaCoCo gate aggiunge regola `PACKAGE com.damaitaliana.shared.ai*` ≥ 85 % line + branch (PLAN-fase-2 §7.8). Gate F1 mantenuto.
  - **Task 2.15** — ADR-024 (architettura `AiEngine` sealed + 3 livelli), ADR-025 (Evaluator modulare), ADR-026 (Zobrist + TT), ADR-027 (cancellation cooperativa), ADR-028 (rumore Principiante deterministico) in `ARCHITECTURE.md`. Matrice tracciabilità popolata con FR-SP-02, NFR-P-02 (perf gate), AC §17.1.1 parziale, e i 13 acceptance criteria operativi della Fase 2.

### Changed

- **F1 hardening (`ItalianRuleEngine.isThreefoldRepetition`)**: replay-from-initial ora cattura `IllegalMoveException` e ritorna `false` quando la history non è coerente con `GameState.initial()` (es. stati hand-built per test fixture o esplorazione AI). Conservativo: "no repetition" è la risposta sicura per il chiamante. Coerente con la limitazione documentata in ADR-021. Necessario perché il search di F2 esplora stati hand-built fino a depth 8 e ne calcola lo status ricorsivamente.
- **REVIEW-fase-2 closure**: 7 finding chiusi — 2 RESOLVED (F-004 Javadoc su `ZobristHasher.hashAfterMove` non implementato, F-005 Javadoc su `MoveOrderer.order(state)` parametro intenzionalmente unused), 5 ACKNOWLEDGED (F-001 + F-002 deferred-F4 con la futura ottimizzazione Zobrist-incremental di `isThreefoldRepetition`, F-003 deferred-F3 by design — AC §17.1.1 richiede UI E2E, F-006 + F-007 design intentional).
- **TEST-PLAN-fase-2 closure**: 391 test totali (387 default + 1 `slow` + 3 `performance`); coverage modulo 97.3% line / 95.5% branch, package `rules` 96.2% / 95.7%, package `ai` 97.7% / 96.2%, tutti sopra i gate (90% / 90% / 85%). Gating A2.2 PASSED in 16:02 min. Limiti tracciati per F4 (Zobrist-incremental).

---

## [0.1.0] — 2026-04-28

Tag git: `v0.1.0` (commit `2f6a14e`). Chiusura della **Fase 1 — Dominio e regole nel modulo `shared`** della roadmap (`SPEC.md` §16). Tutte e 4 le sotto-fasi (PIANIFICA / IMPLEMENTA / REVIEW / TEST) chiuse. Branch `feature/1-domain-and-rules` mergiato `--no-ff` in `develop` (`9fb533f`) e poi in `main` (`2f6a14e`).

- `plans/PLAN-fase-1.md` (sotto-fase PIANIFICA Fase 1, approvata 2026-04-28).
- **Fase 1 — Dominio e regole nel modulo `shared`** (sotto-fase IMPLEMENTA conclusa). 245 test su 4 sotto-package, JaCoCo ≥ 90% modulo + ≥ 90% package `rules`, SpotBugs 0 High.
  - **Task 1.1** — `com.damaitaliana.shared.notation.FidNotation`: bijezione `Square ↔ 1..32` in orientamento standard FID (ADR-020), parsing/format mosse, record `ParsedMove` (59 test).
  - **Task 1.2** — `com.damaitaliana.shared.domain`: `Square`, `Color`, `PieceKind`, `Piece`, `Board` (immutabile, `initial()` con 24 pezzi sulle case scure rank 0-2/5-7), `Move` sealed + `SimpleMove` + `CaptureSequence`, `GameStatus` esteso (6 voci con `isOngoing`/`isWin`/`isDraw`), `GameState` (58 test).
  - **Task 1.3** — `com.damaitaliana.shared.rules.RuleEngine` interface + `IllegalMoveException` + `ItalianRuleEngine` movimenti semplici (22 test).
  - **Task 1.4** — Catture singole con la regola "pedina non cattura dama" (SPEC §3.3) e regola della cattura obbligatoria (14 test).
  - **Task 1.5** — Sequenze multi-jump via DFS con anti-loop (set di pezzi catturati) e stop alla promozione mid-sequenza per le pedine (SPEC §3.5) (10 test).
  - **Task 1.6** — Le 4 leggi italiane di precedenza (SPEC §3.4): quantità → qualità → precedenza dama → prima dama, ognuna come filtro privato isolato (7 test).
  - **Task 1.7** — Promozione su `applyMove`: una pedina che termina su rank 7 (bianco) o rank 0 (nero) viene promossa a dama prima di essere posizionata (7 test).
  - **Task 1.8** — `computeStatus` completo (SPEC §3.6): vittoria per assenza pezzi, vittoria per stallo (sconfitta del bloccato in variante italiana), patta per regola 40 mosse (≥80 ply), patta per triplice ripetizione via replay della history da `GameState.initial()` con record privato `PositionKey` (ADR-021), tramite metodo `applyCore` non ricorsivo (11 test).
  - **Task 1.9** — Test corpus parametrizzato `test-positions.json` con **48 posizioni** distribuite per categoria (CLAUDE.md §2.4.4); schema disgiunto `whiteMen`/`whiteKings`/`blackMen`/`blackKings` (ADR-022); loader Jackson con validazione + `RuleEngineCorpusTest` parametrizzato + `RuleEngineCorpusSchemaTest` + `RuleEngineCorpusCoverageTest` (4 + 3 + 48 = 55 test totali).
  - **Task 1.10** — `EndToEndGameApiTest` con 3 partite scriptate via API pura (white-wins, black-wins per stallo, draw per regola 40 mosse) (3 test).
  - **Task 1.11** — `shared/pom.xml`: JaCoCo `haltOnFailure=true`, regole `BUNDLE` ≥ 90% line+branch e `PACKAGE com.damaitaliana.shared.rules` ≥ 90% line+branch. `SharedSmokeTest` rimosso.
  - **Task 1.12** — `package-info.java` per `shared`, `shared.domain`, `shared.notation`, `shared.rules`. ADR-020/021/022 in `ARCHITECTURE.md`. Matrice tracciabilità `tests/TRACEABILITY.md` popolata con FR-SP-04, FR-SP-05, FR-SP-09, FR-COM-01, FR-RUL-01, NFR-M-01, NFR-M-04, AC §17.1.6/7/8/§17.2.4/5 e gli 11 acceptance criteria operativi della Fase 1.

### Changed

- **Modello branch git**: adozione di **GitFlow leggero** (`CLAUDE.md` §4.3-§4.4). `main` = production / tag, `develop` = integrazione e default branch su GitHub, branch effimeri `feature/<fase>-<topic>` e `fix/review-N-F-<id>` staccati da `develop` e mergiati `--no-ff`. Tag delle fasi (`v0.<fase>.0`) sul commit di merge in `main` (eccezione: `v0.0.0` taggato direttamente su `main` prima dell'introduzione del modello GitFlow).
- README sezione "Convenzioni": riflette il nuovo modello branch.
- **SPEC §8.1 — `GameStatus` esteso a 6 voci** (CR-001 della REVIEW-fase-1, opzione A). L'enum diventa `{ ONGOING, WHITE_WINS, BLACK_WINS, DRAW_REPETITION, DRAW_FORTY_MOVES, DRAW_AGREEMENT }` con helper `isOngoing/isWin/isDraw`. Motivazione: la UI e il replay viewer (FR-RUL, FR-NET-09) devono distinguere il motivo della patta. ADR-023 documenta il rationale. Il codice `shared` era già allineato (Task 1.2).
- **REVIEW-fase-1 closure**: 7 finding chiusi — 4 RESOLVED (F-002 IllegalMoveException Javadoc, F-003 ItalianRuleEngine class Javadoc, F-004 legalMoves duplicate iteration, F-006 GameStatus extension ADR), 3 ACKNOWLEDGED (F-001 thin coverage del repetition test → F2, F-005 isThreefoldRepetition O(n²) → F2 con Zobrist, F-007 `// TODO Fase 3` in `client/pom.xml` fuori scope F1).
- **TEST-PLAN-fase-1 closure**: 245 test verdi, coverage modulo 96.7% line / 94.7% branch e package `rules` 95.7% line / 94.9% branch (entrambi sopra il gate 90%). 48 posizioni del corpus distribuite per le 14 categorie minime (CLAUDE.md §2.4.4). Limiti noti tracciati (REVIEW F-001 e F-005 deferred-F2).

### Removed

### Fixed

---

## [0.0.0] — 2026-04-28

Tag git: `v0.0.0` (commit `e68335f`). Chiusura della **Fase 0 — Setup infrastruttura** della roadmap (`SPEC.md` §16).

### Added

- File di workflow SDD: `AI_CONTEXT.md`, `ARCHITECTURE.md`, `CHANGELOG.md`, `README.md` (Fase 0, Task 0.1).
- Skeleton matrice di tracciabilità `tests/TRACEABILITY.md` (vuota, popolata dalla Fase 1).
- Struttura directory di workflow `plans/`, `reviews/`, `tests/` con `.gitkeep`.
- `.gitignore` per Java/Maven/IDE/OS, `.editorconfig` con preset 2-spazi Java, `.gitattributes` con normalizzazione LF.
- Parent POM `pom.xml` con BOM Spring Boot 3.4.5 + Testcontainers + Ikonli (Fase 0, Task 0.2).
- `dependencyManagement` per JavaFX 21.0.5, JmDNS 3.5.12, springdoc 2.6.0, TestFX 4.0.18, logstash-logback-encoder 7.4, JJWT 0.12.6.
- `pluginManagement` con maven-compiler 3.13 (release=21), Spotless 2.43 con google-java-format 1.22 (style GOOGLE, 2 spazi), JaCoCo 0.8.12 (`haltOnFailure=false` in Fase 0), SpotBugs 4.8 (threshold High), Enforcer 3.5 (Maven ≥3.9, Java 21, dependencyConvergence).
- Plugin Enforcer, Spotless e SpotBugs attivi a livello parent (ereditati da tutti i moduli).
- Modulo `shared` (Task 0.3): solo Jackson + JUnit 5 + AssertJ. Vincolo "no Spring/JavaFX/JPA/WebSocket" documentato nel POM.
- Modulo `core-server` (Task 0.4): `shared` + `spring-context` (DI) + `spring-messaging` (DTO STOMP) + Mockito. Vincolo "no Tomcat/Jetty/JPA" documentato nel POM.
- Modulo `client` (Task 0.5): `shared` + `core-server` + Spring Boot starter (no -web) + Spring Boot WebSocket starter con **Tomcat escluso e Jetty incluso** + JavaFX 21 (controls/fxml/graphics/media) + Ikonli (Material 2 + FontAwesome 5) + JmDNS + TestFX.
- Modulo `server` (Task 0.6): Spring Boot completo (web/websocket/security/data-jpa/validation/actuator) + MySQL connector + Flyway + Caffeine + JJWT + springdoc-openapi + micrometer-registry-prometheus + logstash-logback-encoder + Testcontainers MySQL + H2 (test).
- `application.yml` minimal del server con datasource via env var (default `localhost:3306`).
- Smoke test per ogni modulo (`<Modulo>SmokeTest`), da rimuovere quando ogni modulo avrà test reali.
- `package-info.java` per ogni modulo (Task 0.9): garantisce `target/classes` per JaCoCo report; documenta i sotto-package previsti dalle fasi successive e i vincoli architetturali (CLAUDE.md §8).
- ADR-018 (MySQL locale come ambiente dev, Docker Compose rimosso).
- ADR-019 (CI GitHub Actions disattivata per scelta in questa fase).
- `plans/PLAN-fase-0.md` (sotto-fase PIANIFICA).
- `reviews/REVIEW-fase-0.md` (sotto-fase REVIEW): 13 finding totali, 6 RESOLVED, 7 ACKNOWLEDGED, nessuno bloccante.
- `tests/TEST-PLAN-fase-0.md` (sotto-fase TEST): documenta la natura infrastrutturale della fase e la validazione della Definition of Done.

### Changed

- REVIEW closure: 6 fix applicati ai findings:
  - F-001 (CODE_QUALITY Medium): aggiunta regola `<dependencyConvergence/>` al maven-enforcer-plugin.
  - F-002 (Low): rimossa configurazione vestigiale Lombok da `spring-boot-maven-plugin` in `server/pom.xml`.
  - F-003 (Low): rimosso default vuoto da `${DB_PASSWORD:}` in `application.yml`.
  - F-004 (Low): SpotBugs attivato a livello parent `<build><plugins>` (gating globale).
  - F-007 (Low): commento `TODO Fase 3` aggiunto a `javafx-maven-plugin` in `client/pom.xml`.
  - F-008 (Low): rimossa voce ridondante `jacoco.exec` da `.gitignore`.

### Removed

- `docker-compose.yml` e `.env.example` durante revisione post-feedback utente (ADR-018).

### Fixed

- Parent POM: import non valido `io.jsonwebtoken:jjwt-bom` (artifact non pubblicato) sostituito con dichiarazione esplicita di `jjwt-api`, `jjwt-impl`, `jjwt-jackson` in `dependencyManagement`.
