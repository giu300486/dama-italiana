# AI Context

> File di stato corrente del progetto.
> Da aggiornare ad ogni task completato (CLAUDE.md §2.2).

## Stato corrente

- **Branch corrente**: `develop` (HEAD `c3983a0`); **mini-fase F4.5 in apertura** — branch `feature/4.5-ui-responsiveness` da staccare post-commit PLAN. Fase 4 chiusa il 2026-05-06 con tag `v0.4.0` su `main` (commit di merge `face88c`, mergiata in develop via `c3983a0`). Modello GitFlow leggero (CLAUDE.md §4.3): merge `--no-ff` su `develop` a chiusura mini-fase, poi `develop → main` con tag `v0.4.5` sul commit di merge.
- **Fase roadmap**: **Mini-fase F4.5 — UI Responsiveness desktop** (fix mirato post-demo cliente: parte bassa scacchiera tagliata su risoluzione cliente, finestra non si adatta al resize). Modello F3.5 (mini-fase intermedia, pull-forward parziale di feature di adattività UI da F11). Out of scope: re-skin design system F3.5 (anti-pattern #15 vincolante), mobile/tablet/touch, multi-monitor span, dark mode, i18n a11y, replay/profilo/server.
- **Hardware target cliente F4.5** (input utente 2026-05-06): PC ~10 anni (era 2014-2015), upgrade Win 8.1 → Win 10. Inference senior: CPU Intel Sandy/Ivy Bridge i3/i5, GPU **Intel HD 3000-4000** (sub-UHD-620 baseline NFR-P-01), RAM 4-8 GB, **HDD spinning disk** (probabile causa "Win 10 lento"), display **1366×768** o 1920×1080 a **100% DPI** (pre-HiDPI). Defect "parte bassa scacchiera tagliata" → quasi certamente 1366×768 (768 verticali insufficienti per frame + side panel + board nell'attuale design F3.5 ~1080p-centric). **Valida pienamente Task 4.5.4 come core del fix** + floor 1024×720 del PLAN appropriato. Note F4.5: (a) DPI matrix 125/150/200% utile per **altri** clienti futuri, irrilevante per questo cliente specifico; (b) asset audit Task 4.5.3b — confermo soglia ≥2048×2048 ma **NON oltre 4096×4096** (texture grandi caricano lente su HDD vecchio + 4 GB RAM); (c) Stage state persistence Task 4.5.7b cruciale per questo cliente (deve memorizzare la finestra ottimale). **Performance F3.5 animazioni su HD 3000/4000**: hardware acceleration JavaFX funziona, particle 8-12 + DropShadow + texture statiche sono leggere — probabilmente OK, da verificare a manual demo run A4.5.16; se jank, **futura F4.6 / F11 aggiungerà settings toggle "Performance mode"** (out of scope F4.5 — F4.5 è layout-only).
- **Ultimo task completato**: **F4.5 Task 4.5.10 — Documentation + ADR finalization + TRACEABILITY + IMPLEMENTA closure** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #73 chiuso il 2026-05-06). **CHIUSURA SOTTO-FASE IMPLEMENTA F4.5** (Task 4.5.1 → 4.5.10 completati, 4.5.6b skipped per Opzione B utente). Output: **1 file prod nuovo** (`client/src/main/java/com/damaitaliana/client/layout/package-info.java`, documenta i 2 helper del package: `JavaFxScalingHelper` Task 4.5.7 + `BoardFrameThicknessHelper` Task 4.5.8, cita anti-pattern #15 + idempotency contract) + **3 file docs aggiornati**: (a) `ARCHITECTURE.md` ADR-043 esteso con sezione "Stato post-implementazione F4.5 (Task 4.5.10)" che documenta delta tra plan e realtà — 2 helper effettivamente introdotti vs `BoardSizingHelper` ipotizzato non realizzato (board centering risolto direttamente in `BoardRenderer.layoutChildren`), side panel auto-hide < 1024 non implementato (Stage min enforce 1024 unreachable), `BackgroundImage` REPEAT mode su 4 selettori, DPI manuale, coverage delta (70 → 74 classi, +58 fast test, +56 slow); (b) `tests/TRACEABILITY.md` con **3 estensioni**: NFR-U-05 nuova riga (UI client adattiva 1280×720 → 4K, DPI 100/125/150/200%, Stage v3), AC §17.2.8 nuova riga (UI responsive desktop SPEC v2.3 CR-F4.5-001), **nuova sezione "Acceptance criteria di Fase 4.5"** con tutte le 19 righe A4.5.1..A4.5.19 con status (✅ COVERED 13, ⚠️ PARTIAL 2, ⚠️ DEFERRED to TEST sotto-fase 4: A4.5.10 ortogonalità scaling 150%×1366×768, A4.5.11 DPI varianti, A4.5.12 resize fluido jank, A4.5.16 manual demo, A4.5.18 mvn clean verify root) e riferimenti a test class / file evidence; (c) `CHANGELOG.md` con voce sintetica F4.5 closure. **A4.5.x coverage summary**: 13 ✅ COVERED via test/code evidence (A4.5.1/2/3/4/5/6/7/8/9/13/14/14b/14d/15/17/19), 2 ⚠️ PARTIAL (A4.5.10 ortogonalità implicita via pipeline + manual; A4.5.14c ultrawide caps applicati Task 4.5.6 + manual review screenshot post-fix conferma centratura), 4 ⚠️ DEFERRED to manual/TEST sotto-fase (A4.5.11 DPI varianti, A4.5.12 resize jank, A4.5.16 manual demo run cliente/VM, A4.5.18 `mvn clean verify` root regression). **Sotto-fase IMPLEMENTA F4.5 chiusa**: 10 task chiusi su develop+feature branch (4.5.1, 4.5.2, 4.5.3, 4.5.3b, 4.5.4, 4.5.5, 4.5.6, 4.5.7, 4.5.7b, 4.5.8, 4.5.9, 4.5.10 — totale 12 commit feature/4.5-ui-responsiveness incluse 2 docs + 10 code/test). **Build**: nessun nuovo test da eseguire (Task 4.5.10 docs-only); ultimo `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS 379/379 (Task 4.5.9 closure), JaCoCo gate met, SpotBugs 0, Spotless OK. **Prossima azione**: avvio **sotto-fase REVIEW F4.5** (CLAUDE.md §2.3) — review code-by-code dei file modificati nelle 12 task, mappatura A4.5.x → codice, identificazione findings categorizzati (BLOCKER/REQUIREMENT_GAP/BUG/SECURITY/PERFORMANCE/CODE_QUALITY/DOC_GAP), output `reviews/REVIEW-fase-4.5.md`. In attesa di conferma utente prima di avviare REVIEW (memoria `feedback_ask_before_next_task.md`).

- **Task precedente**: **F4.5 Task 4.5.9 — Test parametrizzato + screenshot baseline post-fix** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #72 chiuso il 2026-05-06). Output: **1 file di test nuovo** (`ResponsivenessParametricTest.java`, `@SpringBootTest` + `@Tag("slow")` + `@ParameterizedTest @MethodSource("scenarios")` cartesian product **7 risoluzioni × 8 schermate = 56 esecuzioni**) + **2 PNG defect samples post-fix committati** (`board-view_1366x768-laptop.png` + `board-view_3440x1440-ultrawide.png` — coppia identica al pre-fix di Task 4.5.2 per A/B comparison diretta) + **`.gitignore` whitelist negata** in `tests/visual-review/responsiveness-baseline-post-fix/` che esclude i restanti 54 PNG (~190 MB rigenerabili in 32s via `mvn -pl client test -Dgroups=slow -Dtest=ResponsivenessParametricTest`). **Risoluzioni canoniche** (identiche al pre-fix): 1024×720-floor, 1280×720-small, 1366×768-laptop, 1920×1080-fhd, 2560×1440-qhd, 3440×1440-ultrawide, 3840×2160-4k (tutte 100% logical DPI — varianti DPI 125/150/200% verificate manualmente in Task 4.5.10 TEST-PLAN-fase-4.5 §5.2). **Schermate** (identiche): splash, main-menu, sp-setup, board-view, load-screen, settings, rules, save-dialog. **Strategia controller**: identica a `BaselineScreenshotCapture` — `loader.setControllerFactory(springContext::getBean)` per le 7 con `fx:controller=...`; `loader.setController(springContext.getBean(SaveDialogController.class))` per save-dialog. **Helper `ensureFakeGameSession()`** pre-popola GameSession per board-view/save-dialog. **Pipeline rendering**: dopo `themeService.applyTheme(scene)` ora chiama anche `uiScalingService.applyTo(scene)` + `JavaFxScalingHelper.applyFluidFontsToScene(scene)` (nuovo path Task 4.5.7 — fluid typography binding) per riprodurre fedelmente quello che fa `SceneRouter.show` in produzione. **Assertion bundle** (3 invarianti per schermata `assertable`, eseguite dopo `applyCss + layout`): (a) **No clipping** — `root.layoutBounds.maxX/Y ≤ scene.width/height + 1.0px` (tolerance per arrotondamenti float), per ogni schermata; (b) **No unnecessary scrollbar** — walk del tree per `ScrollBar` nodes (non `ScrollPane`), filtra `isVisible() && isManaged()`, asserisce list empty (la baseline GameSession è vuota = no saved games, no overflow content, no expanded list — quindi una bar visibile significherebbe che il layout non riesce a far stare neanche il contenuto della baseline empty); (c) **Board square** (solo `board-view`) — `findFirst(root, BoardRenderer.class)` recursive search, `currentCellSize() > 0` (renderer ha ricevuto allocation), `currentCellSize() × 8 ≤ renderer.width + 1` AND `≤ renderer.height + 1` (gate contro regression future che renderebbe cells non-square). **save-dialog non assertable**: rendering fuori dal flow showAndWait reale = PNG vuoto ~10KB (limite documentato in Task 4.5.2 audit), capture mantenuto per parità file list pre/post-fix. **`@Tag("slow")`**: escluso dal fast loop `mvn -pl client verify -DexcludedGroups=slow,performance` (fast=379 invariato), incluso nel closure regression `mvn clean verify`. **DPI scaling note** identica a BaselineScreenshotCapture: layout math DPI-independent → single-JVM run valida correctness anche senza testare le varianti scaling. **Output dir**: `tests/visual-review/responsiveness-baseline-post-fix/` con `.gitignore` matching pre-fix pattern (whitelist negata `*.png` + `!board-view_1366x768-laptop.png` + `!board-view_3440x1440-ultrawide.png`). **A/B visual review confirmation**: `board-view_1366x768-laptop.png` post-fix mostra **board completamente visibile** (defect cliente "parte in basso non si vede" risolto — board cells fill la full available area, frame proporzionato ~25px); `board-view_3440x1440-ultrawide.png` post-fix mostra **board centrata + scalata correttamente** (Task 4.5.4) con texture wood **tiled naturalmente** (Task 4.5.8) intorno — vs pre-fix dove board era piccolo top-left e texture stretched. **Build**: `mvn -pl client test -Dgroups=slow -Dtest=ResponsivenessParametricTest` BUILD SUCCESS in 32.39s, **56/56 test verdi** (3 assertions × 7 schermate assertable × 7 risoluzioni + 7 capture-only save-dialog = 49 + 7 = 56). Fast verify regression `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **379/379 test verdi invariati** (slow tag esclude correttamente i 56 nuovi).
- **Task precedente**: **F4.5 Task 4.5.8 — BackgroundImage REPEAT mode + frame scaling** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #71 chiuso il 2026-05-06). Output: **5 file prod modificati** + **1 file prod nuovo** + **1 file di test nuovo**. **CSS — switch da `cover` a `repeat` mode**: 4 selettori che dressavano grandi aree con texture wood (`theme-light.css` `.splash-root` su `frame.jpg`, `.main-menu-root` su `board_dark.jpg`, `.screen-root` su `board_dark.jpg`; `components.css` `.board-frame` su `frame.jpg`) sono passati da `-fx-background-size: cover` (single texture stretched per coprire la viewport, distorsione visiva a ultrawide/4K — confermato da audit screenshots Task 4.5.2) a `-fx-background-repeat: repeat` con native size 2048×2048 — al floor 1024×720 una singola tile copre già il viewport, a ultrawide 3440 / 4K il pattern tila in modo seamless (texture Poly Haven CC0 sono progettate seamlessly tileable, ADR-037). Le 2 cell selectors (`.board-cell-light/dark` in `components.css`) **non modificate**: ogni cella è ~63-240px e usa correttamente `100% 100% no-repeat` (texture scalata per riempire la cella). **Frame scaling proporzionale**: `BoardFrameThicknessHelper` (nuovo `final class` in `client.layout`, secondo helper del package introdotto in Task 4.5.7) — `computeFrameThickness(boardSide, minPx, maxPx, scaleFactor)` pure math `min(maxPx, max(minPx, boardSide × scaleFactor))` testabile headless; `bindFrameThickness(StackPane frame, BoardRenderer renderer)` programmatic binding via `Bindings.createObjectBinding(supplier, renderer.widthProperty, renderer.heightProperty)` (depende da entrambe le proprietà — boardSide = `min(rendererWidth, rendererHeight)`, perché cells sono sempre quadrate e inscritte nel rect del renderer da Task 4.5.4 layoutChildren centering); idempotente (unbind first se già bound, no "A bound value cannot be set" exception); pattern strutturalmente parallelo a `JavaFxScalingHelper` (stessa shape, stessa filosofia di pure-math separato dal binding). **Constants**: `THICKNESS_MIN_PX=16` (frame visibile a 1024×720 floor), `THICKNESS_MAX_PX=48` (cap prima che la cornice domini il playing area a 4K), `THICKNESS_SCALE=0.035` (3.5% di board side, fluid nel middle range board ~460-1370px → thickness 16-48). **Mapping concreto**: floor 1024×720 → renderer area ~720×560 → boardSide=560 → 560×0.035 = 19.6px frame; FHD 1920×1080 → board ~880px → 30.8px frame; 1920×1080 board side esatto a 1372 → cap 48px; 4K 3840×2160 → board ~1800px → cap 48px. Sostituisce il padding fisso 24px definito in `board-view.fxml` (era visivamente sproporzionato a 1024×720 — troppo grosso — e a 4K — troppo piccolo). **`board-view.fxml`**: aggiunto `fx:id="boardFrame"` allo StackPane `.board-frame` per binding wiring; padding 24px conservato come fallback statico per FXML preview tools / headless tests (commento esplicativo); FXML imports invariati (StackPane già importato). **`BoardViewController.initialize`**: dopo `gameTitleLabel.setText / backButton.setText`, chiamata `BoardFrameThicknessHelper.bindFrameThickness(boardFrame, boardRenderer)`; aggiunto `@FXML private StackPane boardFrame` field + import `javafx.scene.layout.StackPane` + import `BoardFrameThicknessHelper`. Binding installato una sola volta per controller lifecycle (controller è `@Scope("prototype")` quindi si crea ad ogni navigazione a BOARD); idempotente comunque — se per qualunque motivo viene re-invocato, unbind first. **`BoardFrameThicknessHelperTest`** (nuovo, **10 test = 7 parametric math + 3 FX-binding**): math (no FX) — 7 boardSide → expected: 400→16.0 (clamp floor), 460→16.1 (just past floor), 560→19.6 (1024×720 floor renderer), 880→30.8 (1920×1080), 1370→47.95 (just under cap fluid), 1372→48.0 (exact cap boundary), 1800→48.0 (4K cap); FX-binding (toolkit-required, self-skip Assumptions) — `bindFrameThicknessAppliesProportionalPaddingAndReactsToResize` (instantiate BoardRenderer + StackPane, bind, `renderer.resize(1024, 720)` → padding 25.2 (boardSide=720, all 4 sides), poi `renderer.resize(1800, 1800)` → 48.0, poi `renderer.resize(400, 400)` → 16.0 floor — verifica reattività live al resize), `bindFrameThicknessIsIdempotentAndUnbindsBeforeRebinding` (chiamata 2 volte non lancia exception, padding finale 30.8 a renderer 880×880), `bindFrameThicknessUsesMinOfWidthAndHeightSoUltrawideRendererStillSizesByBoardSide` (renderer 2400×1200 → boardSide = min = 1200 → padding 42.0). **`@BeforeAll Platform.startup` + `Assumptions.assumeTrue(fxToolkitReady)` self-skip pattern** identico a `JavaFxScalingHelperTest`/`BoardRendererLayoutTest` (ADR-018). **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **379/379 test verdi** (+10 vs 369 = 7 math + 3 FX), JaCoCo gate met (74 classi vs 73 — `BoardFrameThicknessHelper` aggiunto al bundle), SpotBugs 0, Spotless OK. Zero regressioni sui 369 test pre-esistenti — i test esistenti BoardViewController / FxmlLoadingSmokeTest passano con il nuovo `fx:id="boardFrame"` aggiunto e con il padding ora bound nel controller post-game-load. **Effetto utente atteso post-fix**: a 1024×720 frame thickness ~20px (era 24, leggermente più sottile ma più proporzionato al board ~560px); a 1920×1080 ~31px (era 24, frame più visibile); a ultrawide 3440×1440 con board side 1200 → 42px; a 4K con board side 1800 → 48px cap (era ancora 24, frame ora visibilmente proporzionato al board grande). Texture wood al posto di stretched a tutti i viewport: a 1024×720 e FHD invisible difference (single tile copre comunque), a ultrawide/4K il pattern wood mantiene la sua scala "naturale" (la grana del legno non si allunga in modo deformante).
- **Task precedente**: **F4.5 Task 4.5.7b — Stage state persistence schema v3** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #70 chiuso il 2026-05-06). Output: **5 file prod** (1 modificato `UserPreferences.java` schema v2→v3 + 5 campi nuovi + factory `fromJson` esteso + 9 helper `with*` updated; 1 modificato `PreferencesService.java` migration `v1 || v2 → v3` con `withSchemaVersion` transparent — geometry resta null per file pre-v3, audio defaults preservati per v1; 1 nuovo `StagePersistenceValidator.java` in `client.app`, pure-math `isStateValid(prefs, screenVisualBounds, minWidth, minHeight)` static utility + production wrapper `isStateValidUsingPrimaryScreens(prefs)` che pulla `Screen.getScreens().getVisualBounds()` + `PrimaryStageInitializer.MIN_*`; 1 nuovo `StagePersistenceCoordinator.java` `@Component` in `client.app`, orchestrator del lifecycle: `initialize(Stage)` carica prefs → se `isStateValidUsingPrimaryScreens` true restore `setX/setY/setWidth/setHeight/setMaximized + setMinWidth/setMinHeight`, altrimenti delega a `PrimaryStageInitializer.initialize(stage)` per fallback 80% computed; `setOnCloseRequest` → `persist()` ricarica latest prefs (per non clobberare audio/locale changes), `current.withWindowState(round(w/h/x/y), isMaximized())`, `preferencesService.save(updated)`, `try/catch RuntimeException + log.warn` per non bloccare la chiusura della finestra; 1 modificato `JavaFxApp.start` ora chiama `context.getBean(StagePersistenceCoordinator.class).initialize(primaryStage)` invece del `PrimaryStageInitializer` diretto — il Coordinator delega comunque al PrimaryStageInitializer in fallback, parità di behaviour quando no persisted state). **`UserPreferences` schema v3**: 5 nuovi campi — `Integer windowWidth, windowHeight, windowX, windowY` (boxed nullable per detection di "no persisted state yet") + `boolean windowMaximized` (default false); `CURRENT_SCHEMA_VERSION` bumped 2→3; `defaults()` setta i 4 geometry a `null` e `windowMaximized=false`; nuovo helper `withWindowState(int w, int h, int x, int y, boolean maximized)` per setting atomico (geometry catturata atomicamente on close); compact constructor lascia i 4 nullable as-is (`null` = "no persisted state"); Jackson `@JsonCreator fromJson` esteso con 5 nuovi `@JsonProperty` boxed (`Integer windowWidth/Height/X/Y`, `Boolean windowMaximized`) per detection chiavi missing dei file pre-v3 → null/false defaults. Migration v1→v3: file v1 (no audio, no window) → audio defaults 30/70/false/false + window null/false. Migration v2→v3: file v2 (audio sì, window no) → audio preservato + window null/false. **`StagePersistenceValidator` failure cases**: (a) any of 4 geometry nullable null (fresh install / file just migrated v2→v3); (b) width/height < responsive floor 1024×720 (corrupted file / schema downgrade); (c) top-left corner sits outside every screen `visualBounds` (user unplugged secondary monitor where window was last shown). Edge case maximized=true con valid geometry: state ammesso (validator non controlla maximized — il flag è ortogonale, restore via `setMaximized(true)` sul Stage post-set di width/height). Validator pure-math testabile headless con synthetic `Rectangle2D` bounds (no FX toolkit needed); production wrapper requires JavaFX thread (chiama `Screen.getScreens()`). **3 file di test**: 1 nuovo `StagePersistenceValidatorTest` (9 test, no FX toolkit — uses synthetic bounds): `validStateOnPrimaryScreen` (1366×768 @ 100,50 in 1920×1080), `validStateOnSecondaryScreen` (1366×768 @ 2200,50 in dual 1920+1920), `invalidWhenAnyGeometryFieldIsNull` (5-arg null UserPreferences), `invalidWhenWidthBelowFloor` (800px < 1024 floor), `invalidWhenHeightBelowFloor` (600px < 720 floor), `invalidWhenTopLeftCornerOffscreenSecondaryUnplugged` (window remembers x=2200, only primary connected → invalid), `invalidWhenNullPrefs`, `boundaryAtFloorIsValid` (1024×720 @ 0,0 — exact floor accepted), `maximizedFlagDoesNotAffectValidity`; 1 modificato `UiScalingServiceTest` (UserPreferences constructor updated con 5 args nuovi `null, null, null, null, false`); 1 modificato `PreferencesServiceTest` (constructor in `saveThenLoadRoundTrips` esteso con `1600, 900, 120, 80, false` + assertions su 5 campi loaded; nuovo test `migratesSchemaV2FileLeavingWindowFieldsNull` verifica file v2 valido → v3 con window fields null; test esistente `migratesSchemaV1FileFillingAudioDefaults` esteso con `loaded.windowWidth/Height/Maximized` checks). **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **369/369 test verdi** (+10 vs 359 = 9 nuovi `StagePersistenceValidatorTest` + 1 nuovo `migratesSchemaV2FileLeavingWindowFieldsNull` di `PreferencesServiceTest`), JaCoCo gate met (73 classi vs 71 — +`StagePersistenceValidator` +`StagePersistenceCoordinator`), SpotBugs 0, Spotless OK. Zero regressioni sui 359 test pre-esistenti. **Effetto utente atteso post-fix**: il cliente apre l'app → prima volta usa l'80% computed centrato di Task 4.5.3 (no persisted state); resize/move/maximize → close → state salvato; riapertura → window restored at last known position/size/maximized state. Edge cases: secondary monitor unplugged → fallback a 80% computed (non si apre offscreen); width/height below floor (corrupted file) → fallback a 80% computed; persistence I/O fail su close → log.warn, finestra si chiude comunque (non blocca l'utente).
- **Task precedente**: **F4.5 Task 4.5.7 — Tipografia fluida programmatic** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #69 chiuso il 2026-05-06). **Decisione senior** (Task 4.5.6b skip): l'utente ha approvato Opzione B — saltare 4.5.6b (aspect ratio handling ultrawide manual review) e procedere direttamente a 4.5.7; ultrawide verificato in Task 4.5.9 post-fix screenshot regen, eventuale ritorno a 4.5.6b reattivo se emerge problema. Output Task 4.5.7: **1 file prod nuovo** (`client/src/main/java/com/damaitaliana/client/layout/JavaFxScalingHelper.java`, prima classe del nuovo package `client.layout` ADR-043) + **1 file prod modificato** (`SceneRouter.java`: import + chiamata `JavaFxScalingHelper.applyFluidFontsToScene(scene)` post `uiScalingService.applyTo(scene)`) + **1 file CSS modificato** (`theme-light.css`: aggiunte classi `.display-fluid` (28px baseline) e `.display-fluid-lg` (38px baseline) come marker, con commento explicit del cascade — runtime binding via inline `style` overrides class declaration in JavaFX) + **5 file FXML modificati** (titleLabel di splash + 4 form screens con marker class aggiunto: `splash.fxml` `display-fluid-lg`; `sp-setup.fxml`, `settings.fxml`, `load-screen.fxml`, `rules.fxml` `display-fluid`) + **1 file di test nuovo** (`JavaFxScalingHelperTest.java`, **15 test = 12 parametric math + 3 FX-binding**). **`JavaFxScalingHelper`**: package `client.layout`, helper finale stateless. (a) `computeFluidFontSize(sceneWidth, minPx, maxPx, scaleFactor)` pure math `min(maxPx, max(minPx, sceneWidth * scaleFactor))` testabile headless. (b) `applyFluidFontsToScene(Scene)` walk del tree del root, idempotente (unbind prima di bind), `instanceof Labeled` + check di `getStyleClass().contains(STYLE_CLASS_FLUID_LG)` (priorità lg) o `STYLE_CLASS_FLUID`. (c) Constants: `FLUID_MIN_PX=24, FLUID_MAX_PX=32, FLUID_SCALE=0.018` (display-fluid medium); `FLUID_LG_MIN_PX=28, FLUID_LG_MAX_PX=48, FLUID_LG_SCALE=0.025` (display-fluid-lg hero — PLAN-spec). (d) Internal `bindFluidFontSize(Labeled, ObservableDoubleValue, ...)` package-private per testabilità con `SimpleDoubleProperty` synthetic in test. **Format output**: `String.format(Locale.ROOT, "-fx-font-size: %.2fpx;", fontSize)` — 2 decimali deterministico, sidesteps il floating point precision issue (24.587999... → 24.59) e CSS-compatible. **Inline style binding via `label.styleProperty().bind(StringBinding)`**: applica via inline che ha priorità più alta della class selector nel cascade JavaFX, quindi il CSS class baseline (28/38px) è override-able dal binding. `Bindings.createStringBinding(supplier, dependencies)` registra `sceneWidthProperty` come dependency → re-evaluate automatica su scene resize. **Wiring `SceneRouter.show`**: dopo `themeService.applyTheme + uiScalingService.applyTo`, chiama `applyFluidFontsToScene(scene)`. Per ogni navigation re-walks il tree del root e re-binds (unbind first, idempotente). **CSS class aggiunte `theme-light.css` (additive)**: `.display-fluid { -fx-font-size: 28px; }` e `.display-fluid-lg { -fx-font-size: 38px; }` — baseline tra min e max che fa fallback decente quando helper non bound (FXML preview tools, headless test senza scene). Commento esplicativo del cascade winner. **6 FXML modificati** ai titleLabel display, additive `display-fluid` o `display-fluid-lg` agli styleClass esistenti `label-display, label-display-md/lg`: splash → `display-fluid-lg`, sp-setup/settings/load-screen/rules → `display-fluid`. save-dialog non modificato (modal stage fixed-width — fluid bind a sceneWidth statica non porta valore); main-menu-card-title non modificato (CSS 22px fixed, non era display class — out of scope PLAN); board-view gameTitleLabel non modificato (`label-title` non `label-display`, è subtitle non hero). **`JavaFxScalingHelperTest`**: 12 parametric math (6 punti per `display-fluid-lg` clamp 1024→28, 1366→34.15, 1920→48 boundary, 2560/3840→48 cap; 6 punti per `display-fluid` clamp 1024→24, 1334/1366 fluid, 1777→31.986 just under cap, 1920/3840→32 cap) + 3 FX-binding (toolkit-required, self-skip Assumptions): `applyFluidFontsBindsLabelsCarryingMarkerClasses` (verifica isBound + content "-fx-font-size: 34.15px" / "24.59px" a sceneWidth=1366, plain label senza marker class resta unbound), `boundFontSizeUpdatesWhenSceneWidthChanges` (re-bind con synthetic `SimpleDoubleProperty(1920)` → "48.00px" cap), `applyFluidFontsIsIdempotentAndUnbindsBeforeRebinding` (chiamata 2 volte non lancia "A bound value cannot be set", style finale rimane "28.00px" floor a 1024). **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **359/359 test verdi** (+15 vs 344 = 15 nuovi: 12 math + 3 FX-binding), JaCoCo gate met (71 classi vs 70 — +1 `JavaFxScalingHelper`), SpotBugs 0, Spotless OK. Zero regressioni sui 344 test pre-esistenti. **Effetto visuale atteso post-fix**: a 1024×720 splash title 28px (era 48px fixed), sp-setup/settings/load-screen/rules titles 24px (era 32px fixed) — più legibile a viewport stretti; a 1366×768 splash 34.15px / form 24.59px (fluid); a 1920×1080+ splash 48px (cap) / form 32px (cap, parità con fixed); a ultrawide stesso cap, no overgrowth incoerente. Il binding tracking a `scene.widthProperty()` reagisce live al window resize.
- **Task precedente**: **F4.5 Task 4.5.6 — Main menu + 5 form screens responsive** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #68 chiuso il 2026-05-06). Output: **5 file FXML modificati** + **1 file di test nuovo** (`ScreenLayoutTest.java`, 5 test). **Cambi FXML**: (1) `main-menu.fxml` — root cambiato da `GridPane` a `StackPane` (porta classe `main-menu-root` con texture wood); inner `GridPane fx:id="menuGrid" maxWidth="960" maxHeight="-Infinity"` (USE_PREF_SIZE) — cards 5 ora capate a 960px totali (~440px ciascuna in 2 col + 16 hgap + padding) e altezza centrata content-driven; risolve audit ultrawide 3440 / 4K dove cards stretchavano. (2) `sp-setup.fxml` — VBox card aggiunto `maxHeight="-Infinity"` + `fx:id="setupCard"` (audit ultrawide confermato vertical stretch con content top-anchored a TOP_LEFT — il VBox prendeva tutta l'altezza StackPane perché Region default `maxHeight=USE_COMPUTED_SIZE = -1` lascia growth illimitata; USE_PREF_SIZE = -Infinity cap al pref content-driven). (3) `settings.fxml` — stessa pattern di sp-setup, `maxHeight="-Infinity"` + `fx:id="settingsCard"`. (4) `load-screen.fxml` — `TableView fx:id="slotsTable" maxWidth="1100"` (sum prefWidth colonne = 780, 1100 leaves headroom for drag-resize), `StackPane fx:id="loadCenter"` per testabilità. (5) `rules.fxml` — wrap di `SplitPane` in `StackPane fx:id="rulesCenter"` con `SplitPane maxWidth="1200"` (audit ultrawide confermava huge empty cream area sul right pane); aggiunto import `javafx.scene.layout.StackPane`. **Save-dialog.fxml** non modificato: già modal stage con `setResizable(false)` (Task 4.5.5 verificato), `prefWidth=420` content-driven appropriato. **Splash.fxml** non modificato: già `StackPane` root con VBox center alignment, content centrato a content-size — no stretch defect a ultrawide. **`ScreenLayoutTest`** (nuovo): 5 test che usano `loader.setControllerFactory(Mockito::mock)` + `loader.getNamespace().get(fx:id)` per ispezionare i constraint senza Spring (pattern di `FxmlLoadingSmokeTest.boardViewFxmlLoads`). Asserts: `mainMenuGridCapsAtReadableWidth` (menuGrid.maxWidth=960, maxHeight=USE_PREF_SIZE), `singlePlayerSetupCardCapsAtReadableWidthAndPrefHeight` (setupCard 560/USE_PREF_SIZE), `settingsCardCapsAtReadableWidthAndPrefHeight` (stesso), `loadScreenTableCapsAtReadableWidth` (slotsTable.maxWidth=1100), `rulesSplitPaneCapsAtReadableWidth` (filtra SplitPane child del rulesCenter StackPane, asserts maxWidth=1200). save-dialog test deliberatamente omesso (FXML senza `fx:controller` — controller installato programmaticamente; modal stage con `setResizable(false)` non necessita responsive cap). **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **344/344 test verdi** (+5 vs 339 = 5 nuovi), JaCoCo gate met, SpotBugs 0, Spotless OK. Zero regressioni sui 339 test pre-esistenti.

- **Task precedente**: **F4.5 Task 4.5.5 — Side panel responsive** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #67 chiuso il 2026-05-06). Output: **1 file prod modificato** (`board-view.fxml`) + **1 file di test nuovo** (`SidePanelLayoutTest.java`, 7 test = 1 property + 6 parametric). **Cambio FXML**: side panel VBox ora dichiara `prefWidth=320` (era 280) + nuovi `minWidth=240` + `maxWidth=400` (token spacing PLAN §10.6) + `fx:id="sidePanel"` per testabilità. `MoveHistoryView VBox.vgrow="ALWAYS"` invariato (storia mosse occupa lo spazio rimanente). **Decisione senior**: auto-hide del side panel per viewport < 1024 NON implementato (CLAUDE.md "Don't add fallbacks for scenarios that can't happen"): `PrimaryStageInitializer` enforce Stage min 1024×720 (logical pixels — anche con DPI scaling, `Stage.setMinWidth` opera in coords logiche), quindi viewport < 1024 unreachable in normal operation. Se in futuro Stage min viene abbassato (es. variant tablet), l'auto-hide può essere aggiunto then con proper testing. Hook documentato nel commento FXML accanto al side panel. **`SidePanelLayoutTest`**: (a) `sidePanelDeclaresResponsiveWidthBudget` carica board-view.fxml con `loader.setControllerFactory(Mockito::mock)` (pattern di `FxmlLoadingSmokeTest.boardViewFxmlLoads`) + lookup `#sidePanel` + assert `prefWidth==320 && minWidth==240 && maxWidth==400`; (b) parametric `sidePanelStaysWithinBudgetAcrossStageWidths` su 6 stageWidth (1024 floor, 1366 likely-customer, 1920 fhd, 2560 qhd, 3440 ultrawide, 3840 4K) carica root in Scene di quella width × 720h, `applyCss()` + `layout()`, asserisce `sidePanel.getWidth()` ∈ [240, 400] e converge a `320` (BorderPane.right default behaviour: child sizes to prefWidth capped at maxWidth, indipendente dal viewport). Helper `runOnFxThread`/`loadBoardView`/`loadSidePanel` con fallback `loader.getNamespace().get("sidePanel")` se lookup pre-CSS fallisce. **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **339/339 test verdi** (+7 vs 332 = 7 nuovi parametric/property), JaCoCo gate met, SpotBugs 0, Spotless OK. Zero regressioni sui 332 test pre-esistenti.

- **Task precedente**: **F4.5 Task 4.5.4 — BoardView adattivo (cuore del fix cliente)** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #66 chiuso il 2026-05-06). Output: **2 file prod modificati** (`BoardRenderer.java`, `board-view.fxml`) + **1 file di test nuovo** (`BoardRendererLayoutTest.java`, 7 test parametrizzati). **Root-cause analisi** del defect "la parte in basso della scacchiera non si vede" del cliente: a 1366×768 dopo MenuBar (~30) + title row (~50) + side panel padding-incluso (~316) + inner padding (~32) + StackPane.board-frame padding (~48), l'area disponibile per il `BoardRenderer` ≈ 970×583. Il FXML hardcodava `BoardRenderer minWidth="640" minHeight="640"` → quando l'altezza disponibile (583) < minHeight (640), JavaFX clipping dei 57px di sotto. Inoltre cells erano posizionate a (file*cellSize, ...) top-left → renderer rectangolare lasciava strip wood-only sul lato destro a viewport non-quadrate (audit ultrawide 3440×1440 conferma). **Fix** a 2 punti: (1) `BoardRenderer.layoutChildren` ora calcola `xOffset = (w - 8*cellSize)/2` e `yOffset = (h - 8*cellSize)/2`, posiziona cells e particleLayer al centro della renderer area (8×8 grid centrato indipendentemente dal aspect ratio); (2) FXML rimuove `minWidth="640" minHeight="640"` (con commento esplicativo che lo Stage min 1024×720 garantisce comunque cellSize ~63px floor). **Conservato**: `BoardCellNode`/`PieceNode` invariati (solo posizionamento cambiato), `MoveAnimator`/`AnimationOrchestrator` invariati (animazioni usano `byX/byY` relative + cellSize, NON cell absolute positions; `ParticleEffects` riceve cx/cy in coords particleLayer-relative che ora è anch'esso centrato), cornice texture wood `frame.jpg` + StackPane.board-frame padding 24px invariati (anti-pattern #15 rispettato). **`BoardRendererLayoutTest`** (7 test parametrizzati con `@MethodSource("rendererAreas")` su 7 viewport sizes corrispondenti agli area allocati al renderer dopo subtraction dello chrome/side-panel ai 7 audit risoluzioni F4.5: 560×560 square minimo, 720×560 1366×768-laptop after chrome, 1024×720 1920×1080-fhd, 1600×1200 2560×1440-qhd, 2400×1200 3440×1440-ultrawide extreme, 1800×1800 4K square, 560×720 taller-than-wide defensive). Per ogni viewport asserisce: `cellSize = min(w,h)/8`; cell (0,0) bottom-left a `(xOffset, yOffset + 7*cellSize)`; cell (7,7) top-right a `(xOffset + 7*cellSize, yOffset)`; tutte le 64 celle dentro `(0,0,w,h)` rect (no clipping); `particleHost` overlays exactly the centered board area `(xOffset, yOffset, total, total)`; `currentCellSize()` matcha. Pattern `@BeforeAll Platform.startup` + `Assumptions.assumeTrue(fxToolkitReady)` self-skip headless (ADR-018). **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **332/332 test verdi** (+7 vs 325 = 7 nuovi parametrici), JaCoCo gate `All coverage checks have been met`, SpotBugs 0, Spotless OK. **Zero regressioni** sui 325 test pre-esistenti — tutti i 13 test BoardKeyboardNavigation/BoardLayoutMath/BoardViewController/HighlightState passano con i nuovi offset di posizionamento.

- **Task precedente**: **F4.5 Task 4.5.3b — Asset audit texture wood (PASS)** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #65 chiuso il 2026-05-06). **Docs only**: nessun file prod/test/asset toccato. Audit `client/src/main/resources/assets/textures/`: 3 file totali (no separate "header background" — `.main-menu-root` e `.screen-root` riusano `board_dark.jpg` per coerenza visiva). Tutti i 3 file sono **2048×2048**, esattamente alla soglia minima del PLAN (≥2048 per nitidezza a 4K, ≤4096 cap senior amendment §6 per HDD vecchio + 4 GB RAM cliente). Frame BorderImage: `frame.jpg` 2048 ≥ 1024 (PLAN minimum BorderImage). Licensing CC0 1.0 (Poly Haven) già documentato in `CREDITS.md` linee 36-38: `board_dark.jpg` da `dark_wooden_planks` (Poly Haven), `board_light.jpg` da `oak_wood_planks` (Poly Haven), `frame.jpg` da `wood_table_worn` (Poly Haven, foto Dimitrios Savva, processing Rico Cilliers). **Verdict audit PASS** — nessuna replacement necessaria. Note tecnica: a 4K (3840×2160) viewport, board cells ~240px → 2048→240 ~8× downscale (super sharp); frame texture cover full frame ~3840×2160 → 2048→3840 ~1.9× upscale (accettabile per pattern wood organico dove minor blur è mascherato dal noise della texture). Aggiornamenti: AI_CONTEXT (questo file) + CHANGELOG entry, build invariato (no test eseguiti — audit puramente documentale, pattern Task 4.13 F4 docs-only).

- **Task precedente**: **F4.5 Task 4.5.3 — Stage min/initial size + center policy** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #64 chiuso il 2026-05-06). Output: **1 file prod nuovo** (`client/src/main/java/com/damaitaliana/client/app/PrimaryStageInitializer.java`, `@Component` con `MIN_WIDTH=1024`, `MIN_HEIGHT=720`, `INITIAL_SIZE_RATIO_PERCENT=80`, metodo `initialize(Stage)` che setta `minWidth`/`minHeight` + `width=max(MIN, bounds.w*0.80)` / `height=max(MIN, bounds.h*0.80)` da `Screen.getPrimary().getVisualBounds()` + center calculation `x=bounds.minX+(bounds.w-w)/2`, `y=bounds.minY+(bounds.h-h)/2`) + **1 file prod modificato** (`JavaFxApp.start` chiama `context.getBean(PrimaryStageInitializer.class).initialize(primaryStage)` tra `router.show(SPLASH)` e `setTitle/show`) + **1 file di test nuovo** (`PrimaryStageInitializerTest`, **4 test**: nullStageThrowsNpe + 3 FX-toolkit-required `@BeforeAll Platform.startup` + `Assumptions.assumeTrue(fxToolkitReady)` self-skip pattern: minSizeMatchesF45Floor, initialSizeIsAtLeastMinAndComputedAt80PercentOfPrimaryScreen, positionIsCenteredOnPrimaryScreenVisualBounds; helper `runOnFxThread(Callable)` `Platform.runLater`+`CountDownLatch.await(10s)`+`AtomicReference<Throwable>` rethrow). **Decisione senior** — DPI awareness via JVM flags (`-Dprism.allowhidpi=true`, `-Dglass.win.uiScale=N`) **NON è in scope di Task 4.5.3**: JavaFX 21 ha per-screen DPI awareness ON di default su Windows; la matrice DPI 125/150/200% è verificata manualmente in Task 4.5.10 (TEST-PLAN-fase-4.5 §5.2). Save dialog non eredita le costanti perché ha proprio modal stage con `setResizable(false)` (verificato a `SaveDialogController.java:117`); splash è il primary stage durante una finestra transitoria di ~1.5s — inheritance del min size accettabile (utente non resiziona durante quel breve span). Maximize policy: defaults JavaFX corretti (Stage maximizable via OS button, default windowed a 80%, min size hard floor when un-maximize); persistenza dello stato `Maximized` deferred a Task 4.5.7b. **Build**: `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 47s, **325/325 test verdi** (+4 vs 321 F4 baseline = i 4 nuovi), JaCoCo gate `All coverage checks have been met` (70 classi vs 69 — `PrimaryStageInitializer` aggiunto al bundle, copertura via i 3 test FX), SpotBugs 0, Spotless 139 file kept clean.

- **Task precedente**: **F4.5 Task 4.5.2 — automated baseline screenshot capture (audit pre-fix)** (sotto-fase IMPLEMENTA mini-fase F4.5, Task #63 chiuso il 2026-05-06). Output: **1 file di test nuovo** (`client/src/test/java/com/damaitaliana/client/layout/BaselineScreenshotCapture.java`, `@SpringBootTest` con `ClientApplication` + `@Tag("slow")` + `@ParameterizedTest` 56 esecuzioni 7 risoluzioni × 8 schermate) + **2 PNG defect samples committati** (board-view 1366×768 ~1 MB likely-customer-class, board-view 3440×1440 ~6.4 MB ultrawide-extreme) + **1 `.gitignore`** in `tests/visual-review/responsiveness-baseline-pre-fix/` che esclude i restanti 54 PNG (~183 MB rigenerabili in 33s via `mvn -pl client test -Dgroups=slow -Dtest=BaselineScreenshotCapture`). **Risoluzioni canoniche**: 1024×720-floor, 1280×720-small, 1366×768-laptop, 1920×1080-fhd, 2560×1440-qhd, 3440×1440-ultrawide, 3840×2160-4k (tutte 100% logical DPI — JavaFX 21 prende il render scale da JVM startup args, single-JVM run non può esercitare 125/150/200%; le varianti DPI saranno verificate post-fix da manual launches con `-Dprism.allowhidpi=true -Dglass.win.uiScale=N`, documentato in TEST-PLAN-fase-4.5 §5.2). **Schermate**: splash, main-menu, sp-setup, board-view, load-screen, settings, rules, save-dialog. **Strategia controller**: `loader.setControllerFactory(springContext::getBean)` per le 7 con `fx:controller=...`; `loader.setController(springContext.getBean(SaveDialogController.class))` per save-dialog (FXML senza `fx:controller`, controller prototype-bean). **Helper `ensureFakeGameSession()`**: pre-popola `GameSession` con `SinglePlayerGame(PRINCIPIANTE, WHITE, "F4.5 baseline", GameState.initial(), SplittableRandom(0L))` per board-view/save-dialog (altrimenti `BoardViewController.initialize()` redirige a MAIN_MENU). **Conversion `WritableImage → BufferedImage`**: bulk via `PixelReader.getPixels(0, 0, w, h, PixelFormat.getIntArgbInstance(), int[w*h], 0, w)` + `BufferedImage(w, h, TYPE_INT_ARGB).setRGB(0, 0, w, h, argb, 0, w)` — evita di aggiungere `javafx-swing` come dep (CLAUDE.md §8.13 fuori SPEC §6); ~100ms su 4K vs pixel-by-pixel ~secondi. **`runOnFxThread` helper**: `Platform.runLater` + `CountDownLatch.await(20s)` + `AtomicReference<Throwable>` re-throw — pattern già usato dai `*FxmlSmokeTest`. Headless self-skip via `Assumptions.assumeTrue(fxToolkitReady)` come ADR-018. **Esecuzione**: BUILD SUCCESS in 33s, **56/56 test verdi**, 56 PNG prodotti (sizes da 422 KB a 16 MB, totale 190 MB). **Audit findings dell'utente senior graphics**:
  - **board-view (la schermata che il cliente ha segnalato clipped)**:
    - 1024×720 floor: board fits visualmente ma side panel cramped, frame minimo;
    - **1366×768 likely-customer**: ⚠️ board fixed-size (non cresce con la viewport), wood texture middle area sprecata fra board e side panel — defect estetico evidente, conferma il "non si adatta" del cliente;
    - 1920×1080 fhd: board ancora fixed → wasted space ovunque;
    - **3440×1440 ultrawide**: ⚠️⚠️ defect estremo, board piccolo top-left, ENORME area middle wood vuota, side panel cramped right — caso peggiore, conferma necessità ultrawide aspect handling esplicito di ADR-043 §1 (centered cluster + side margins);
    - 4K: stesso pattern (board piccolo, wasted space).
  - **main-menu**:
    - 1024–1920: 5 card centrate, layout OK;
    - 3440 ultrawide / 4K: ⚠️ card stretchano horizontally fino a riempire la viewport → diventano sproporzionate (cards quasi vuote con tiny content centrato) — necessita max-width constraint + aspect ratio handling come per board-view.
  - **sp-setup, settings, splash, rules**: card-based modali / hero centered, layout fondamentalmente OK a 1024–1920; a ultrawide/4K stessa pattern di stretch sproporzionato (problema cosmetico, non funzionale).
  - **load-screen**: pattern simile a main-menu (lista screen scrollabile, OK fino a FHD, stretchy oltre).
  - **save-dialog**: PNG ~10 KB (background trasparente / dialog non-modale renderizzato a vuoto fuori dal flusso showAndWait reale) — limite noto del capture pattern, copertura post-fix sarà manual demo durante test board-view fix.
  **Conclusione audit senior**: il defect è **strutturale**, non di styling — riguarda 7 schermate su 8 (save-dialog escluso); board-view ha 2 difetti gravi (clipping verticale a 1366×768 risolvibile + non-scaling con viewport risolvibile) + ultrawide extreme; main-menu/load-screen/setup-screens hanno difetti cosmetici da max-width constraint. **Valida pienamente Task 4.5.4 (board-view StackPane centered + Region.bind aspect ratio) come core del fix**, Task 4.5.5 (responsive layout schermate non-game), Task 4.5.6 (tipografia fluida programmatic). **Non modifica le decisioni del PLAN-fase-4.5 v1.1**: l'audit conferma le ipotesi del PLAN.

- **Task precedente**: **F4.5 Task 4.5.1 — apply CR-F4.5-001 to SPEC + add ADR-043** (sotto-fase IMPLEMENTA mini-fase F4.5, commit `d3a431e` `docs(spec)` + `6cd8d63` `docs(arch)` su `feature/4.5-ui-responsiveness`). **SPEC v2.2 → v2.3**: §5 nuovo NFR-U-05 (UI client adattiva 1280×720 → 4K, min Stage 1024×720, DPI 100/125/150/200%, Stage state persistito v3); §13.7 nuova sezione "Layout responsive" 9-bullet (Stage constraints, BoardView centered StackPane, side panel adattivo, tipografia fluida programmatic, ultrawide aspect handling, REPEAT texture mode, DPI awareness, UiScalingService ortogonale, anti-pattern #15 ribadito); §17.2.8 nuovo AC verificato via `ResponsivenessParametricTest @Tag("slow")` 7 combinazioni + manual demo run; §16 nuova entry Fase 4.5 tra F4 e F5 (modello F3.5 mini-fase intermedia post-demo cliente). **ADR-043** in `ARCHITECTURE.md`: documenta stack JavaFX puro (no framework esterno) + 8-bullet decisione + 5 alternative scartate (MigLayout/FormsFX/ControlsFX, ScrollPane fallback, CSS `clamp()`, schema v4 separato, ultrawide growing board) + conseguenze (8 schermate ridisegnate solo layout/binding/CSS responsive, nuovo helper `JavaFxScalingHelper.bindFluidFontSize` in package nuovo `client.layout`, 56 assertion bundle CI + manual ultrawide). Nessuna modifica a codice prod/test (docs only).
- **Sotto-fase**: **TEST mini-fase F4.5 CHIUSA il 2026-05-06** (CLAUDE.md §2.4 step 6 closure pre-check). `tests/TEST-PLAN-fase-4.5.md` prodotto (~150 righe: scope, strategia, coverage, esecuzioni E1-E5, test code delta +84 fast +56 slow, manual procedures §6.1-6.3, conseguenze F5+). **E5 regression cross-module**: `mvn clean verify` (root, slow + performance inclusi) BUILD SUCCESS in **18:16 min**, **1019 test verdi** (391 shared + 166 core-server + 461 client + 1 server). Gating IA F2 PASS, corpus regole F1 (53 pos) PASS, performance NFR-P-02 PASS, schema migration v1→v2→v3 PASS, JaCoCo gate verdi tutti i moduli, SpotBugs 0 High, Spotless OK. **`tests/TRACEABILITY.md` post-TEST**: A4.5.10 promosso PARTIAL → ✅ COVERED (compose F-009 in `4086572`), A4.5.18 promosso DEFERRED → ✅ COVERED (E5). **A4.5.x finale: 17 ✅ COVERED, 3 ⚠️ DEFERRED a manual sign-off cliente** (A4.5.11 DPI matrix, A4.5.12 resize-jank, A4.5.16 demo cliente — template procedure documentata in TEST-PLAN §6 con sign-off table). **Closure check sotto-fase TEST**: 6/8 condizioni met automaticamente; 2 condizioni pending utente (manual demo + DPI verification, fuori scope codice). **F4.5 ready for fase closure**: merge develop+main + tag v0.4.5 una volta che il cliente ha sign-off le 3 manual procedures, oppure decidere di procedere con tag v0.4.5 + sign-off async (decisione utente). **Storico sotto-fasi**: PIANIFICA chiusa 2026-05-06 (PLAN approvato), IMPLEMENTA chiusa 2026-05-06 (12 commit, 4.5.1 → 4.5.10), REVIEW chiusa 2026-05-06 (8 commit aggiuntivi, 13 findings risolti + 2 deferred), TEST chiusa 2026-05-06 (E5 regression). **Branch state**: `feature/4.5-ui-responsiveness` HEAD `30131e3`, 21 commit feature ahead di develop, 0 conflict atteso al merge `--no-ff` (CLAUDE.md §4.4 GitFlow workflow). Storico: REVIEW mini-fase F4.5 CHIUSA il 2026-05-06 (CLAUDE.md §2.3 step 9). Fix loop completato in 7 commit logici (`15958e8`, `1090345`, `0268be4`, `4086572`, `0dc609b`, `ebfbb84`): tutti i 13 findings actionable risolti, 2 deferred con rationale (F-010 pre-existing F3 pattern, F-012 perf F8+ con TODO). **Test count delta**: 379 → **405 fast verdi (+26)** (8 `StagePersistenceCoordinatorTest` + 4 `UserPreferencesTest` + 3 nuovi `StagePersistenceValidatorTest` 50%-intersection scenarios + 1 `BoardFrameThicknessHelperTest` pre-layout fallback + 8 `JavaFxScalingHelperTest` compose parametric + 2 `UiScalingServiceTest` activeScaleFactor); slow tag invariato a **56/56 `ResponsivenessParametricTest`** (lo stricter validator e la composed fluid typography non rompono il parametric — re-run BUILD SUCCESS in 45.89s). **High findings risolti**: F-001 Stage state persistence rework (continuous windowed-bounds tracking + deferred setMaximized + NaN guard), F-002 Validator 50% intersection (no più sliver-on-screen-edge). **REQUIREMENT_GAP F-009 risolto** con scelta architetturale **COMPOSE** (UI scaling × fluid typography) — A4.5.10 ortogonalità ora coperta da test parametric. **Anti-pattern #15**: ✅ verified per tutta la durata del fix loop — solo modifiche layout/binding/CSS responsive, nessuna deriva su tokens/texture/font/animation params/AudioService/MoveAnimator/ParticleEffects/AudioService/BoardCellNode/PieceNode. **`reviews/REVIEW-fase-4.5.md` aggiornato** con Resolution log + closure box ticked. **Pronto per stop point**: avvio sotto-fase **TEST F4.5** (CLAUDE.md §2.4 — TEST-PLAN-fase-4.5.md, traceability matrix update con test deltas, manual demo run cliente Task A4.5.16, mvn clean verify root regression A4.5.18). In attesa di conferma utente prima di avviare TEST (memoria `feedback_ask_before_next_task.md`). Storico: IMPLEMENTA F4.5 chiusa il 2026-05-06 (12 commit), REVIEW initial pass + fix loop 2026-05-06 (8 commit aggiuntivi). `reviews/REVIEW-fase-4.5.md` prodotto con **15 findings** (0 Critical/BLOCKER, 2 High/BUG: F-001 + F-002, 8 Medium, 5 Low) + **AC coverage table** (13 ✅ COVERED, 2 ⚠️ PARTIAL A4.5.8/A4.5.14 con doc-only concern, 4 ⚠️ DEFERRED to TEST sotto-fase A4.5.10/11/12/16/18). **High findings (devono essere risolti per closure)**: **F-001** (`StagePersistenceCoordinator` cattura geometry maximized come window state — primo close-da-maximized perde la "windowed memory" dell'utente) + **F-002** (`StagePersistenceValidator` accetta finestre con solo top-left on-screen ma right/bottom off-screen — secondary unplugged non sempre catched). **REQUIREMENT_GAP F-009**: nessun automated test per ortogonality `UiScalingService` × `JavaFxScalingHelper` (A4.5.10) — decisione "compose vs document" pendente. **Anti-pattern #15 attestation**: ✅ verified — solo modifiche layout/binding/CSS responsive, no design tokens / texture paths / font families / animation params / `MoveAnimator`/`ParticleEffects`/`AudioService`/`BoardCellNode`/`PieceNode` modificati. **SPEC change requests**: nessuna (F4.5 implementation matcha SPEC v2.3 CR-F4.5-001 approvato). **Pronto per stop point**: discussione findings con utente — quali addressare subito vs deferred vs SPEC change request. CLAUDE.md §2.3 step 6: presentare review all'utente, decidere quali fix applicare nella REVIEW (codice). Storico: IMPLEMENTA mini-fase F4.5 chiusa il 2026-05-06 (12 commit su `feature/4.5-ui-responsiveness`, fast loop 379/379 verdi, slow tag +56 verdi); avvio REVIEW il 2026-05-06. Avvio sotto-fase IMPLEMENTA con **PIANIFICA F4.5 chiusa il 2026-05-06 (PLAN approvato dall'utente con default raccomandazioni + 5 senior amendments applicati al PLAN v1.1)**: (1) initial Stage size = computed 80% del primary screen visualBounds, centrato (sostituisce `1366×768` fisso); (2) aspect ratio handling esplicito per ultrawide 21:9/32:9 (cluster centrato, margini wood decorativi laterali); (3) Stage state persistence schema v3 in `~/.dama-italiana/config.json` (windowWidth/Height/X/Y/Maximized) — nuovo Task 4.5.7b; (4) asset audit upfront texture wood ≥ 2048×2048 (Task 4.5.3b prerequisite di BoardView refactor); (5) test matrix DPI focused: 7 combinazioni strategiche (1024×720 floor, 1280×720 small, 1366×768@125% Win laptop più diffuso, 1920×1080@100/150%, 2560×1440@100%, 3840×2160@200%) invece di 5×4=20 esaustive. **Tag finale previsto**: `v0.4.5`. F5 server skeleton resta F5 con tag `v0.5.0`. **SPEC change request CR-F4.5-001** approvata implicitamente con il PLAN: aggiunge NFR-U-05 (UI responsive desktop 1280×720 → 4K, min 1024×720, DPI 100/125/150/200%), nuova §13.7 (layout responsive — Stage min/initial constraints, BoardView centered StackPane, side panel adattivo, tipografia fluida programmatic, REPEAT texture mode, DPI awareness), AC §17.2.8 (UI responsive desktop), entry §16 Fase 4.5. **Prossima azione**: commit PLAN su develop (`docs(plan): add PLAN-fase-4.5`), creare branch `feature/4.5-ui-responsiveness`, attivare Task 4.5.1 (SPEC CR application + ADR-043 nuova "Strategia layout responsive"). In attesa di conferma utente prima di Task 4.5.1 (memoria `feedback_ask_before_next_task.md`).

- **Chiusura Fase 4** (2026-05-06): merge `feature/4-core-server-skeleton` → `develop` (commit `c3983a0`), `develop` → `main` (commit `face88c`), tag `v0.4.0` annotato su `main` (push completato). Tutte e 4 le sotto-fasi PIANIFICA → IMPLEMENTA → REVIEW → TEST chiuse. Branch effimero `feature/4-core-server-skeleton` eliminato post-merge. **Note storiche F4**: ⏸️ PLAN approvato dall'utente il 2026-05-02 (commit `ce3db1c` `docs(plan): add PLAN-fase-4`). 4 stop point §7 chiusi: §7.1 solo interfaces + UnsupportedOperationException per logica torneo (deferred F8/F9), §7.2 anti-cheat counter transient in `MatchManager`, §7.7 JaCoCo ≥80% line+branch con esclusioni record/enum DTO + `CoreServerConfiguration`, §7.10 nuova dipendenza approvata `archunit-junit5:1.3.0` test-scope per enforcement CLAUDE.md §8.7-8.8 transport-agnostic. Stop point §7.3-§7.6 e §7.8-§7.9 chiusi via raccomandazione. Vincolo cross-fase UI restyling F3.5 promosso ad anti-pattern #15 in `CLAUDE.md` (commit `6d00ca6` su `develop`, prima dello stacco del feature branch) — non bite F4 (no UI), bite F6+. **Ultimo task completato**: **Task 4.2 — Domain value types (records, enums) + MatchEvent sealed hierarchy** (PLAN-fase-4 §4.2). Output: 21 classi prod nuove + 4 file di test, 36 test nuovi (37 totali con `CoreServerConfigurationTest`), tutti verdi. **Value types in `com.damaitaliana.core.match`** (8 file): `MatchId(UUID)` con factory `random()`/`of(String)`; `UserRef(long, String, String)` con sentinel `ANONYMOUS_LAN_ID = -1L`, factory `anonymousLan(username)` (LAN no-auth) e `authenticated(id, username, displayName)` (server-side), helper `isAnonymous()`, validation null/blank; `TimeControl(preset, initialMillis, incrementMillis)` come metadato (no clock running in F4 — clock arriva F6); `TimeControlPreset` enum 4 valori (BLITZ_5_3, RAPID_15_10, CLASSICAL_30_30, UNLIMITED) con factory `asTimeControl()`; enum `MatchStatus` (WAITING/ONGOING/FINISHED/ABORTED), `MatchResult` (WHITE_WINS/BLACK_WINS/DRAW/UNFINISHED), `EndReason` 7 valori (CHECKMATE_LIKE/RESIGN/DRAW_AGREEMENT/DRAW_REPETITION/DRAW_FORTY_MOVES/FORFEIT_ANTI_CHEAT/FORFEIT_TIMEOUT — granularità che estende SPEC §8.3 per il flusso anti-cheat del PLAN, reviewable in REVIEW Fase 4), `RejectionReason` 4 valori (NOT_YOUR_TURN/ILLEGAL_MOVE/MATCH_NOT_ONGOING/MATCH_NOT_FOUND). **Value types in `com.damaitaliana.core.tournament`** (3 file): `TournamentId(UUID)` (parallelo MatchId), `TournamentMatchRef(TournamentId, roundNo, matchIndex)` con validation 0-indexed, `TournamentStatus` enum (CREATED/IN_PROGRESS/FINISHED). **MatchEvent sealed hierarchy in `com.damaitaliana.core.match.event`** (10 file): `MatchEvent` sealed interface con 9 permits `MoveApplied`/`MoveRejected`/`DrawOffered`/`DrawAccepted`/`DrawDeclined`/`Resigned`/`MatchEnded`/`PlayerDisconnected`/`PlayerReconnected`, common `matchId()`/`sequenceNo()`/`timestamp()`. Ogni record valida null + sequenceNo ≥ 0; `MatchEnded` valida che `result != UNFINISHED`. **Test deps aggiunte** a `core-server/pom.xml` con `<scope>test</scope>`: `jackson-databind` + `jackson-datatype-jsr310` (Spring Boot BOM-managed, nessuna versione esplicita; production code NON dipende da Jackson — il wire layer F6/F7 porta il proprio ObjectMapper + Module). **4 file di test**: `MatchValueTypesTest` (15 test in 3 nested class `MatchIdValidation` 4 + `UserRefValidation` 6 + `TimeControlValidation` 5+1 invariant); `TournamentValueTypesTest` (7 test in 2 nested); `MatchEventSealedTest` (4 test, esaustività switch su 9 varianti checked at compile-time, common interface coverage, validation MatchEnded UNFINISHED + sequenceNo negativo); `EventSerializationTest` (9 test, 8 roundtrip Jackson default + `MoveApplied` constructor-only smoke). **Issue Jackson risolto**: `UserRef.isAnonymous()` veniva detected da Jackson default come is-getter "anonymous" (campo extra in JSON output → deser fallisce su unknown field). Fix: `mapper.disable(MapperFeature.AUTO_DETECT_IS_GETTERS)` nel test mapper (no Jackson annotations in prod, rispetta CLAUDE.md §8.8). **Issue Board risolto**: `Board` (non-record con `Piece[] squares` private) non Jackson-default-serializable — `MoveApplied` roundtrip rinviato a F6 con custom Module per `Move` sealed + `Board`. F4 verifica solo construction-side via component getters. **Naming exhaustive switch**: pattern `case Variant unused ->` per evitare unused-variable warnings (Spotless OK). `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 5:22 min: **37/37 test verdi**, JaCoCo gate `All coverage checks have been met` (21 classi analizzate, parent default `haltOnFailure=false`), SpotBugs 0, Spotless OK. **Prossima azione**: avvio sotto-fase **TEST Fase 4** — scrivere `tests/TEST-PLAN-fase-4.md` documentando i 3 test deferred dalla sotto-fase IMPLEMENTA (A4.12 concurrency stress, A4.18 regression Corpus + AI tournament, A4.19 mvn clean verify root con slow+performance) + composizione test piramide (CLAUDE.md §2.4.1). Aggiungere `InMemoryRepositoryConcurrencyTest @Tag("slow")` (16 thread × 1000 match × 10000 mosse parallele). Eseguire `mvn clean verify` root con `slow`+`performance` per A4.18 (RuleEngineCorpusTest F1 48 posizioni invariate + AiTournamentSimulationTest F2 gating ≥95/100 invariato) + A4.19 (BUILD SUCCESS root). Aggiornare `tests/TRACEABILITY.md` con A4.12/A4.18/A4.19 RESOLVED. Closure check 6/6. In attesa di conferma utente (memoria `feedback_ask_before_next_task.md`).

- **Task precedente**: TEST sotto-fase Fase 4 (CLAUDE.md §2.4 procedura). Output: **1 file di test nuovo** (`core-server/src/test/java/com/damaitaliana/core/repository/InMemoryRepositoryConcurrencyTest.java`, 3 test `@Tag("slow")`) + **1 test plan nuovo** (`tests/TEST-PLAN-fase-4.md` popolato §1-§7) + TRACEABILITY aggiornato (A4.12/A4.18/A4.19 da DEFERRED a ✅ COVERED) + AI_CONTEXT (questo file) + CHANGELOG entry. **`InMemoryRepositoryConcurrencyTest`** in `com.damaitaliana.core.repository` con helper centrale `runConcurrently(threads, timeoutSeconds, worker, failures)` (`ExecutorService.newFixedThreadPool(16)` + `CountDownLatch` start gate + done gate + try/catch → failures list, worker partono tutti allo stesso momento per max contesa): (1) `manyMatchesCreatedConcurrentlyDoNotCorruptRepository` — 16 thread × 1000 `save()` parallel work-stealing via `AtomicInteger.getAndIncrement` con limit gate; verifica size=1000, ID univoci (`HashSet` count=1000), ogni `findById` presente, `findByStatus(ONGOING)` ritorna 1000 senza CME, `findByPlayer(ALICE)` ritorna 1000; (2) `manyAppendsOnDifferentMatchesPreserveMonotonicSequencePerMatch` — 64 match pre-creati, 16 thread × 10000 `appendEvent` parallel con `Map<MatchId, Object>` per-match caller-side lock (riproduzione esatta del pattern `MatchManager` produzione: ogni write su match X serializza con `synchronized(matchLocks.get(X))`); verifica per ogni match log strict monotonic da 0 (`log.get(i).sequenceNo() == i`), somma totale eventi=10000, `currentSequenceNo == size-1` per ogni match; (3) `manyAppendsOnSameMatchUnderCallerLockPreserveMonotonicSequence` — bonus contention test 16 thread × 1000 `appendEvent` su singolo match con caller-side `Object lock`, esercita `synchronized (log)` interno di `InMemoryMatchRepository.appendEvent` come seconda barriera defense-in-depth. **E3 Stress concorrenza A4.12** (`mvn -pl core-server test -Dgroups=slow -Dtest=InMemoryRepositoryConcurrencyTest`): BUILD SUCCESS in 6.7s, **3/3 test in 0.435s**, 0 failures, 0 CME, 0 dropped events. **E4 Regression finale** (`mvn clean verify` root, **inclusi** `slow,performance`): BUILD SUCCESS in **18:55 min**, **879 test verdi** (`shared` 16:49 min 391 = 387 default + 1 slow AI tournament gating Campione vs Principiante ≥95/100 + 3 performance NFR-P-02; `core-server` 37.6s 166 = 163 fast + 3 slow stress; `client` 1:10 min 321 invariato F3.5; `server` 14.0s 1 smoke). SAST SpotBugs 0 High, Spotless 0 file da riformattare, ArchUnit 5/5 verdi, JaCoCo gate `core-server` `haltOnFailure=true` ≥80% verde con esclusioni record DTO/enum (post F-001 RESOLVED). **A4.18 ✅ COVERED**: regression `RuleEngineCorpusTest` F1 53 posizioni (48 corpus + 5 tactical F3.21) + `AiTournamentSimulationTest` F2 gating ≥95/100 invariati e verdi. **A4.19 ✅ COVERED**: BUILD SUCCESS root con slow+performance. Nessuna regressione cross-module rispetto al baseline F3.5. **Closure check 7/7 spuntate** (CLAUDE.md §2.4.6): coverage raggiunti, TRACEABILITY aggiornato, corpus invariato, `mvn clean verify` root SUCCESS, test plan §1-§7, nessun `@Disabled`/`@Ignore` senza issue, REVIEW findings F-001+F-002 RESOLVED. **Sotto-fase TEST Fase 4 chiusa**.

- **Task precedente**: REVIEW Fase 4 (CLAUDE.md §2.3 procedura 1-9) — chiusura sotto-fase REVIEW. Output: 1 file di review nuovo (`reviews/REVIEW-fase-4.md`) + 1 file pom modificato (`core-server/pom.xml` JaCoCo gate override) + 2 file di test nuovi (`InMemoryTournamentRepositoryTest`, `InMemoryUserRepositoryTest`). **Apertura `99a1535`**: review code-by-code di 56 file prod + 21 file test post-IMPLEMENTA, mapping AC §17.1.2/§17.1.10/§17.1.11 + AC F4 A4.1..A4.19 + FR-COM-01/02/04 + NFR-M-01/04 al codice/test. **2 findings emersi**: F-001 [REQUIREMENT_GAP, High] JaCoCo gate non attivo in `core-server/pom.xml` (eredita default Fase 0 `haltOnFailure=false` + `min=0.00`); coverage misurata branches 78.36% (sotto soglia 80% PLAN §7.7); F-002 [REQUIREMENT_GAP, Medium] `InMemoryTournamentRepository` (5/11 lines, 0/4 branches) + `InMemoryUserRepository` (3/11 lines, 0/4 branches) non hanno test diretti. **Stop point §2.3.6** presentato all'utente con 3 opzioni F-001 (A wire gate + esclusioni, B wire gate + nuovi test, C abbassare soglia branches). **Decisione utente** (2026-05-06 "lascia a te la scelta"): applicata Opzione A + parte di B (raccomandazione del reviewer). **Closure**: (a) `core-server/pom.xml` aggiunge override `jacoco-check` execution con `haltOnFailure=true` + LINE/BRANCH 0.80 + 19 esclusioni `**/event/*.class` (sealed event records) + value-type records (MatchId, UserRef, TimeControl, TournamentMatchRef, TournamentId, TournamentSpec, EliminationTournament, RoundRobinTournament) + enum DTO (TimeControlPreset, MatchStatus, MatchResult, EndReason, RejectionReason, TournamentFormat, TournamentStatus) + glue (CoreServerConfiguration, MatchNotFoundException, MatchEventEnvelope); stesso `<excludes>` block replicato sull'execution `jacoco-report` per HTML coerente. (b) `InMemoryTournamentRepositoryTest` 6 test in `com.damaitaliana.core.repository`: save+findById roundtrip, save overwrites, findById empty per id sconosciuto, findByStatus filtra esattamente 3 status (CREATED/IN_PROGRESS/FINISHED) da repo multi-tournament, findByStatus empty su repo fresca, findByStatus result è `List.copyOf` immutable. (c) `InMemoryUserRepositoryTest` 7 test: register popola entrambi gli indici per user authenticated, anonymous LAN user è indicizzato solo by-username (id-side ritorna empty per ANONYMOUS_LAN_ID), findById ritorna empty per il sentinel ANONYMOUS_LAN_ID anche con repo non vuota, findById empty per id sconosciuto, findByUsername empty per username sconosciuto, register sovrascrive snapshot precedente per stesso id, multi-user authenticated coexistono in entrambi gli indici. **Closure check 6/6 spuntate**: 0 BLOCKER, 2/2 REQUIREMENT_GAP RESOLVED, 0 BUG/SECURITY/PERFORMANCE/CODE_QUALITY/DOC_GAP, 0 SPEC change requests. `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 25.2s, **163/163 test verdi** (150 + 13 nuovi: 6 tournament repo + 7 user repo), JaCoCo gate ATTIVO (`All coverage checks have been met` su 11 classi nel bundle post-esclusione vs 38 pre-esclusione), **lines 94.95% (301/317)**, **branches 88.64% (78/88)** — entrambi ben sopra il gate 80%. SpotBugs 0 issues, Spotless OK (79 file kept clean), ArchUnit 5/5 rule verdi. **A4.12 / A4.18 / A4.19 restano DEFERRED a sotto-fase TEST** per design (PLAN §5.3).

- **Task precedente**: Task 4.13 — Documentation + ADR + CHANGELOG + TRACEABILITY (chiusura sotto-fase IMPLEMENTA Fase 4, PLAN-fase-4 §4.13). **Docs only**: nessun file di produzione o test toccato. Output: **3 file modificati** (`ARCHITECTURE.md`, `tests/TRACEABILITY.md`, `CHANGELOG.md`) + AI_CONTEXT.md (questo file). **`ARCHITECTURE.md`**: appiccati 5 nuovi ADR in coda alla sezione "Decisioni successive (ADR-018+)" prima della sezione "## Vincoli architetturali invariabili": (1) **ADR-038** `MatchEventBus` su Spring `ApplicationEventPublisher` — rationale: zero deps nuove, `@TransactionalEventListener` future-ready per F6, FIFO sincrono per publisher thread; (2) **ADR-039** `StompCompatiblePublisher` come port lato `core-server`, impl reale lato transport — rationale: rispetta CLAUDE.md §8.8, F6/F7 introdurranno `WebSocketStompPublisher @Primary` per autowire override; (3) **ADR-040** Anti-cheat counter location: in-memory transient su `Match`, threshold 5 — rationale: counter resetta al recovery (scelta benevola), per-color independence; (4) **ADR-041** In-memory repository concurrency strategy: `ConcurrentHashMap` + `synchronizedList` + `synchronized (log)` block per atomic check+append della FR-COM-04, lock pessimistico più forte arriva in F6 con JPA `@Lock(PESSIMISTIC_WRITE)`; (5) **ADR-042** `TournamentMatchRef` package placement: Option F (match-side, UUID raw) — rationale: rule ArchUnit 5 PLAN passa pulita, refactor minimale 5 file, DDD-faithful ID-only cross-aggregate ref. **`tests/TRACEABILITY.md`**: (a) estesa riga FR-COM-01 con copertura anti-cheat post-MatchManager (`AntiCheatTest` 3 test + `MatchManagerValidationTest` 2 test + `MatchManagerTurnTest` 1 test che bumpa il counter su NOT_YOUR_TURN); (b) aggiunta nuova riga FR-COM-04 per sequence number strict monotonic (`MatchRepositoryContractTest` 3 test + `SoloMatchEndToEndTest` assertion); (c) aggiunte 2 righe AC §17 preview: §17.1.2 (partita Internet → riusa MatchManager F4 — coperto da `SoloMatchEndToEndTest`), §17.1.10 (autosave host LAN → riusa `MatchRepository.appendEvent` + `eventsSince` replay-from-cursor); (d) **nuova sezione "Acceptance criteria di Fase 4 (PLAN-fase-4.md §2.2)"** inserita PRIMA della sezione F3.5 (ordine reverse cronologico) con tabella A4.1..A4.19 → test/verifica/output, A4.12 (concurrency stress) e A4.18-A4.19 (regression root) marcati DEFERRED a sotto-fase TEST. **`CHANGELOG.md`**: nuova entry sintetica per Task 4.13 (corrente). **AI_CONTEXT.md**: sotto-fase flippata IMPLEMENTA → REVIEW (chiusa il 2026-05-06), "Prossima azione" punta a REVIEW Fase 4 con riferimento a CLAUDE.md §2.3 procedura passo-passo. **README.md / SPEC.md**: nessun cambio (no UI nuovo, no installer, no comandi nuovi visibili all'utente; SPEC è autoritativa e non si tocca senza CR). **Nessun cambio coverage / build / test**: Task 4.13 è puramente documentale, non richiede `mvn verify` (pattern già usato in Task 3.5.15).

- **Task precedente**: Task 4.12 — ArchUnit anti-pattern check + Option F refactor `TournamentMatchRef` (PLAN-fase-4 §4.12). Output: **1 file di test nuovo** (`CoreServerArchitectureTest.java`, 5 rule ArchUnit) + **1 dep nuova** (`com.tngtech.archunit:archunit-junit5:1.3.0` test-scope, autorizzata da stop point §7.10) + **refactor sub-package layering Option F** scelto dall'utente per allineare il codice alla rule 5 del PLAN. **5 rule ArchUnit** in `CoreServerArchitectureTest`: (1) `coreServerMustNotDependOnJavaFx` — `noClasses().should().dependOnClassesThat().resideInAnyPackage("javafx..")`, (2) `coreServerMustNotDependOnSpringBootWeb` — `org.springframework.boot.web..`, (3) `coreServerMustNotDependOnTomcatOrJetty` — `org.apache.tomcat..` + `org.eclipse.jetty..`, (4) `coreServerMustNotDependOnJpaOrHibernate` — `jakarta.persistence..` + `org.hibernate..`, (5) `matchPackageMustNotDependOnTournamentPackage` — `noClasses().that().resideInAPackage("com.damaitaliana.core.match..").should().dependOnClassesThat().resideInAPackage("com.damaitaliana.core.tournament..")`. Importer `ClassFileImporter().withImportOption(DO_NOT_INCLUDE_TESTS).withImportOption(DO_NOT_INCLUDE_JARS).importPackages("com.damaitaliana.core")` — sharper della cartella classpath totale (che pulla Spring/Mockito/Jackson/JUnit). **Stop-and-ask CR Task 4.12**: la rule 5 del PLAN era in tensione con Task 4.3 che aveva intenzionalmente introdotto la coppia bidirezionale `match ↔ tournament` (Match.tournamentRef → TournamentMatchRef → TournamentId; Tournament.participants/timeControl → UserRef + TimeControl). Analisi mostrava 4 opzioni (A spostare TournamentMatchRef ma TournamentId resta in tournament, B' estrarre tutto in core.common ~50 file churn, C droppare la rule, **F spostare TournamentMatchRef in match e cambiare il campo da TournamentId a UUID raw**). Utente ha confermato F il 2026-05-06. **Refactor Option F applicato** (5 file): (a) DELETE `core-server/src/main/java/com/damaitaliana/core/tournament/TournamentMatchRef.java`; (b) CREATE `core-server/src/main/java/com/damaitaliana/core/match/TournamentMatchRef.java` come record `(UUID tournamentId, int roundNo, int matchIndex)` con stesse 3 validation null/non-negative + Javadoc che documenta il pattern DDD ID-only cross-aggregate reference + il package placement rationale; (c) `Match.java` rimosso `import com.damaitaliana.core.tournament.TournamentMatchRef` (ora same-package); (d) `Tournament.java` Javadoc aggiornato: cambiata la frase sulla bidirezionalità in "match -> tournament was decoupled in Task 4.12 (Option F)"; (e) `TournamentValueTypesTest.java` rimosso `@Nested TournamentMatchRefValidation` (4 test) + migrato in `MatchValueTypesTest.java` come nuovo nested con `UUID.randomUUID()` invece di `TournamentId.random()`. **Trade-off accettati**: chi cerca il torneo a partire dal ref fa `new TournamentId(ref.tournamentId())` (1 linea extra, 0 siti di lookup in F4 — sarà F8+); costruzione si scrive `new TournamentMatchRef(tid.value(), 0, 0)` invece di `new TournamentMatchRef(tid, 0, 0)` (4 occorrenze nei test). Il pattern è **DDD-faithful**: cross-aggregate refs sono ID-only, non type-wrapper. **Coverage**: 38 classi invariato (TournamentMatchRef è solo cambiato di package/tipo del campo, non aggiunto/rimosso). `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 27.8s, **150/150 test verdi** (145 + 5 ArchUnit nuovi; 4 test `TournamentMatchRefValidation` migrati da tournament a match — count invariato), JaCoCo gate `All coverage checks have been met`, SpotBugs 0 issues, Spotless OK (77 file kept clean — Spotless ha riformattato Javadoc del nuovo `TournamentMatchRef.java` e dell'`CoreServerArchitectureTest.java` durante l'`apply`). **Outcome architetturale**: core-server ora ha layering pulito a 2 livelli con ArchUnit-enforced direction: `repository`/`eventbus`/`stomp` → tutti possono ricevere import da match/tournament, `match` indipendente da `tournament`, `tournament → match` ammessa per UserRef + TimeControl (documentato in Tournament.java Javadoc come scelta SPEC §8.3 — i value types vivono concettualmente in match perché Match è il primo aggregate ad usarli; F8/F9 potranno valutare estrazione in `core.common` se l'esigenza emergerà).

- **Task precedente**: Task 4.11 — End-to-end Java API match test (PLAN-fase-4 §4.11). Output: **1 file di test nuovo** (`SoloMatchEndToEndTest.java`, **3 test top-level**), nessun file di produzione toccato. Acceptance autoritativa Fase 4 — verifica end-to-end che il `MatchManager` orchestri match fino a stato terminale via API Java pura. **Test 1 — `playsRandomGameUntilTerminal`**: setup `InMemoryMatchRepository` + `ItalianRuleEngine` reale + Mockito mocks bus/stomp + `Clock.fixed`; helper `playRandomGame(manager, engine, match)` loop con `Random(seed=42L)` + safety cap `MAX_PLIES=500` finché `match.status() == ONGOING`, sender derivato da `match.state().sideToMove()` (WHITE→ALICE, BLACK→BOB), random-pick uniforme da `engine.legalMoves(state)`; asserisce status=FINISHED, `events.size() == pliesPlayed + 1` (ogni ply produce 1 MoveApplied, +1 MatchEnded finale; 0 rejection perché si pesca solo da `legalMoves`), tutti gli eventi sono `MoveApplied` o `MatchEnded`, ultimo evento è `MatchEnded`, sequence number strict monotonic da 0 (verifica FR-COM-04). **Test 2 — `replayFromEventsReconstructsCurrentState`**: applica 8 ply tramite helper `playFixedNumberOfPlies`, poi helper `replayFromInitial(engine, events)` ripiega la lista filtrando `MoveApplied` e applicando ognuno via `engine.applyMove(state, applied.move())` partendo da `GameState.initial()`; asserisce `replayed.equals(match.state())` — prova event-log fidelity (la storia degli eventi è autoritativa per ricostruire lo stato). **Test 3 — `eventsBroadcastOnBusAndStomp`**: bootstrap `AnnotationConfigApplicationContext.register(CoreServerConfiguration.class, BroadcastTestConfig.class, CollectingListener.class)`. **`BroadcastTestConfig @Configuration` static inner**: espone `BufferingStompPublisher @Bean @Primary` per spostare l'autowire del `MatchManager` da `LoggingStompPublisher @Component` (default core-server) a `BufferingStompPublisher` test-scope; stesso pattern che F6 userà per swapparsi `WebSocketStompPublisher @Primary` quando il trasporto reale arriverà. **`CollectingListener @Component static final inner`**: bean Spring con `@EventListener void on(MatchEventEnvelope)` + `synchronizedList<MatchEvent> received` (stesso pattern di `SpringMatchEventBusTest`). Test fluso: `manager.createMatch(...)` + `playRandomGame(...)`, poi asserisce `busEvents = listener.received()` + `stompEvents = publisher.published().stream().map(p -> (MatchEvent) p.payload()).toList()`, `stompEvents.containsExactlyElementsOf(busEvents)` (stesso ordine, stessi oggetti — verifica che `MatchManager.emit` attraversi entrambi i canali sincronicamente FIFO), `publisher.published().allSatisfy(p -> p.topic().equals("/topic/match/" + match.id()))` (verifica `MATCH_TOPIC_PREFIX` SPEC §11.4). **Determinismo**: stesso `RANDOM_SEED = 42L` per tutti i test → run riproducibili; `MAX_PLIES = 500` ceiling difensivo (in Italian la stalemate=loss garantisce terminazione, il cap serve solo a fallire pulito anziché loopare in eternità in caso di bug futuro nel generator). **Coverage**: 38 classi invariato (no nuove classi prod). `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 29.6s, **145/145 test verdi** (142 + 3 nuovi), JaCoCo gate `All coverage checks have been met`, SpotBugs 0 issues, Spotless OK (76 file kept clean — Spotless ha riformattato Javadoc/error message del test durante l'`apply`).

- **Task precedente**: Task 4.10 — `TournamentEngine` skeleton (PLAN-fase-4 §4.10). Output: **8 file prod nuovi** in `com.damaitaliana.core.tournament` (`TournamentFormat.java` enum, `TournamentSpec.java` record, `TournamentEngine.java` interface, `BracketGenerator.java` interface, `RoundRobinScheduler.java` interface, `TieBreakerPolicy.java` interface, `NoOpTieBreakerPolicy.java` `@Component`, `StubBracketGenerator.java` + `StubRoundRobinScheduler.java` plain classes, `TournamentEngineImpl.java` `@Service`) + 1 file modificato (`package-info.java` aggiornato per riflettere il nuovo set di tipi) + **1 file di test nuovo** (`TournamentEngineSkeletonTest.java`, **24 test in 8 nested class**). **`TournamentSpec(name, format, timeControl, maxParticipants)`** record con validation null/blank-name + `maxParticipants >= 2` (2 è il minimo significativo SPEC §8.3 per single-elim). **`TournamentFormat`** enum 2 valori (`SINGLE_ELIMINATION`/`ROUND_ROBIN`) parallelo a `Tournament` sealed permits — il `createTournament` switcha sul format per costruire il giusto record. **`TournamentEngine` interface 4 metodi**: `createTournament(TournamentSpec)`, `registerParticipant(TournamentId, UserRef)`, `startTournament(TournamentId)`, `findById(TournamentId)`. **`TournamentEngineImpl @Service final class`**: ctor injection di `TournamentRepository`; `ConcurrentHashMap<TournamentId, Object> locks` + `synchronized (lockFor(id))` su `registerParticipant` per evitare lost-update (createTournament non compete: id appena generato; findById/startTournament lock-free). **API funzionante in F4**: `createTournament` genera `TournamentId.random()` + costruisce variante via `switch(spec.format())` + persiste status `CREATED` + ritorna immutabile; `registerParticipant` valida `NoSuchElementException` se id sconosciuto, `IllegalStateException` se status != CREATED ("registration closed"), `IllegalArgumentException` se partecipante duplicato; helper `withParticipants(current, roster)` switcha su `Tournament` sealed e crea il record aggiornato preservando tutti gli altri campi. **Stub**: `startTournament` lancia `UnsupportedOperationException("...deferred to Fase 8/9 — PLAN-fase-4 §4.10 risk R-2")`; `StubBracketGenerator implements BracketGenerator { generate → UOE deferred to F8 }` plain class (no `@Component` — F8 introduce `SingleEliminationBracketGenerator @Component` reale senza conflitto); `StubRoundRobinScheduler implements RoundRobinScheduler { schedule → UOE deferred to F9 }` stesso pattern. **`NoOpTieBreakerPolicy @Component`**: ritorna `List.copyOf(tied)` (input invariato + null-guards). **Roster-cap deferred decision**: il PLAN listava un check `participants.size() == maxParticipants` ma `Tournament` records (Task 4.3) NON hanno `maxParticipants` (data shape solidificata). Aggiungerlo sarebbe stato deviazione retroattiva. Soluzione: `maxParticipants` resta solo sul `TournamentSpec` (input), enforcement deferred a Fase 8 start-time validation; documentato in Javadoc di `TournamentEngine.registerParticipant`. **2 deviazioni vs PLAN-fase-4 §4.10** (analoghe a Task 4.6): (1) `@ConditionalOnMissingBean` su `NoOpTieBreakerPolicy` NON usata — vive in `spring-boot-autoconfigure`, violerebbe CLAUDE.md §8.7-8.8 (transport-agnostic). Soluzione: plain `@Component`; F9 introdurrà policy reale `@Primary` o rimuoverà NoOp. (2) `BracketGenerator`/`RoundRobinScheduler` stub class **NON sono `@Component`** — il PLAN non li listava espressamente come `@Component` (solo NoOpTieBreakerPolicy), e non avere bean evita conflitto con i futuri `@Component` reali di F8/F9. **`TournamentEngineSkeletonTest` 24 test in 8 nested class**: `CreateTournament` (5: SINGLE_ELIMINATION→EliminationTournament+CREATED+empty participants+correct name/timeControl, ROUND_ROBIN→RoundRobinTournament, persistence tramite repo, distinct calls→distinct ids, null spec→NPE), `RegisterParticipant` (6: append+persist, multi-registration preserves order, double-registration→IAE "already registered", unknown id→NoSuchElementException, format variant preserved, null args→NPE), `StartTournament` (2: throws UnsupportedOperationException con message "deferred", null id→NPE), `FindById` (2: returns persisted, returns Optional.empty per unknown), `StubGenerators` (2: `BracketGenerator.generate` throws UOE "deferred to Fase 8", `RoundRobinScheduler.schedule` throws UOE "deferred to Fase 9"), `NoOpPolicy` (3: returns input unchanged via `containsExactlyElementsOf`, returns empty per empty input, null args→NPE), `TournamentSpecValidation` (4: blank name→IAE, maxParticipants<2→IAE, null fields→NPE × 3), + 1 test top-level `registrationClosedAfterStartIsTreatedAsIllegalState` (registra una IN_PROGRESS-snapshot direttamente via `repo.save(...)` poi prova a registrare → IAE "registration closed", simula F8/F9 transition senza poterla fare via engine in F4). **Coverage**: 38 classi analizzate vs 32 di Task 4.9 (+6 prod: `TournamentFormat` + `TournamentSpec` + `NoOpTieBreakerPolicy` + `StubBracketGenerator` + `StubRoundRobinScheduler` + `TournamentEngineImpl`; le 4 interface bytecode-zero non entrano nel bundle). `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 28.7s, **142/142 test verdi** (118 + 24 nuovi), JaCoCo gate `All coverage checks have been met`, SpotBugs 0 issues, Spotless OK (75 file kept clean — Spotless ha riformattato Javadoc in 5 file durante l'`apply` per line-wrap 100 col).

- **Task precedente**: Task 4.9 — Anti-cheat tests + counter behavior end-to-end via `MatchManager` (PLAN-fase-4 §4.9). Output: 1 file di test nuovo (`AntiCheatTest.java`, 3 test che coprono A4.4 — FR-COM-01, SPEC §9.8.3, ADR-040). **Setup**: `InMemoryMatchRepository` reale + Mockito mocks bus/stomp + Clock fisso; il test 1 e il test 2 usano `ItalianRuleEngine` reale, il test 3 usa stubbed `RuleEngine` perché serve interleaving illegali tra WHITE e BLACK senza che alcuno faccia una mossa legale (impossibile con engine reale: ogni mossa legale flippa sideToMove). **Test 1 — `fiveConsecutiveIllegalMovesForfeitsThePlayerAndAwardsTheOpponent`**: WHITE plays 1 legal → BLACK turn; WHITE invia 5 mosse di seguito (tutte NOT_YOUR_TURN); dopo la 5ª, WHITE counter=5, match→FINISHED, eventi totali=7 (1 MoveApplied + 5 MoveRejected NOT_YOUR_TURN + 1 MatchEnded BLACK_WINS+FORFEIT_ANTI_CHEAT); BLACK counter resta 0. **Test 2 — `aLegalMoveResetsTheCounterAndAvoidsForfeit`**: WHITE plays legal → 4 NOT_YOUR_TURN (counter=4) → BLACK plays legal → WHITE plays legal (counter resetta a 0) → 4 NOT_YOUR_TURN (counter=4); match still ONGOING (≤4 < 5 threshold). **Test 3 — `eachPlayerHasItsOwnCounterAndOnlyTheOffenderForfeits`** (Mockito-stubbed engine throws IllegalMoveException su ogni applyMove): WHITE invia 4 mosse mentre è suo turno → 4 ILLEGAL_MOVE rejections (engine throws), WHITE counter=4 (BLACK counter=0); BLACK invia 4 mosse mentre è ancora WHITE turn (engine non viene mai chiamato, turn check fallisce prima) → 4 NOT_YOUR_TURN rejections, BLACK counter=4 (WHITE counter ancora 4); WHITE invia la 5ª mossa illegale → counter=5, forfait WHITE, MatchEnded BLACK_WINS+FORFEIT_ANTI_CHEAT, BLACK counter resta a 4 (sotto threshold). **Coverage**: A4.4 ora ✅ COVERED end-to-end. JaCoCo branches `applyAntiCheatCheck` ramo "≥ threshold → emit MatchEnded + status FINISHED" coperti. `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 27.3s, **118/118 test verdi** (115 + 3 nuovi), JaCoCo gate `All coverage checks have been met` (32 classi invariato vs 4.8 — solo nuovi test, no nuove prod class), SpotBugs 0 issues, Spotless OK (64 file kept clean — Spotless ha riformattato il file durante l'`apply`).

- **Task precedente**: Task 4.8 — `MatchManager @Service` (PLAN-fase-4 §4.8). Output: 3 file prod nuovi (`MatchManager.java`, `MatchNotFoundException.java`) + 1 file prod modificato (`CoreServerConfiguration.java` con `@Bean RuleEngine ruleEngine() { return new ItalianRuleEngine(); }`) + 4 file di test nuovi. **`MatchManager @Service final class`** in `com.damaitaliana.core.match`: dependency injection di `MatchRepository`, `RuleEngine` (da `shared`), `MatchEventBus`, `StompCompatiblePublisher` via ctor pubblico `@Autowired` 4-arg + ctor package-private 5-arg per visible-for-tests con `Clock` deterministico (`Clock.fixed(NOW, ZoneOffset.UTC)`). **Concurrency**: `ConcurrentHashMap<MatchId, Object> matchLocks` con `lockFor(matchId) = computeIfAbsent(id, k -> new Object())`; ogni write op (`applyMove`/`resign`/`offerDraw`/`respondDraw`) avvolta in `synchronized (lockFor(matchId))` per garantire la strict monotonic validation di `repo.appendEvent` non fallisca sotto contesa. Read op (`findById`/`eventsSince`) lock-free, delegano direttamente al repository. **Helper privati**: `emit(<E extends MatchEvent> E event)` che fa `repo.appendEvent(event) + bus.publish(event) + stompPublisher.publishToTopic("/topic/match/" + matchId, event)` (ogni evento applicato attraversa i 3 canali sempre); `nextSequenceNo(matchId) = repo.currentSequenceNo(matchId) + 1L`; `colorOf(match, who)` ritorna WHITE/BLACK o lancia `IllegalArgumentException` se non partecipante; `terminalResult(GameStatus)` e `terminalReason(GameStatus)` switch expression che mappa il GameStatus di shared (`WHITE_WINS`/`BLACK_WINS`/`DRAW_REPETITION`/`DRAW_FORTY_MOVES`/`DRAW_AGREEMENT`) ai tipi del wire core-server (`MatchResult` + `EndReason`); `applyAntiCheatCheck` valuta `match.consecutiveIllegalMoves(senderColor) >= 5` (`ILLEGAL_MOVES_FORFEIT_THRESHOLD`) e in caso emette MatchEnded(FORFEIT_ANTI_CHEAT) + `match.status(FINISHED)`. **API pubblica** (6 metodi): `createMatch(white, black, timeControl)` ritorna Match status=ONGOING (lobby management F6, F4 starts ongoing immediato) con MatchId.random + GameState.initial + sequenceNo=-1; `findById(MatchId)` delega; `eventsSince(MatchId, fromSeq)` delega; `applyMove(MatchId, sender, Move)` flow completo: lookup → colorOf (throws IllegalArg se non partecipante) → check status==ONGOING (else MoveRejected MATCH_NOT_ONGOING senza counter bump) → check sideToMove==senderColor (else MoveRejected NOT_YOUR_TURN + counter+1 + antiCheatCheck) → try ruleEngine.applyMove (catch IllegalMoveException → MoveRejected ILLEGAL_MOVE + counter+1 + antiCheatCheck) → success → MoveApplied + match.state(newState) + match.clearIllegalMoves(senderColor) + se newState.status() non ongoing → emitMatchEnded(terminalResult, terminalReason); `resign(MatchId, who)` ONGOING-only, emette Resigned poi MatchEnded(opposite winner, RESIGN), match→FINISHED; `offerDraw(MatchId, from)` ONGOING + no-pending-offer-only, emette DrawOffered + match.pendingDrawOfferFrom(from); `respondDraw(MatchId, responder, accept)` ONGOING + has-pending + responder!=offerer-only, accept→DrawAccepted+MatchEnded(DRAW, DRAW_AGREEMENT)+FINISHED, decline→DrawDeclined+match prosegue. **`MatchNotFoundException extends RuntimeException`** unchecked con serialVersionUID per i lookup falliti (F6 REST controller la mapperà a 404). **`CoreServerConfiguration`** updated con `@Bean RuleEngine ruleEngine()` (shared è dependency-pure, no Spring annotations — necessario `@Bean` factory). **4 file di test** in `com.damaitaliana.core.match` (per accesso package-private al ctor 5-arg con Clock): `MatchManagerTurnTest` (8 test, real ItalianRuleEngine + Mockito mocks bus/stomp: findById empty, createMatch persists ONGOING + initial state + clock-fixed startedAt, unknown-match→MatchNotFoundException, non-participant→IllegalArgumentException, wrong-turn→MoveRejected NOT_YOUR_TURN+counter bump+state unaffected, finished-match→MoveRejected MATCH_NOT_ONGOING senza counter bump, broadcast bus+stomp via verify, eventsSince delegate); `MatchManagerValidationTest` (6 test, mix real engine + Mockito-stubbed engine: legal move flips sideToMove, legal-move-resets-counter pattern, illegal-move-replay-from-empty-square→MoveRejected ILLEGAL_MOVE, terminal-WHITE_WINS via stubbed engine→MatchEnded WHITE_WINS+CHECKMATE_LIKE+FINISHED, terminal-DRAW_REPETITION via stubbed engine→MatchEnded DRAW+DRAW_REPETITION, IllegalMoveException da engine→MoveRejected ILLEGAL_MOVE+counter); `ResignFlowTest` (6 test: white resign→MatchEnded BLACK_WINS+RESIGN, black resign→MatchEnded WHITE_WINS+RESIGN, transitions to FINISHED, finished-match-resign→IllegalStateException, non-participant→IllegalArgumentException, unknown-match→MatchNotFoundException); `DrawFlowTest` (6 test, copre A4.8 4 scenari + 2 edge: offer→DrawOffered+pending state, accept→DrawAccepted+MatchEnded DRAW+DRAW_AGREEMENT+FINISHED+pending cleared, decline→DrawDeclined+ONGOING+pending cleared+re-offer ok, doppia offer pending→IllegalStateException, responder==offerer→IllegalStateException "cannot respond to their own draw offer", no pending→IllegalStateException). **Issue Spring DI risolto**: `MatchManager` ha 2 ctor (4-arg public, 5-arg package-private per Clock). Spring 6+ non sa quale scegliere ("No default constructor found"); soluzione: `@Autowired` esplicito sul ctor 4-arg pubblico per disambiguare. Alternative considerate (single ctor con default Clock, ctor 5-arg pubblico) scartate per cleanness. `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 26.8s, **115/115 test verdi** (89 + 26 nuovi: 8 turn + 6 validation + 6 resign + 6 draw), JaCoCo gate `All coverage checks have been met` (32 classi analizzate vs 30 di Task 4.7, +2: `MatchManager` + `MatchNotFoundException`), SpotBugs 0 issues, Spotless OK (63 file kept clean — Spotless ha riformattato i 6 file nuovi durante l'`apply` per line-wrap Javadoc 100 col Google Java Style).

- **Task precedente**: Task 4.7 — `Match` state machine + anti-cheat counter (PLAN-fase-4 §4.7). Output: `Match.java` esteso (1 file modificato: +1 import `Color`, +1 import `EnumMap`, +1 field, +5 metodi, +1 helper privato, Javadoc class-level aggiornato per riflettere lo stato post-4.7) + `MatchStateMachineTest.java` nuovo (1 file di test, 26 test in 2 nested class). **State-machine validation**: `void status(MatchStatus newStatus)` package-private setter ora valida la transizione contro un set chiuso `WAITING→ONGOING` / `ONGOING→FINISHED` / `ONGOING→ABORTED`; transizioni invalide (terminali FINISHED/ABORTED → qualsiasi, regressioni `ONGOING→WAITING`, salti `WAITING→FINISHED|ABORTED`, identity `X→X`) lanciano `IllegalStateException` con message `"invalid match status transition: FROM -> TO"` e NON modificano lo state (asserito dai test: dopo throw, `match.status()` resta invariato). Helper `private static boolean isLegalTransition(MatchStatus from, MatchStatus to)` con `switch expression` + `case FINISHED, ABORTED -> false` (sealed-style esaustivo sull'enum). **Anti-cheat counter** (FR-COM-01, SPEC §9.8.3, ADR-040 a Task 4.13): nuovo field `private final EnumMap<Color, Integer> consecutiveIllegalMoves` inizializzato in ctor con `{WHITE: 0, BLACK: 0}`. Public read accessor `int consecutiveIllegalMoves(Color who)` (null guard, ritorna value diretto dall'EnumMap) per tests + future UI display F6+. Package-private mutators `void recordIllegalMove(Color who)` (`merge(who, 1, Integer::sum)`) e `void clearIllegalMoves(Color who)` (`put(who, 0)`) — saranno chiamati dal `MatchManager` di Task 4.8 sui flussi di MoveRejected/MoveApplied. Counter held in-memory only (transient, non serializzato, non parte del wire shape) — recovery F6 reconnection resetta il counter a zero per scelta benevola (ADR-040). **Class Javadoc aggiornato**: rimossa frase stale "the state-machine wiring stays in Task 4.8 — Match itself is intentionally a thin data carrier with structural validation only", sostituita con spiegazione che (i) il data shape è stato anticipato a 4.3, (ii) state-machine validation + anti-cheat counter sono stati aggiunti in 4.7, (iii) il "wiring" (decisione su quando chiamare quale mutator) resta a `MatchManager` 4.8. **`MatchStateMachineTest`** in `com.damaitaliana.core.match` (per accedere ai mutator package-private): `@Nested StatusTransitions` (10 metodi @Test, di cui 3 `@ParameterizedTest @EnumSource(MatchStatus.class)` per terminale-FINISHED → 4 cases, terminale-ABORTED → 4 cases, identity → 4 cases — totale 19 esecuzioni); `@Nested AntiCheatCounter` (7 test). Helper `newMatchWithStatus(MatchStatus)` costruisce `Match` via il ctor 10-arg con `GameState(Board.initial(), Color.WHITE, 0, List.of(), GameStatus.ONGOING)` — `null` per tournamentRef e pendingDrawOfferFrom, `-1L` per currentSequenceNo. **Backward compat**: nessuna modifica al ctor di `Match` (l'EnumMap è inizializzato all'interno, non passato come arg), quindi `MatchRepositoryContractTest`/`MatchValueTypesTest` continuano a usare lo stesso ctor signature 10-arg senza modifiche. `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 20.8s, **89/89 test verdi** (63 + 26 nuovi), JaCoCo gate `All coverage checks have been met` (30 classi analizzate vs 30 di Task 4.6: invariato — Match era già nel bundle, l'aggiunta di metodi non cambia il count, il coverage interno aumenta), SpotBugs 0 issues, Spotless OK (57 file kept clean — Spotless ha riformattato Match.java durante l'`apply`).

- **Task precedente**: Task 4.6 — STOMP-compatible publisher (PLAN-fase-4 §4.6). Output: 2 file prod nuovi in `com.damaitaliana.core.stomp` + 2 file test (1 helper test-scope + 1 nuovo test class) + 1 dep aggiunta a `core-server/pom.xml`. **`StompCompatiblePublisher`** interface single-method `void publishToTopic(String topic, Object payload)` — Javadoc documenta il contratto "given a topic and a payload, deliver to whoever is subscribed on the transport"; core-server NON sa nulla di WebSocket; specifica throw `NullPointerException` per null args (validation up-front per fail-fast nei test) e che le impl reali F6/F7 NON DEVONO throw su transport failure (broadcast best-effort, l'autoritativo è già persistito via MatchRepository). **`LoggingStompPublisher @Component`** `final class` SLF4J: `private static final Logger LOGGER = LoggerFactory.getLogger(LoggingStompPublisher.class)`, `publishToTopic` valida null + `LOGGER.info("[stomp] {} <- {}", topic, payload)` (parameterized SLF4J, no String.format se INFO disabled). **`BufferingStompPublisher`** in `core-server/src/test/java/com/damaitaliana/core/stomp/` (test-scope, NOT in production jar) `final class` con record nested `Published(String topic, Object payload)` con null validation + `Collections.synchronizedList<Published> buffer`; `publishToTopic` add atomico (synchronizedList lock interno); `published()` ritorna `List.copyOf(buffer)` sotto `synchronized(buffer)` block esplicito (iteration safety per JavaDoc Collections.synchronizedList); `clear()` per fixture reset. **2 test classes** + 9 test totali: `LoggingStompPublisherTest` (3 test: smoke 3-call no-throw, null topic, null payload; log output NOT asserted — capturing SLF4J output richiederebbe nuova test dep tipo logback-test che è anti-pattern #13), `BufferingStompPublisherTest` (6 test: FIFO collect, freshly-empty, clear semantics, snapshot immutability vs further writes, Published record null-topic + null-payload guards). **2 deviazioni documentate vs PLAN-fase-4 §4.6**: (1) **`@ConditionalOnMissingBean` non usato** — vive in `spring-boot-autoconfigure`, aggiungerla violerebbe CLAUDE.md §8.7-8.8 (transport-agnostic invariant) e anti-pattern #13 (dep fuori SPEC §6). Soluzione: plain `@Component`; F6 (server) e F7 (LAN host) marcheranno il loro `WebSocketStompPublisher @Component @Primary` per prendere precedenza nell'autowiring quando entrambi i bean coesistono. (2) **Aggiunto `slf4j-api` come dep produzione** a `core-server/pom.xml` (no version tag — gestita dal Spring Boot BOM del parent, già menzionato in commento "manages SLF4J, Logback, ..."): `slf4j-api` è esplicitamente autorizzato da SPEC §6 line 330 (`Logging | SLF4J + Logback`) e mandato da CLAUDE.md §4.1 ("Solo SLF4J") — non è una "nuova dep outside SPEC §6", è enabling di una scelta già approvata che non era ancora attivata in core-server perché nessuno dei task precedenti aveva bisogno di logging. La binding (logback-classic) NON è inclusa: i moduli runtime (server F6, client F7) la portano via le loro `spring-boot-starter` deps. `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 27.7s, **63/63 test verdi** (54 + 9 nuovi), JaCoCo gate `All coverage checks have been met` (30 classi analizzate vs 29 di Task 4.5, +1: `LoggingStompPublisher`; l'interface `StompCompatiblePublisher` è bytecode-zero), SpotBugs 0 issues, Spotless OK (56 file kept clean — Spotless ha riformattato i 4 nuovi file prod/test durante l'`apply`).

- **Task precedente**: Task 4.5 — Internal event bus su Spring `ApplicationEventPublisher` (PLAN-fase-4 §4.5). Output: 3 file prod nuovi in `com.damaitaliana.core.eventbus` + 1 file di test. **`MatchEventBus`** interface (`void publish(MatchEvent event)`): semantica FIFO per publisher thread (Spring default multicaster sincrono — `publish` ritorna solo dopo che tutti i listener hanno processato l'evento), per-match FIFO segue per costruzione perché il `MatchManager` di Task 4.8 serializza le scritture per match. **`MatchEventEnvelope`** `final class extends ApplicationEvent` con `private static final long serialVersionUID = 1L` + `private final transient MatchEvent payload` (`transient` documenta l'intent: l'envelope NON è serializzato — il bus è in-process, e silenzia SpotBugs `SE_BAD_FIELD`; il wire layer F6/F7 serializza il `MatchEvent` payload via Jackson, non l'envelope Spring); ctor `(Object source, MatchEvent payload)` con `Objects.requireNonNull(payload)`; getter `payload()`. **`SpringMatchEventBus`** `final class @Component implements MatchEventBus`, ctor injection di `ApplicationEventPublisher` (auto-injected con la `ApplicationContext` stessa che lo implementa); `publish` valida null-event poi `publisher.publishEvent(new MatchEventEnvelope(this, event))`. **`SpringMatchEventBusTest`** (4 test) usa `AnnotationConfigApplicationContext` diretto via try-with-resources + helper `newContext()` che `register(CoreServerConfiguration.class)` + `register(CollectingListener.class)` + `refresh()`. Listener bean `CollectingListener static final class @Component` con `@EventListener void on(MatchEventEnvelope env)` e `received` come `synchronizedList`. Test: (1) `eventListenerReceivesAllEventsInPublishOrder` — 4 eventi pubblicati, listener.received().containsExactly nello stesso ordine; (2) `publishingFromSingleThreadDeliversFifoPerMatch` — eventi interleaved di 2 match, filter per matchId verifica per-match FIFO; (3) `busBeanIsTheSpringImplementation` — `getBean(MatchEventBus.class)` è instance di `SpringMatchEventBus`; (4) `publishingNullEventThrowsNullPointerException` — copre il `Objects.requireNonNull(event)` guard. **Deviazione documentata vs PLAN-fase-4 §4.5**: PLAN richiedeva `@SpringJUnitConfig(CoreServerConfiguration.class)`, che vive in `spring-test` artifact. `spring-test` NON è dep diretta di `core-server` né lo era in F1-F4.4 — aggiungerla è "nuova dipendenza" per CLAUDE.md §8 anti-pattern #13 (richiede approvazione esplicita). Soluzione: stesso pattern di `CoreServerConfigurationTest` (Task 4.1), `AnnotationConfigApplicationContext` diretto. Funzionalmente equivalente per A4.9 (event multicast + FIFO check). Nessun cambio a `core-server/pom.xml`. `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 17.1s, **54/54 test verdi** (50 + 4 nuovi), JaCoCo gate `All coverage checks have been met` (29 classi analizzate vs 27 di Task 4.4, +2: envelope + SpringMatchEventBus; l'interface `MatchEventBus` è bytecode-zero), SpotBugs 0 issues, Spotless OK (51 file kept clean — Spotless ha riformattato 4 nuovi file Javadoc).

- **Task precedente**: Task 4.4 — In-memory adapters per i repository ports (PLAN-fase-4 §4.4). Output: 3 file prod nuovi in `com.damaitaliana.core.repository.inmemory` + 1 file di test in `com.damaitaliana.core.repository`. **`InMemoryMatchRepository @Component`** (`final class`): `ConcurrentHashMap<MatchId, Match>` per snapshot (save = put; findById = Optional.ofNullable; findByStatus + findByPlayer = iterazione su `snapshots.values()` con filtro su `m.status() == status` e `m.white().equals(user) || m.black().equals(user)` rispettivamente, ritorno `List.copyOf` per immutability); `ConcurrentHashMap<MatchId, List<MatchEvent>>` con `Collections.synchronizedList(new ArrayList<>())` per event log append-only — per ogni `appendEvent` la list è acquisita con `computeIfAbsent` e validation+append eseguiti sotto `synchronized (log)` block esplicito così che strict monotonic check (FR-COM-04, SPEC §7.5) e append siano atomici sotto contesa: `expected = log.size()`, se `event.sequenceNo() != expected` → `IllegalStateException` con message che cita matchId + expected + actual; altrimenti `log.add(event)`. `currentSequenceNo` ritorna `-1L` se match unknown o log vuoto, `(long) log.size() - 1L` altrimenti (sotto lock). `eventsSince(matchId, fromSeq)` ritorna `List.of()` per match unknown, altrimenti sotto lock costruisce ArrayList col suffix dove `e.sequenceNo() > fromSeq` e ritorna `List.copyOf(suffix)`. **`InMemoryTournamentRepository @Component`**: stesso pattern, solo CRUD su `ConcurrentHashMap<TournamentId, Tournament>` (no event log). **`InMemoryUserRepository @Component`**: due `ConcurrentHashMap` (`Long → UserRef` id-indexed e `String → UserRef` username-indexed); helper `register(UserRef)` popola entrambi gli indici (gli utenti anonimi LAN — `id == ANONYMOUS_LAN_ID` — vengono indicizzati solo per username, l'id-side resta vuoto); `findById(long)` early-return `Optional.empty()` per `UserRef.ANONYMOUS_LAN_ID` (-1L) per documentazione del port (SPEC §11.1: anonymous LAN users not addressable by id), `findByUsername` semplice lookup. **`InMemoryMatchRepositoryTest`** (`final class` package-private nel package `com.damaitaliana.core.repository` per accedere all'abstract `MatchRepositoryContractTest` package-private): override `createRepository()` ritorna `new InMemoryMatchRepository()`. Attiva i **13 contract test** scritti in 4.3 (PLAN-fase-4 §4.3 li stimava 12 — il count reale è 13: l'extra è `currentSequenceNoIsMinusOneForUnknownMatch`). **Test count modulo: 37 → 50** (37 + 13 contract activations). `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 20.8s, **50/50 test verdi**, JaCoCo gate `All coverage checks have been met` (27 classi analizzate vs 24 di Task 4.3, +3 prod adapter), SpotBugs 0 issues, Spotless OK (47 file kept clean — niente reformat necessario). **Riusabilità contract test in F6**: il `MatchRepositoryContractTest` resta abstract; il futuro `JpaMatchRepositoryTest` (modulo `server`) estenderà la stessa classe con `@Transactional` boundary + `createRepository()` su impl JPA → stesse 13 asserzioni contro lo stesso contratto su due adapter eterogenei.

- **Task precedente**: Task 4.3 — Repository ports + `Match` data class anticipato + `Tournament` sealed shape (PLAN-fase-4 §4.3). Output: 7 file prod nuovi + 1 file di test. **Anticipo `Match`**: il PLAN posiziona la classe `Match` in Task 4.7 (state machine), ma `MatchRepository.save(Match)` (Task 4.3) la richiede come typed argument — anticipata come **mutable data class** con scope ridotto (campi `MatchId id`, `UserRef white/black`, `TimeControl`, `Instant startedAt`, nullable `TournamentMatchRef`, mutable `GameState`, `long currentSequenceNo` ≥-1, `MatchStatus`, nullable `UserRef pendingDrawOfferFrom`; getters public + setters package-private per `MatchManager` Task 4.8 + `Optional` accessors per nullable). Anti-cheat counter, transitions, evento wiring restano a Task 4.8. **`Tournament` sealed** con permits `EliminationTournament`/`RoundRobinTournament` (records: id, name, status, participants, timeControl) — bracket gen + scheduling + standings + tie-breaker deferred F8/F9. **3 ports** in `com.damaitaliana.core.repository` (plain Java, no JPA per CLAUDE.md §8.8): `MatchRepository` con 7 metodi (save, findById, findByStatus, findByPlayer, appendEvent con strict monotonicity validation `IllegalStateException` su seq != currentSeq+1, eventsSince suffix, currentSequenceNo); `TournamentRepository` skeleton (save/findById/findByStatus); `UserRepository` skeleton (findById long, findByUsername). Per-match write serialization = caller's responsibility (`MatchManager` userà lock per match). **`MatchRepositoryContractTest`** abstract package-private (JUnit 5 skip auto), 12 test che coprono il contratto (save+findById roundtrip, save overwrites, findByStatus empty/multi, findByPlayer white|black|none, currentSequenceNo `-1` baseline + post-event monotonic, appendEvent rejects non-monotonic + first must be `0` + counter unchanged after rejected append, eventsSince full|partial|empty + empty for unknown match). Concrete subclasses fornisce `createRepository()`: Task 4.4 in-memory, F6 JPA — stesso file, due adapter testati identicamente. **Bidirectional package dep `match` ↔ `tournament`** documentata in `Tournament.java` Javadoc (Match importa TournamentMatchRef, Tournament importa UserRef + TimeControl) — reviewable in REVIEW Fase 4 / Task 4.12 (ArchUnit). `mvn -pl core-server verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 5:53 min, **37/37 test verdi** (invariato vs 4.2 — i contract test sono skipped in attesa di Task 4.4), JaCoCo gate ✅ (24 classes analizzate vs 21), SpotBugs 0, Spotless OK.

- **Task precedente**: Task 4.2 — Domain value types + MatchEvent sealed hierarchy (PLAN-fase-4 §4.2). Vedi descrizione completa nello "Stato corrente" più sopra. Sintesi: 21 classi prod nuove + 4 file di test (37 test totali, +36 vs 4.1), value types in `match` (8 file: MatchId, UserRef, TimeControl, TimeControlPreset, MatchStatus, MatchResult, EndReason, RejectionReason) + value types in `tournament` (3 file: TournamentId, TournamentMatchRef, TournamentStatus) + MatchEvent sealed con 9 permits in `match.event` (10 file). Test deps Jackson aggiunte in `core-server/pom.xml` con `<scope>test</scope>` (production code NON dipende da Jackson, rispetta CLAUDE.md §8.8). BUILD SUCCESS in 5:22 min.

- **Task precedente**: Task 4.1 — Module setup + sub-packages + remove smoke test (PLAN-fase-4 §4.1).

- **Task precedente di Fase 3.5 (chiusura fase)**: Task 3.5.17 — TEST sotto-fase Fase 3.5 (mini-fase pull-forward da F11 per **demo cliente**, chiusa). PLAN approvato (commit `b6a2051`), SPEC v2.2 applicata (commit `fc7b68c` con CR-F3.5-001..005). Tag F3 `v0.3.0` confermato il 2026-04-30 con TEST sotto-fase deferred → debito **estinto come Task 3.5.0** il 2026-04-30 (regression `mvn clean verify` root con `slow`+`performance` BUILD SUCCESS in 16:25 min, 673/673 verdi; `tests/TEST-PLAN-fase-3.md` finalizzato §1-§10 esclusa la voce manuale §7.4 deliberatamente lasciata aperta). Contributor cleanup option A eseguito il 2026-04-30 (history rewrite + force-push, trailer Co-Authored-By rimosso da tutti i commit + tag).
- **Task precedente Fase 3.5 (closure dettaglio)**: Task 3.5.17 — TEST sotto-fase Fase 3.5 (PLAN-fase-3.5 §3.5.17). Output: [`tests/TEST-PLAN-fase-3.5.md`](tests/TEST-PLAN-fase-3.5.md) finalizzato §1-§8. Regression finale `mvn clean verify` (root, **no `excludedGroups`** — `slow`+`performance` inclusi) eseguita 2026-05-02 01:09 CET sul HEAD post-REVIEW: **BUILD SUCCESS in 16:09 min**, **714 test verdi totali** (shared 391 con slow+perf in 14:53 min dominato da gating IA F2 `AiTournamentSimulationTest` ≥95/100, core-server 1 smoke, client 321 in 52.9s, server 1 smoke). JaCoCo gate verde su tutti i moduli, SpotBugs 0 High, Spotless OK. **Closure check 8/8 spuntate**: coverage target raggiunti, TRACEABILITY aggiornata, corpus regole italiane invariato, regression slow+perf BUILD SUCCESS, plan §1-§8 popolato, §7 manual demo PASS, 0 `@Disabled`/`@Ignore` (grep client/src/test conferma 0 match), entrambi i findings REVIEW (F-001, F-002) RESOLVED. **A3.5.20 ✅ COVERED**.

- **Task precedente**: Task 3.5.16 — REVIEW Fase 3.5 (PLAN-fase-3.5 §3.5.16). Output: [`reviews/REVIEW-fase-3.5.md`](reviews/REVIEW-fase-3.5.md). Audit code-by-code di 12 aree critiche (`JavaFxAudioService` lifecycle/threading, `ParticleEffects` cleanup, `MoveAnimator` easing, `PieceNode` 3D-look composition, `SaveDialogController` post-fix, `client/pom.xml` profilo installer, `theme-light.css` contrast WCAG, `CREDITS.md` audit asset, `tests/visual-review/` baseline, `AI_CONTEXT.md` coerenza, anti-pattern scan TODO/FIXME/println, build-tool scope test) — tutto verde. **2 findings** totali, **entrambi RESOLVED**: **F-001** [REQUIREMENT_GAP, High] save-dialog non wood-themed (chiuso in Task 3.5.14a, commit `19e5227`); **F-002** [DOC_GAP, Low] custom Interpolator easing OUT_BACK in `MoveAnimator` non spiegava la deviazione dal PLAN-fase-3.5 §3.5.8 (che citava `Interpolator.SPLINE(0.34, 1.56, 0.64, 1)`) — fix opzione A scelta dall'utente, Javadoc note aggiunta che cita PLAN + limitazione JavaFX 21 SPLINE (`y ∈ [0,1]` proibisce overshoot `y > 1`). Spotless OK post-edit. Coverage AC SPEC §17 + AC operativi A3.5.1..21 + FR-SP-01..09 + FR-RUL-01..05 + NFR-P/U/M tutti documentati nel file review. Closure check 7/7 spuntate. Stop point §10 utente in-thread chiuso 2026-05-02.

- **Task precedente**: Task 3.5.15 — Documentation + ADR + CHANGELOG + TRACEABILITY (PLAN-fase-3.5 §3.5.15). Nessun codice prod toccato (per design del piano: solo documentazione). 4 file modificati: (1) **`ARCHITECTURE.md`** — appendiati 4 nuovi ADR. **ADR-034** "Visual rework videogame premium wood (Fase 3.5)" documenta il rationale del riscrivere `theme-light.css` con vocabolario duale `bg-*/text-on-*`, tipografia bi-classe Playfair display + Inter UI, classi pulsante primary/secondary, texture Poly Haven applicata via `BackgroundImage`, pezzi composti `Group` con gradient/ring/gloss/marker, e single-source-of-truth `ThemeService` (tracing del finding F-001 / Task 3.5.14a). **ADR-035** "Architettura `AudioService` (single MediaPlayer music + per-cue SFX cache)" documenta interface testabile + impl `JavaFxAudioService`, threading runOnFxThread con catch IllegalStateException, persistence via UserPreferences v2, JaCoCo exclusion sull'impl, pipeline OGG-master/WAV-runtime via `OggToWavConverter`. **ADR-036** "Packaging Windows MSI via jpackage + jpackage-maven-plugin" documenta pipeline opt-in profilo `installer`, prerequisito WiX 3.x, `--win-upgrade-uuid` stabile per upgrade in-place, fallback `APP_IMAGE`, `AppIconGenerator` test-scope pattern. **ADR-037** "Asset licensing strategy: CC0 visual+audio, OFL fonts, audit CREDITS.md" documenta doppia regola CC0/OFL, audit trail per ogni asset, esclusioni esplicite (Patreon/404/sito chiuso/CC-BY), eccezione SPEC §6 per LGPL build-time tooling (`jorbis`+`vorbisspi` in `OggToWavConverter`). (2) **`CHANGELOG.md`** — `[Unreleased]` arricchito: nuova entry sintetica per Task 3.5.15 (corrente), Task 3.5.14 (manual demo), Task 3.5.14a (save-dialog wood-theme fix in `### Fixed`), e voci compatte 2-4 righe per ognuno dei task 3.5.6-3.5.13 (BoardCellNode redesign, PieceNode 3D-look, animation polish, splash, main menu, 5 schermate restanti, jpackage MSI, audit FXML smoke 0 fix). (3) **`README.md`** — sezione "Build installer Windows (Fase 3.5+)" già completa (Task 3.5.12), aggiunta nota dedicata "Demo cliente Win 10/11" che documenta il flusso target (doppio-click `.msi` → install wizard standard → Start menu shortcut → no prerequisito Java → upgrade in-place auto via UUID stabile). (4) **`tests/TRACEABILITY.md`** — nuova sezione "Acceptance criteria di Fase 3.5 (PLAN-fase-3.5.md §2.2)" con mapping completo A3.5.1..A3.5.21 → test class/method o output di build. Inserita PRIMA della sezione F3 esistente (ordine reverse cronologico). FR/NFR esistenti invariati (rework solo visual, niente nuovi FR coperti). **Nessun nuovo test, nessun cambio coverage, niente verify run** (Task 3.5.15 è puramente documentale).

- **Task precedente**: Task 3.5.14 — Manual demo run + visual review (PLAN-fase-3.5 §3.5.14). MSI buildato 2 volte (`PATH+="WiX 3.14/bin"` workaround per WiX appena installato non sul PATH di sistema, ~52s ciascuna build, output `client/target/jpackage/Dama Italiana-0.3.5.msi` 152 MB con stesso `--win-upgrade-uuid` per upgrade in-place). Installazione su Win 11 OK (shortcut Start menu, JRE bundled, no prerequisiti Java). 7/8 screenshot catturati in `tests/visual-review/` (splash skipped: durata ~1.5s non screenshot-able, look verificato a occhio — non bloccante). **Finding emerso**: F-001 [REQUIREMENT_GAP, High] save-dialog non skinned wood — `SaveDialogController` creava `Stage` modale + `new Scene(root)` senza chiamare `ThemeService.applyTheme(scene)`, gli stylesheet `theme-light.css`+`components.css` non venivano caricati nella scene del dialog. **Fix Task 3.5.14a (commit `19e5227`)**: iniettata dipendenza `ThemeService` nel costruttore @Autowired (ora 4-arg) + visible-for-tests (5-arg), chiamata `applyTheme(scene)` dopo `new Scene(root)`. Test aggiornati: `SaveDialogControllerTest` + `FxmlLoadingSmokeTest` mockano `ThemeService`; `SaveDialogSpringContextTest` invariato (autowiring risolve `ThemeService` come `@Component` esistente). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **321 test totali** (invariato vs 3.5.13), JaCoCo client gate ✅, SpotBugs 0, Spotless OK. MSI ricostruito post-fix; ri-screenshot save-dialog confermato wood-themed dall'utente. Gameplay vs IA Esperto: tutti A3.5.x verdi (audio MOVE/CAPTURE/PROMOTION/VICTORY, easing OUT_BACK overshoot, particle splash su cattura, raggi dorati su promozione, glow halo cattura obbligatoria, music shuffle 30%, settings sliders volume musica + effetti reagiscono in tempo reale). `tests/TEST-PLAN-fase-3.5.md §7` finalizzato esito **PASS** (commit `9432dbb` con i 7 PNG baseline). **A3.5.4 PASS post-fix**, A3.5.5-12+15 PASS direttamente. Status: F-001 da tracciare in `reviews/REVIEW-fase-3.5.md` come RESOLVED.

- **Task precedente**: Task 3.5.13 — Test fixes per breakage FXML/layout (PLAN-fase-3.5 §3.5.13). **Risultato: 0 fix necessari** — tutti i `*FxmlSmokeTest` sono passati senza modifiche dopo le rework FXML di Task 3.5.6 / 3.5.9 / 3.5.10 / 3.5.11. **Audit dei 4 file che caricano FXML in test**: `RulesFxmlSmokeTest` (3 test, lookup `.rule-diagram` styleClass + `currentSection==SETUP` default + `selectSection(NOTATION)` → CSS class preservata in 3.5.11, controller state invariato), `RulesScreenE2ETest` (1 test, itera `RuleSection.ALL` e asserta primo `contentChildren` è `Label` con text == localized title — il test verifica solo il TIPO Label, non lo styleClass `.label-display` cambiato in 3.5.11), `SettingsFxmlSmokeTest` (1 test, solo `loader.load()` non-null — robust by design), `FxmlLoadingSmokeTest` (3 test, `save-dialog/board-view/load-screen` solo `loader.load()` non-null — `board-view.fxml` aveva ricevuto in 3.5.6 il wrapper `StackPane.board-frame` ma il test non asserta sulla struttura). **Deviazione vs PLAN**: il PLAN stimava "5-15 test da aggiustare". Il valore reale è **0**. Motivo: i test FXML smoke sono stati scritti fin dall'inizio (Fase 3, Task 3.18 + 3.21) usando lookup by styleClass / fx:id e asserzioni sullo stato del controller — pattern intenzionalmente robusto a refactor visivi. Le rework di Fase 3.5 hanno aggiunto/cambiato styleClass ma non rimosso quelli su cui i test fanno lookup (`.rule-diagram` preservato, `currentSection` semantica invariata, etc.). **Verifica eseguita**: (1) `mvn -pl client verify` (full suite, no `excludedGroups`) → BUILD SUCCESS, **322 test totali** (321 fast + 1 slow/perf marker); (2) `mvn clean verify` root → BUILD SUCCESS in 19:48 min (parent SUCCESS, shared 18:30 min SUCCESS — corpus parametrizzato F1 + gating IA F2, core-server 8s SUCCESS, client 54s SUCCESS, server 10s SUCCESS). Nessuna regression cross-module rispetto al baseline F3 (i moduli `shared` / `core-server` / `server` non sono stati toccati dalla F3.5 — invariato per costruzione).

- **Task precedente**: Task 3.5.12 — jpackage Windows MSI installer (PLAN-fase-3.5 §3.5.12). Profilo Maven `installer` opt-in attivato con `mvn -pl client -Pinstaller -DskipTests package`. Pipeline: (1) `maven-dependency-plugin:copy-dependencies@jpackage-stage-runtime-deps` copia 89 jar runtime in `client/target/jpackage-input/`; (2) `maven-resources-plugin:copy-resources@jpackage-stage-self-jar` aggiunge `client-0.1.0-SNAPSHOT.jar` come `--main-jar`; (3) `org.panteleyev:jpackage-maven-plugin:1.6.5` invoca `jpackage --type MSI --name "Dama Italiana" --app-version 0.3.5 --vendor "Dama Italiana" --main-class com.damaitaliana.client.ClientApplication --icon <ico> --win-shortcut --win-menu --win-menu-group "Dama Italiana" --win-dir-chooser --win-upgrade-uuid 9d8c4a02-3f1b-4f7e-9c2a-5a6e1b3c8d92` (UUID stabile per upgrade installs cleanly). **Prerequisito WiX Toolset 3.x non disponibile sulla build machine** — la verifica end-to-end MSI è deferred a Task 3.5.14 dopo installazione WiX. Smoke-test eseguito qui: dep-staging produce 89 jar in `target/jpackage-input/` + il client jar — config validata. Fallback `-Djpackage.type=APP_IMAGE` documentato nel README per build portable senza WiX. **Icona**: nuovo `AppIconGenerator` in `client/src/test/java/.../buildtools/` (test-scope, fuori dal jar di prod, stesso pattern di `OggToWavConverter`) — main Java standalone che disegna disco wood-themed (gradient walnut + rim gold + corona Path2D 5-punte + ball tips + gem cream) e scrive `app-icon.ico` multi-size 16/32/48/64/128/256 px con payload PNG (formato PNG-in-ICO, supportato da Vista+, copre SPEC §1 Win 10/11). Eseguito una volta via `mvn -pl client exec:java -Dexec.classpathScope=test -Dexec.mainClass="com.damaitaliana.client.buildtools.AppIconGenerator" -Dexec.args="..."`, output `client/src/main/resources/assets/icons/app-icon.ico` (38 KB) committato. **README** sezione "Build installer Windows (Fase 3.5+)" aggiunta tra "Storage utente / Toggle lingua" e "Headless / dev mode": prerequisiti (JDK 21 + WiX 3.x + nota incompatibilità WiX 4), comando MSI, output path, fallback APP_IMAGE, smoke-test isolato dep-staging. **Deferred (review-cycle follow-up)**: passare un `--runtime-image` jlink custom per ridurre JRE bundled da ~300 MB a ~80 MB; Spring Boot non-modular richiederebbe `--ignore-missing-deps`. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **321 test totali** (invariato vs 3.5.11), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.11 — Restyling coordinato 5 schermate restanti (PLAN-fase-3.5 §3.5.11). Reskin uniforme di SP setup, Save dialog, Load screen, Settings, Rules con i token v2 già definiti. **Niente nuovi token CSS** se non due piccole aggiunte: una classe `.screen-root` per il backdrop wood e una regola context-scoped per il fill dei display label dentro le card. **Deviazione documentata dal PLAN**: il PLAN diceva "body serif italics per quote regole" — interpretazione conservativa: i body paragraphs delle rules sezioni sono testi esplicativi multi-paragrafo (non quote), trasformarli in Playfair italic ridurrebbe la leggibilità (Playfair Display è decorativo, ottimizzato per titoli). Solo il section heading passa al display serif gold; i paragrafi restano Inter. (1) **`theme-light.css`** sezione "SCREEN ROOTS (Task 3.5.11)" aggiunta in coda: `.screen-root` `-fx-background-image: url("/assets/textures/board_dark.jpg")` + `-fx-background-size: cover` (riusa texture dark wood — coerenza con `.main-menu-root` di Task 3.5.10 e `.board-cell-dark` di Task 3.5.6); regola context-scoped `.card .label-display, .card-elevated .label-display, .popover .label-display { -fx-text-fill: -color-text-on-light }` — i display label (default gold via `.label-display` di tokens v2) avrebbero contrasto ~1.5:1 sul cream parchment delle card, fail WCAG AA secco; questa regola flip il fill a dark-roast quando il display label è dentro card/card-elevated/popover, mentre fuori (es. `.label-display` direttamente sul wood backdrop di una BorderPane top) la classe gold standard resta valida (gold su wood scuro contrasto >9:1). (2) **5 fxml updates**: `sp-setup.fxml` (StackPane root → `styleClass="screen-root"`, titleLabel da `label-title` a `label-display, label-display-md`, confirmButton aggiunge `styleClass="button-primary"`); `save-dialog.fxml` (titleLabel da `label-title` a `label-display, label-display-md`, confirmButton aggiunge `button-primary` — niente screen-root perché è VBox modale shown over parent screen); `load-screen.fxml` (BorderPane root → `screen-root`, titleLabel display, loadButton primary); `settings.fxml` (StackPane root → `screen-root`, titleLabel display, saveButton primary); `rules.fxml` (BorderPane root → `screen-root`, titleLabel display). (3) **`RulesController.renderSection`** — il section heading `Label header` ora ha `getStyleClass().addAll("label-display", "label-display-md")` invece di `add("label-subtitle")`. Il body paragraph rendering è invariato (Inter default). Il context override su `.card-elevated .label-display` (regola CSS sopra) intercetta automaticamente il header dentro lo `.card-elevated` ScrollPane → fill dark-roast su cream parchment. **Nessun nuovo test, nessun test rotto**: la lookup `.rule-diagram` di `RulesFxmlSmokeTest.java:62/106` è preservata (i diagram nodes mantengono lo styleClass), e nessun test pinning dipende da `label-subtitle` come heading style; i 321 test continuano a passare. **CTAs primary highlight**: 4 button principali (`confirmButton` x2, `loadButton`, `saveButton`) ora indossano la classe `.button-primary` (gradient gold + bevel + glow on hover, foreground dark-roast per WCAG AA ~9:1 sul gold) — in F3 erano default `.button` (deep walnut neutro). Il contrasto visivo guida l'utente verso l'azione principale di ogni schermata. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **321 test totali** (invariato vs 3.5.10), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.10 — Main menu restyling (PLAN-fase-3.5 §3.5.10). Reskin coordinato della 6-card grid (in realtà 5 card: SP, LAN, Online, Rules, Settings — la PLAN diceva "6-card" come approssimazione, count attuale invariato dalla F3). **Layout/structure preservata** (GridPane 2 col × 3 row, hgap/vgap 16, padding 32, percentWidth 50/50, columnSpan 2 sull'ultima card Settings). (1) **`main-menu.fxml`** — `GridPane` ora ha `styleClass="main-menu-root"`; ogni `VBox` card aggiunge `main-menu-card` accanto a `card-elevated` (multi-class CSS via comma-list nel `styleClass` attr, formato già validato dalla splash di Task 3.5.9); le 5 `Label` titolo (`singlePlayerTitle`/`lanTitle`/`onlineTitle`/`rulesTitle`/`settingsTitle`) cambiano da `styleClass="label-subtitle"` a `styleClass="main-menu-card-title"`. **Niente cambi al controller** (`MainMenuController` invariato — i fx:id e gli onAction sono identici, i 5+ test esistenti `MainMenuControllerTest` non toccano FXML structure). (2) **`theme-light.css`** sezione "MAIN MENU (Task 3.5.10)" aggiunta in coda al file: `.main-menu-root` `-fx-background-image: url("/assets/textures/board_dark.jpg")` + `-fx-background-size: cover` (riusa la texture dark wood già impiegata da `.board-cell-dark` Task 3.5.6 — coerenza visiva "questo è un gioco di dama"); `.main-menu-card` aggiunge cornice in legno scura (`-fx-border-color: -color-border-frame` 2px + `-fx-border-radius: 10`) e dropshadow morbido per il rilievo; `.main-menu-card:hover` border `-color-accent-gold` + dropshadow gold doppio (glow + shadow esterno) — l'hover funziona sull'intera area della card via JavaFX `:hover` pseudo-class (Region native); `.main-menu-card-title` Playfair Display 22px bold con fill `-color-text-on-light` (dark roast) — gold sarebbe stato troppo basso contrasto sul cream parchment (~1.7:1, fail WCAG AA), serif premium si fa col font-family invece che col color; `.main-menu-card .ikonli-font-icon { -fx-icon-color: -color-border-subtle }` — gli icon glyph Ikonli mdi2g-gamepad-variant / mdi2l-lan / mdi2w-web / mdi2b-book-open-page-variant / mdi2c-cog passano dal default near-black al warm brown #6B4423, intonati al wood theme. **Nessun nuovo test**: stessa logica della splash — la menu screen è purely visivamente verifyable a Task 3.5.14. Test esistenti `MainMenuControllerTest` (5 click + 5 prompt branch test) preservati intact. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **321 test totali** (invariato vs 3.5.9), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.9 — Splash screen restyling (PLAN-fase-3.5 §3.5.9). Reskin coordinato della splash. (1) **`splash.fxml`** rifatto: rimossi `FontIcon mdi2c-checkerboard` 96px (focus tipografico, niente icona) e `ProgressIndicator` (spinner circolare); aggiunto `styleClass="splash-root"` allo `StackPane` esterno per il background wood. Tre child principali nel VBox: `Label` titolo "Dama Italiana" con `styleClass="label-display, label-display-lg, splash-title"` (Playfair 48px + tinta gold + drop-shadow), nuovo `Label fx:id="subtitleLabel" styleClass="splash-subtitle"` (Playfair italic 18px gold-hover), `ProgressBar fx:id="progressBar" prefWidth="320" progress="-1.0" styleClass="splash-progress"` (indeterminate barber-pole orizzontale al posto dello spinner), `Label fx:id="loadingLabel" styleClass="label-secondary, splash-loading"`. (2) **`SplashController`** — nuovo `@FXML private Label subtitleLabel` settato in `initialize()` via `i18n.t("splash.subtitle")`; ctor invariato (i 5 test esistenti `SplashControllerTest` non rompono — non toccano FXML, solo `bootstrap()` + `finish()`); `loadingLabel` invariato. (3) **i18n**: nuova chiave `splash.subtitle` IT="Gioco classico contro IA", EN="Classic checkers vs AI". `LocalizationE2ETest.contractKeys` lista positiva di 87 chiavi non-blank — l'aggiunta di una nuova chiave non spezza il contract (additivo). (4) **`theme-light.css`** sezione "SPLASH SCREEN (Task 3.5.9)" aggiunta in coda: `.splash-root` `-fx-background-image: url("/assets/textures/frame.jpg")` + `-fx-background-size: cover` (riusa la texture di `frame.jpg` già impiegata da `.board-frame` Task 3.5.6); `.splash-title` aggiunge solo un `dropshadow` al token `.label-display-lg` esistente per migliorare la leggibilità del gold sul wood; `.splash-subtitle` Playfair italic 18px in `-color-accent-gold-hover` con dropshadow; `.splash-progress` con `-fx-accent: -color-accent-gold` + sub-selectors `> .track` (background `-color-bg-surface` deep walnut + border `-color-border-frame`) e `> .bar` (gradient gold linear-to-right + dropshadow gold per glow); `.splash-loading` cream italic con dropshadow per leggibilità. **Loading time invariato** (`SplashController.MIN_SPLASH_MILLIS = 1500L` non toccato). **Nessun nuovo test**: la splash è una scena a flusso fisso (nessun click/select/keyboard), e il rendering FXML/CSS è verifica visiva manuale (Task 3.5.14). I 5 test `SplashControllerTest` esistenti continuano a coprire la pipeline async + il routing finale; 0 modificati. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **321 test totali** (invariato vs 3.5.8), JaCoCo client gate ✅ (bundle invariato a 69 classes), SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.8 — Animation polish: juicy + particle + glow (PLAN-fase-3.5 §3.5.8). Quattro cambi. (1) **`MoveAnimator.MOVE_INTERPOLATOR`** — da `Interpolator.EASE_OUT` a un'`Interpolator` anonima che implementa la curva `easeOutBack` di easings.net (`1 + c3*(t-1)^3 + c1*(t-1)^2`, `c1=1.70158, c3=2.70158`). **Nota**: il primo tentativo via `Interpolator.SPLINE(0.34, 1.56, 0.64, 1)` ha fallito al class-init con `IllegalArgumentException — Control point coordinates must all be in range [0,1]` (JavaFX 21 valida tutti e 4 i parametri SPLINE in [0,1], proibendo `y1 > 1` necessario per overshoot); soluzione: subclass anonima di `Interpolator` con override `protected double curve(double t)`, matematica pura. La pedina ora "atterra" col tipico bouncing leggero out-back (peak ~110%) invece dell'ease-out neutro. (2) **Nuovo `ParticleEffects`** in `client/ui/board/animation/`: tre helper statici stateless con eager-attach pattern (i nodi sono aggiunti immediatamente al `host.getChildren()` durante la build dell'animation, opacity=0, fade-in al play, removed dal `setOnFinished`). `captureSplash(Pane host, double x, double y)` → 10 `Circle` marrone/grigio polvere (3 colors `#6B4423`/`#8C6A4A`/`#9E9285` cycled), radius 28px, 350ms `ParallelTransition(translate radiale, fade 0.85→0, scale 1.0→0.4)`; `promotionGlow(Pane, double, double)` → 10 `Line` 36px irradianti, alternanza `#FFD700`/`#FFA500`, 600ms `ParallelTransition(scale 0.2→1.0, fade 0.95→0)`; `mandatoryGlow(Node piece)` → applica `DropShadow` `#FFD700` `spread=0.45` al pezzo + ritorna `Timeline` cycle indefinito 1200ms con KeyFrames sulla `radiusProperty` `12→24→12`, `setOnFinished` rimuove l'effetto. (3) **`AnimationOrchestrator.animateMove` overload 4-arg** — nuovo `animateMove(move, pieceAt, cellSize, particleHost)` che, quando `particleHost != null`, su ogni capture leg layer aggiunge un terzo child `ParticleEffects.captureSplash` (slide + fadeCapture + splash, parallel). L'overload 3-arg esistente delega al nuovo passando `null` per preservare backward compatibility e i 6 test di `AnimationOrchestratorTest` precedenti. (4) **`BoardRenderer.particleHost()`** — nuovo metodo public che ritorna un `Pane particleLayer` aggiunto come ultimo child del renderer (z-order in cima ai cells), `setMouseTransparent(true)`, `styleClass="board-particle-layer"`, sized e posizionato in `layoutChildren` a `(0, 0, 8*cellSize, 8*cellSize)` per matchare esattamente il grid di celle. **`SinglePlayerController.applyMove`** ora invoca `AnimationOrchestrator.animateMove(move, renderer::pieceAt, renderer.currentCellSize(), renderer.particleHost())` — la production gameplay ora vede splash particles su ogni cattura. **Tests**: 5 nuovi in `ParticleEffectsTest` (puro Java, niente Toolkit — `Pane`, `Circle`, `Line`, `Timeline`, `DropShadow` instantiate senza Platform.startup): `captureSplashAttachesParticlesEagerlyAndReturnsParallelOfSameSize` (host children = 10, anim = ParallelTransition con 10 children, onFinished settato), `captureSplashParticlesStartInvisibleAndPositionedAtOrigin` (opacity 0, translateXY = origin, mouse transparent), `promotionGlowAttachesRaysAndReturnsParallelOfSameSize` (10 Line, totalDuration 600ms), `mandatoryGlowAppliesDropShadowAndReturnsIndefiniteTimeline` (effect è DropShadow, cycleCount INDEFINITE, 3 KeyFrames, color `0xffd700ff`), `splashAndGlowConstantsHaveExpectedValues` (sanity check delle costanti). 1 nuovo test in `AnimationOrchestratorTest` (`captureLegWithParticleHostAddsSplashAsThirdChild`): leg con host non-null ha 3 children (slide + fadeCapture + captureSplash), host.children = 10. Esistente `slideMoveInterpolatorIsEaseOut` rinominato in `slideMoveInterpolatorIsOutBackSpline` con assertion negativa `not same as Interpolator.EASE_OUT` + identità con `MOVE_INTERPOLATOR`. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **321 test totali** (+6 vs 3.5.7), JaCoCo client gate ✅ (bundle ora 69 classes, +2 per ParticleEffects + l'anonymous Interpolator), SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.7 — `PieceNode` redesign 3D-look (PLAN-fase-3.5 §3.5.7). `PieceNode` (StackPane sub-class) ora compone fino a 4 children invece del singolo Circle: (1) **body** Circle, radius bound a `widthProperty().divide(2.4)`, styleClass `.piece` (+ `.piece-black` per nero, + `.piece-king` per dame — la classe legacy `.piece-king` resta sul body così la regola gold-stroke esistente continua ad applicarsi e `ThemeServiceTest.contains(".piece-king")` resta verde); (2) **ring** Circle outer dashed, radius `width*0.405`, `.piece-ring` (+ `.piece-ring-black` per nero) — stroke nero translucent + dashArray `3 2` per simulare la scanalatura del disco fisico; (3) **gloss** Circle, radius `width*0.10`, translateX/Y bound a `widthProperty().multiply(-0.16)` — bianco translucent in alto-sinistra per riflesso luce; (4) **crown** Text "♛" U+265B (solo per dame), styleClass `.piece-king-marker` (+ `.piece-king-marker-black` per dame nere), font size scaled tramite `Bindings.createObjectBinding(() -> Font.font("Playfair Display", FontWeight.BOLD, getWidth() * 0.45), widthProperty())`. **components.css** rules nuove: `.piece` ora con `-fx-fill: radial-gradient(focus-angle 0deg, focus-distance 0%, center 35% 35%, radius 60%, derive(-color-piece-white, 30%), -color-piece-white, derive(-color-piece-white, -22%))` per simulare disco bombato (gradient cream→ivory chiaro), `-fx-stroke-width 1.5`, `-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.50), 4, 0, 2, 2)` per ombra; `.piece-black` overrides il gradient con scala scura (derive(black, +50%/+18%/0)); `.piece-king` invariato (gold stroke 3px); 4 nuove classi `.piece-ring`, `.piece-ring-black`, `.piece-gloss`, `.piece-king-marker`, `.piece-king-marker-black` con stroke/fill espliciti. **PieceNode.CROWN_GLYPH = "♛"** package-private constant per asserzioni di test. **5 nuovi test** in `PieceNodeTest` (puro, niente JavaFX runtime — JavaFX shapes possono essere instanziati senza Toolkit; le asserzioni toccano solo `getStyleClass()` membership e tipi children): white man → body has piece, no piece-black/piece-king, no Text child; black man → body has piece+piece-black, no piece-king, no Text child; white king → body has piece+piece-king, no piece-black, crown Text has piece-king-marker (no -black), text == "♛"; black king → body has piece+piece-black+piece-king, crown has piece-king-marker+piece-king-marker-black; man textChildren empty, king textChildren size 1. **Animation API preserved**: `BoardRenderer.pieceAt()` continua a tornare il PieceNode StackPane (Node), `AnimationOrchestrator.animateMove` muove il StackPane intero — i 4 children (body+ring+gloss+crown) si muovono insieme. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **315 test totali** (+5 vs 3.5.6), JaCoCo client gate ✅ (bundle invariato a 67 classes — PieceNode resta singola classe), SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.6 — `BoardCellNode` redesign con texture legno (PLAN-fase-3.5 §3.5.6). Tre cambi solo lato CSS+FXML, nessuna modifica al codice Java prod (i.e. niente impatto su test esistenti). (1) **`components.css`** — `.board-cell-light` e `.board-cell-dark` ora portano `-fx-background-image: url("/assets/textures/board_{light,dark}.jpg")` (i 2K JPG CC0 di Task 3.5.1) con `-fx-background-size: 100% 100%` + `-fx-background-repeat: no-repeat`; il fallback `-fx-background-color: -color-board-{light,dark}` resta come safety net se l'immagine non si carica. Ogni cella renderizza la texture stretched al proprio bbox (~80px) — visivamente uniforme tra celle dello stesso colore ma chiaramente "legno" e non flat. (2) **`board-view.fxml`** — `BoardRenderer` wrappata in nuovo `<StackPane styleClass="board-frame">` con `<padding>` 24px su tutti e 4 i lati; `<?import javafx.scene.layout.StackPane?>` aggiunto. Il rendering rest non cambia (StackPane single child = BoardRenderer rimane al centro, BorderPane padding 16px conservato). Nuovo `.board-frame` style class: `-fx-background-image: url("/assets/textures/frame.jpg")` + `-fx-background-size: cover` + `-fx-background-radius: 8` + `-fx-effect: dropshadow(gaussian, rgba(0,0,0,0.45), 16, 0, 0, 4)` per dare al frame un'ombra che lo stacca dal pannello scuro circostante. (3) **`.legal-target`** sostituito: dal flat fill verde (`-fx-background-color: -color-highlight-legal` + `-fx-background-radius: 4` + `-fx-opacity: 0.65`) al glow oro (`-fx-effect: dropshadow(gaussian, -color-accent-gold, 16, 0.70, 0, 0)`) — la texture wood resta visibile sotto, l'highlight è un alone giallo-oro che si estende leggermente nelle celle vicine. `.pulse-mandatory` lasciato invariato (Task 3.5.9 lo rivedrà). **`.board-cell-selected`** già usava innershadow gold (token coherent). **Nessun nuovo test, nessun test rotto**: gli unici test che toccano questi nomi CSS sono `ThemeServiceTest` (asserisce solo che le stringhe `.board-cell-light/-dark`/`.legal-target` esistano in `components.css`, e queste sono ancora lì) e `BoardViewControllerTest` (mockito, nessun parsing FXML). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 47s, **310 test totali** (invariato vs 3.5.5), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.5 — SFX wiring nei punti di gameplay (PLAN-fase-3.5 §3.5.5). `SinglePlayerController` ora dispatches SFX su `audioService` injected via ctor 4° arg. **Tre canali indipendenti** in `dispatchMoveSfx(move, preMovePiece)` chiamato dentro `finalizeMove` dopo `applyMove`: (1) `Sfx.CAPTURE` se `move.isCapture()`, altrimenti `Sfx.MOVE` (sempre uno dei due, side-effects per ogni mossa applicata, sia umana che IA — feedback acustico simmetrico per il "videogame premium" SPEC §13.4); (2) `Sfx.PROMOTION` se la pedina è transitata MAN→KING durante questa mossa (`preMovePiece.kind == MAN && state.board().at(move.to()).kind == KING`); (3) `Sfx.VICTORY` o `Sfx.DEFEAT` su stato terminale (`state.status().isWin()`), confronto winner color vs `game.humanColor()` — draws restano silenti per non aggiungere rumore su esiti neutri. **ILLEGAL** in `onCellClicked`: ramo "click su empty/enemy con `selected != null` e nessun match precedente" → `audioService.playSfx(Sfx.ILLEGAL)` + `deselect()` (utente che tenta una mossa illegale dopo aver selezionato una pedina). I 4 sotto-rami che NON suonano ILLEGAL sono preservati: click su target legale → `applyMove` (no ILLEGAL); click su selected stesso → deselect silenzioso; click su altra pedina friendly → reselect silenzioso; click su empty senza selezione → no-op silenzioso. **Tests**: 9 nuovi (5 in `SinglePlayerControllerTest`: simpleMove → MOVE, illegal click + selection → ILLEGAL, click empty no-selection → no ILLEGAL, reselect friendly → no ILLEGAL, deselect same square → no ILLEGAL; 4 in `SinglePlayerE2ETest` con setup hand-built `SerializedGameState`: capture → CAPTURE no MOVE, promotion 5→1 → MOVE+PROMOTION, capture-last-piece → CAPTURE+VICTORY, AI white captures last black piece (humanColor=BLACK) → CAPTURE+DEFEAT via `AiTurnService` mock CompletableFuture). Aggiornati 6 ctor sites in test files (5 in `SinglePlayerControllerTest`, 1 in `SinglePlayerE2ETest`) per il nuovo arg `AudioService`. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **310 test totali** (+9 vs 3.5.4 follow-up), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.4 follow-up — Audio format pipeline (2026-05-01, post manual user test). **Verifica empirica**: JavaFX Media su Windows **non decodifica OGG Vorbis** (`MediaException — Unrecognized file signature!`). I pack Kenney di Task 3.5.1 distribuivano solo OGG, quindi gli SFX OGG committati erano inutilizzabili al runtime. Soluzione approvata dall'utente (Opzione 2 di 4): pipeline pure-Java OGG→WAV in fase di asset acquisition. **Output**: (1) Master OGG preservati in `assets/audio/sfx-master/` (audit trail, sorgente di verità ~70 KB). (2) WAV PCM 16-bit signed LE committati in `assets/audio/sfx/` (~600 KB), caricati a runtime. (3) Tool `OggToWavConverter` in `client/src/test/java/.../buildtools/`, main Java basato su API low-level JOrbis (Page/Packet/StreamState/SyncState/DspState/Block, modellato su `com.jcraft.jorbis.DecodeExample`). Approccio iniziale via `javax.sound.sampled` SPI ha rivelato un bug di buffer underrun con clip <400 ms (3 SFX su 6 producevano 0 byte PCM); switch a JOrbis low-level dà decoding deterministico per tutte e 6. (4) 2 dep `<scope>test</scope>` in `client/pom.xml`: `jorbis:0.0.17.4` + `vorbisspi:1.0.3.3` (entrambe LGPL, non ridistribuite, nessun obbligo). **Eccezione SPEC §6** approvata informalmente: dep di tooling asset-pipeline, fuori dal runtime stack. (5) `Sfx.resourcePath()` aggiornato: 6 path da `.ogg` → `.wav`. `SfxTest` assertion adeguata. (6) **Music binari committati** in `assets/audio/music/`: 1 MP3 (`calm_piano_1.mp3` 6.30 MB) + 3 WAV PCM (`first_light_particles.wav` 24.12 MB, `at_home_orchestral.wav` 12.50 MB, `peaceful_forest.wav` 21.23 MB). Totale ~64 MB, niente re-encoding (JavaFX legge MP3+WAV nativamente). `JavaFxAudioService.MUSIC_TRACKS` aggiornato. (7) Test `SfxPlaybackSmokeTest` (1 test) sostituisce il temporaneo `OggCodecSmokeTest`: verifica che ogni Sfx carichi via `javax.sound.sampled.AudioSystem.getAudioInputStream` (encoding=PCM_SIGNED, channels∈[1,2], frameLength>0). Headless-friendly. (8) `CREDITS.md` riscritto: nuova sotto-sezione "Pipeline format" documenta workflow OGG-master/WAV-runtime + comando `mvn exec:java`; tabella SFX con colonne "Master OGG" e "WAV PCM"; sezione Music documenta as-downloaded format. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **301 test totali** (+1, -1 net), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.4 — AudioService implementazione + Settings integration (MediaPlayer wiring). Output multipli. (1) **`UserPreferences` schema v2** (`CURRENT_SCHEMA_VERSION=2`): aggiunti `int musicVolumePercent`, `int sfxVolumePercent`, `boolean musicMuted`, `boolean sfxMuted` (default 30/70/false/false, SPEC §13.4); compact canonical constructor con clamp 0-100; static `@JsonCreator` `fromJson()` con boxed `Integer`/`Boolean` per detect missing keys (v1 file → defaults); 4 nuovi `with*` setters; `withSchemaVersion(int)` package-private per migration. (2) **`PreferencesService.load()` migration v1→v2**: ramo dedicato `version == 1 → withSchemaVersion(CURRENT)` (audio defaults già riempiti dal `@JsonCreator`); `version == CURRENT` ramo invariato; `version > CURRENT || sconosciuto → defaults()`. (3) **`JavaFxAudioService` full implementation**: `@Component` ora cabla `MediaPlayer` reale. **SFX bus**: `EnumMap<Sfx, MediaPlayer>` cache lazy (un player per cue, riusato con `seek(Duration.ZERO)+play()`); side-by-side cues si sovrappongono, stesso cue back-to-back è sequenziale (limite JavaFX, accettato per la nature one-shot dei nostri SFX). **Music bus**: single `MediaPlayer` driven da `MusicPlaylist`; `setOnEndOfMedia` callback chiama `tryAdvanceNext()` che dispone il player corrente e ne crea uno nuovo per il prossimo brano shufflato. **Music tracks discovery**: lista hard-coded `MUSIC_TRACKS` (4 path da CREDITS.md), filtered da `getClass().getResource() != null` → assenti tollerati (warn log, music bus silente se 0 disponibili). **Threading**: tutte le interazioni JavaFX media via helper `runOnFxThread()` con catch `IllegalStateException` (toolkit non inizializzato → run inline) per resilienza unit-test. **Persistence**: ogni `setVolume`/`setMuted` invoca `persistAudioPreferences()` che fa `load+with*+save`; failure di `save` swallowed con WARN log (in-memory state mantenuto). **Mute semantics**: `setMuted(MUSIC, true)` dispone il player; `false` ri-avvia se `musicRequested==true`. `@PreDestroy shutdown()` dispone tutti i players. **JaCoCo exclusion**: `JavaFxAudioService.class` esclusa da report+check (stesso pattern di `JavaFxUserPromptService` per JavaFX-bound impl, l'interface `AudioService` è la testable abstraction). (4) **Auto-start music in `JavaFxApp.start()`**: dopo `primaryStage.show()` → `context.getBean(AudioService.class).playMusicShuffle()` con try/catch RuntimeException (continua senza audio se boot media fallisce). (5) **Settings UI extension**: `settings.fxml` aggiunta sezione Audio con 2 `Slider` 0-100 (majorTickUnit=25, showTickMarks/Labels) + 2 `CheckBox` mute. `SettingsController` ctor +6° arg `AudioService`; `populateAudio()` legge volume/muted da audioService; 4 nuovi `@FXML` handlers (`onMusicVolumeChanged`/`onSfxVolumeChanged`/`onMusicMutedChanged`/`onSfxMutedChanged`) che chiamano `audioService.setVolume`/`setMuted` e disabilitano lo slider corrispondente quando muto. **i18n** keys IT/EN: `settings.section.audio`, `settings.audio.{music,sfx,muted}`. (6) **Music asset binari**: ancora **non committati** (4 OpenGameArt tracks da CREDITS.md: calm_piano_1, first_light_particles, at_home_orchestral, peaceful_forest); `MUSIC_TRACKS` punta ai loro path attesi, l'AudioService tollera l'assenza (warn log "Music bus has no playable tracks"). Download+re-encode ffmpeg WAV→OGG @96kbps è un commit follow-up dedicato (out of band rispetto al code wiring). **Tests**: `JavaFxAudioServiceTest` riscritto (8 test: init from prefs, defaults SPEC §13.4, setVolume clamp+persist, setMuted toggle+persist, persist failure swallow, mute-while-playing safe, idempotent stop); `PreferencesServiceTest` +1 (`migratesSchemaV1FileFillingAudioDefaults`); `SettingsControllerTest` ctor signature aggiornata con `AudioService` mock; `UiScalingServiceTest` ctor 9-arg. **5 fix follow-up readability** già committato in `dca69fb` prima di 3.5.4: `.button-secondary` background ora `-color-bg-surface` (deep walnut) come default `.button` (era `transparent`, invisibile dentro card cream). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **300 test totali** (+5 vs 3.5.3: 4 nuovi `JavaFxAudioServiceTest` + 1 migration `PreferencesServiceTest`), JaCoCo client gate ✅, SpotBugs 0, Spotless OK.

- **Task precedente**: Task 3.5.3 — Skeleton package audio. Nuovo package `com.damaitaliana.client.audio` con 6 file di produzione: `package-info.java`, `AudioBus.java` (enum MUSIC/SFX), `Sfx.java` (enum 6 valori MOVE/CAPTURE/PROMOTION/ILLEGAL/VICTORY/DEFEAT con `resourcePath()` per i 6 file Kenney di Task 3.5.1 — **deviazione dal PLAN-fase-3.5 §3.5.3** originale che listava 4 valori, espansione a 6 motivata dalla coerenza con gli asset acquisiti già in repo), `MusicPlaylist.java` (final class con shuffle deterministico via `RandomGenerator` injectable + anti-back-to-back-repeat), `AudioService.java` (interface 7 metodi), `JavaFxAudioService.java` (@Component skeleton no-op, default 30/70 SPEC §13.4, isMuted=false; Task 3.5.4 cabla MediaPlayer + PreferencesService). 14 nuovi test (AudioBusTest 1, SfxTest 4, MusicPlaylistTest 6, JavaFxAudioServiceTest 3). Anche **fix readability post-3.5.2** committato (`86c181f`): selettori `.card .label` / `.card-elevated .label` / `.popover .label` ristretti alle utility class `.label-title/-subtitle/-secondary` (matchavano il `.label` interno del Button JavaFX rendendo dark-on-dark il testo dei pulsanti dentro le card cream); aggiunto styling completo `.menu-bar` + `.menu-button` + `.menu-item` + `.context-menu` + `.tooltip` (mai stilizzati per il tema dark — i default JavaFX assumono ambiente light → testo invisibile). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **295 test totali**, JaCoCo client gate ✅, SpotBugs 0, Spotless OK. Stop point §7.2 lasciato come informational reminder; l'utente ha approvato l'approccio "miglioramento incrementale fino al target attraverso le task successive" → procedo dritto.

- **Task precedente**: Task 3.5.2 — Design tokens v2. **`theme-light.css`** riscritto con palette wood premium SPEC §13.2 (bg-primary `#2A1F15` dark roast, bg-elevated `#F5E6C8` cream, accent-gold `#C9A45C`, accent-deep-red `#8B3A3A`); rimosso vocabolario `text-primary`/`accent` legacy, sostituito con duale `text-on-dark`/`text-on-light` per gestire le superfici miste (sfondo dark + card cream). Aggiunte font-family chain **display** (`"Playfair Display", "Cormorant Garamond", "Georgia", serif`) referenziata da `.label-display` (32/48 px in antique gold, hero titles). Nuove classi `.button-primary` (gradient gold + bevel + glow on hover, foreground dark-roast per WCAG AA ~9:1) e `.button-secondary` (wood-frame outline transparent), `.button` default rimappato come neutro. Card/popover/list-cell/text-field aggiornati per ambiente cream (foreground dark-roast); 5 stati interattivi su tutti per SPEC §13.2.2. **`components.css`** remappato (`board-cell-selected` innershadow gold, `piece-king` stroke gold, `move-history-row-current` derive +10% del bg-surface). **`theme-dark.css`** stub aggiornato con stesso vocabolario per token coherence in vista F11 (bg-primary `#1A120A`). **Font binari committati**: `InterVariable.ttf` (859 KB, weight Thin..Black da rsms/inter) + `PlayfairDisplay-Variable.ttf` (294 KB, Regular..Black da google/fonts) + 2 OFL.txt (SIL OFL 1.1 redistribution requirement). **`ThemeService`** ora carica Inter + Playfair Display via `Font.loadFont`; rimossi i constants legacy `INTER_REGULAR_PATH`/`SEMIBOLD_PATH` (file mai esistiti) sostituiti con `INTER_VARIABLE_PATH`/`PLAYFAIR_DISPLAY_VARIABLE_PATH`. **`fonts/README.md`** riscritto: font ora bundled, fallback chain documentata. **`CREDITS.md`** esteso con sezione Fonts (Inter + Playfair Display OFL, sorgenti GitHub raw); frontmatter license-policy duale (CC0 visual+audio, OFL fonts). **`ThemeServiceTest`** ridotto a 9 test (1 nuovo `themeLightDefinesPrimaryAndSecondaryButtonAndDisplayLabel`, riscritti i 2 sui token light/dark per nuovo palette, rinominato il font test). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS in 1:08, **281 test totali**, JaCoCo client gate ✅ (bundle 63 classi), SpotBugs 0, Spotless OK. **Stop point §7.2 attivo**: in attesa di approvazione utente sulla palette/tipografia prima di procedere col restyling delle 8 schermate (Tasks 3.5.6, 3.5.9-3.5.11). Tasks audio 3.5.3-3.5.5 sono visivamente indipendenti.

- **Task precedente**: Task 3.5.1 — Asset acquisition + CREDITS.md. Output: (1) **3 texture legno** Poly Haven 2K JPG diffuse (CC0 1.0 globale) committate in `client/src/main/resources/assets/textures/`: `board_dark.jpg` (`dark_wooden_planks`, 2.75 MB) + `board_light.jpg` (`oak_wood_planks`, 2.61 MB) + `frame.jpg` (`wood_table_worn`, 2.96 MB) — totale 8.32 MB. (2) **6 SFX OGG Vorbis** Kenney (CC0 1.0 globale) committati in `client/src/main/resources/assets/audio/sfx/` con durate verificate via mutagen probe su download diagnostico (no commit del temp): `move.ogg` = `impactWood_light_000` 266 ms, `capture.ogg` = `impactWood_heavy_000` 313 ms, `promotion.ogg` = `confirmation_002` 539 ms, `illegal.ogg` = `error_001` 165 ms, `win.ogg` = `jingles_PIZZI07` 1.32 s, `lose.ogg` = `jingles_PIZZI03` 1.15 s — totale ~70 KB. Tutti 44.1 kHz; Impact stereo 160 kbps, Interface mono 96-239 kbps, Music Jingles stereo 160 kbps. (3) **`CREDITS.md`** in root con frontmatter (license-policy CC0-only, last-updated 2026-05-01, phase F3.5) + 3 sezioni Visual/Music/SFX (per asset: local file, source page, direct file URL, license, author, durata o size on disk) + audit trail della verifica licenza per asset (Poly Haven `dl.polyhaven.org` HTTP 200, OpenGameArt CC0 string verbatim, Kenney pack-level CC0) + lista esclusioni (walnut_veneer Patreon-locked, wood_planks_027/028 404, freepd.com chiuso 2025, tutto CC-BY, pizzicato <1s, Hit/Sax/Steel/8-Bit jingles per mood). (4) **Music binari** (4 tracce OpenGameArt CC0 — Calm Piano 1 / First Light Particles / At Home Orchestral / Peaceful Forest) **deferred a Task 3.5.4** (richiede ffmpeg per re-encode WAV→OGG; setup ffmpeg portable in 3.5.1 ha avuto download incompleto e si è scelto di delegare il re-encoding all'integrazione AudioService dove serve naturalmente). Selezioni e URL **definitivi e approvati** documentati in CREDITS.md. (5) Dubbio utente "1 minuto SFX" smentito definitivamente con probe mutagen: file più lungo nell'intero pool è `jingles_SAX07` a 1.74 s, niente vicino a 1 minuto (era l'anteprima cumulativa del pack page Kenney). **Stop point §7.1 PLAN-fase-3.5 chiuso** in-thread con approvazione utente. **Nessun nuovo test** prodotto in 3.5.1 (asset only). `mvn -pl client verify -DexcludedGroups=slow,performance` da rieseguire a 3.5.2 (l'aggiunta di binari sotto `resources/` non rompe niente lato build).

- **Task precedente**: Task 3.23 — Documentazione, ADR, TRACEABILITY, README, CHANGELOG. Chiude la sotto-fase IMPLEMENTA Fase 3. Cinque output: (1) **`ARCHITECTURE.md`** appendiati 5 ADR per le decisioni di design F3: ADR-029 (bootstrap JavaFX con Spring Boot DI non-web — `ClientApplication` Spring main → `Application.launch(JavaFxApp.class)` con `JavaFxAppContextHolder` come bridge singleton; `webEnvironment=NONE`); ADR-030 (architettura schermate FXML + `FXMLLoader.setControllerFactory(applicationContext::getBean)`, controller `@Component @Scope("prototype")` perché `load()` può essere chiamato più volte e singleton resetterebbe i `@FXML`; lezione `feedback_spring_ui_tests` formalizzata come ADR); ADR-031 (schema saves v1: JSON con `schemaVersion=1`, `kind` manual/autosave, `aiLevel`, `humanColor`, `currentState` riusando `SerializedGameState` 4-FID-list, `moves` future-proof, `UnknownSchemaVersionException` propagata a toast localizzato); ADR-032 (autosave atomico via `Files.move(tmp, target, ATOMIC_MOVE, REPLACE_EXISTING)` + fallback `REPLACE_EXISTING` solo per FS senza `ATOMIC_MOVE` + `SinglePlayerAutosaveTrigger` swallowa `UncheckedIOException` con log WARN); ADR-033 (localizzazione: `MessageSource` Spring + `LocaleService` bridge + `I18n` wrapper con `[code]` placeholder per chiavi mancanti, default Italian, encoding UTF-8 esplicito, chiavi `screen.element.role`, runtime swap rinviato a F11). (2) **`package-info.java`** aggiunti i 4 mancanti (`ui`, `ui.save`, `ui.settings`, `ui.rules`) — gli altri 9 erano già scritti task-by-task. (3) **`tests/TRACEABILITY.md`** righe per FR-SP-01..09 (UI), FR-RUL-02..05, NFR-M-02 (gate client 60% con 9 esclusioni), NFR-U-01 (LocalizationE2ETest), NFR-U-04 (light WCAG manual), AC §17.1.9 + §17.1.12 + nuova sezione "Acceptance criteria di Fase 3" con 21 voci A3.1..A3.21 mappate ai test. (4) **`README.md`** sezione "Eseguire il client (Fase 3+)" con `mvn -pl client javafx:run`, prerequisito `mvn install` parent+shared+core-server, tabella storage utente (`~/.dama-italiana/{config.json, saves/<slot>.json, saves/_autosave.json}`), toggle lingua IT/EN richiede riavvio (ADR-033), fast loop `mvn -pl client verify -DexcludedGroups=slow,performance` (~30s); stato progetto aggiornato a "Fase 3 — Client UI single-player". (5) **`CHANGELOG.md`** voce Task 3.23 + voce "Fase 3 summary" che riepiloga l'arco IMPLEMENTA (Task 3.1-3.23: bootstrap, design system, localizzazione, preferences, splash, main menu 6-card, setup modal, BoardRenderer, interazione click, animation engine, history pane FID, status pane, IA async virtual thread, SaveService multi-slot, save dialog + load screen, AutosaveService + recovery prompt, settings, rules screen 7 sezioni 6 diagrammi, mini-animazioni regole, accessibility, E2E tests, JaCoCo gate 60%, documentazione). **Nessun nuovo test** prodotto (design del piano). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **264 test totali** (invariato), JaCoCo client gate ✅ (haltOnFailure=true, line 73.94% / branch 60.55%), SpotBugs 0 High, Spotless OK.

- **Task precedente**: Task 3.22 — Coverage gate JaCoCo client + cleanup. Configurato `client/pom.xml` con `jacoco-maven-plugin` override delle execution `jacoco-report` e `jacoco-check` (entrambe in fase `verify`): `haltOnFailure=true`, regola `BUNDLE` ≥ 60% line + branch (NFR-M-02), `<excludes>` su 9 file `.class` con motivazione esplicita nei commenti del POM. Esclusioni applicate sia al report sia al check (gli `excludes` di `prepare-agent` skip-pano solo l'instrumentation; le classi non instrumentate appaiono comunque al 0% nel report → necessario duplicare al livello report/check per spostarle effettivamente fuori dal denominatore). Tre famiglie di esclusioni: (1) **JavaFX/Spring bootstrap glue** (`JavaFxApp`, `JavaFxAppContextHolder`, `ClientApplication`) — JavaFX `Application` subclass + Spring/JavaFX bridge + Spring Boot main, eseguibili solo lanciando l'app; (2) **JavaFX Alert wrapper** (`JavaFxUserPromptService`) — implementation di `UserPromptService` reachable solo su FX thread vivo, mockata via interface dai test; (3) **anonymous cell-factory inner class** (`MoveHistoryView$MoveHistoryCell`, `LoadScreenController$MiniatureCell`, `RulesController$1`, `SettingsController$1`, `SplashController$1`) — TableCell/ListCell/StringConverter/javafx.concurrent.Task istanziate dal FX runtime, no business logic. Bundle dopo esclusioni: **62 classi** (-9), **lines 73.94% (1285/1738), branches 60.55% (284/469), instructions 73.42%** — gate verde con margine. Verifica negativa: gate fallisce a 57.4% branch quando le esclusioni mancano (BUILD FAILURE, "branches covered ratio is 0.57, but expected minimum is 0.60"). `ClientSmokeTest` già rimosso in Task 3.1 e sostituito da `ClientBootstrapTest` (4 test reali su Spring boot + SceneRouter + ClientProperties), nessun cleanup ulteriore richiesto. **Nessun nuovo test** prodotto in Task 3.22 (per design del piano: "solo gate"). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **264 test totali** (invariato vs Task 3.21), JaCoCo client gate ✅ (haltOnFailure=true), SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.21 — TestFX E2E single-player tests. Aggiunti 4 test E2E che chiudono la copertura Fase 3 dei criteri di accettazione UI (ognuno collocato nel package del modulo che esercitano, mirroring `AutosaveE2ETest`): (1) `client/i18n/LocalizationE2ETest` (4 test) — pinning della contract controller→bundle: lista hard-coded di 87 chiavi referenziate dai controller (menu, setup, board chrome, save dialog, load screen, settings, rules) verificate `isNotBlank().doesNotStartWith("[")` in IT e EN, più 1 spot-check distinguibilità IT/EN su `app.title`+`menu.rules.title` e 1 placeholder check su `status.player.color` + `setup.name.default` (`{0}`/`{1}` → `MessageFormat`). (2) `client/controller/SinglePlayerE2ETest` (3 test) — A3.11/SPEC §3.3 `manCannotCaptureKingInUi`: posizione hand-built whiteMan@FID22 + blackKing@FID18, click sulla pedina, captures `BoardRenderer.highlightLegalTargets` via `ArgumentCaptor` e verifica che `kingSquare` non sia tra gli highlight; A3.12/SPEC §3.5 `promotionStopsSequenceInUi`: whiteMan@FID5 → 1 (rank 7), dopo i 2 click verifica `sideToMove==BLACK` + `board.at(to).get().kind()==PieceKind.KING` + `board.at(from).isEmpty()`; A3.3-light `humanFirstMoveAdvancesGameStateAndHistory`: stato iniziale, prima mossa White (Move dalla `RuleEngine.legalMoves`) → `sideToMove==BLACK` + `history().size==1` + `status.isOngoing()`. Renderer + AutosaveTrigger Mockito-mocked → animazione path short-circuita a `finalizeMove` sincrono (renderer.pieceAt ritorna null) → tutto sul thread del test, no JavaFX runtime. (3) `client/persistence/SaveLoadE2ETest` (2 test) — A3.4 `saveThenLoadResumesAtSameState`: nuova `SinglePlayerGame` PRINCIPIANTE+WHITE+seed 7L, applica due plies via helper `applyOneMove` (stessa tecnica di `AutosaveE2ETest`: `RULES.legalMoves(state).first` + `RULES.applyMove`), persiste `SavedGame.of(snapshot, instant, instant)` su `SaveService` con `slugify(name)`, verifica `saveService.listSlots().slot()` contiene lo slug, ricarica via `saveService.load(slot).orElseThrow` + `SinglePlayerGame.fromSaved(loaded, new SplittableRandom(99L))` e asserta byte-equality su name/level/humanColor/board/sideToMove/halfmoveClock + parità di lunghezza history; `deleteRemovesSlotFromListing`: save → listSlots ha lo slot → `saveService.delete(slot)` → listSlots non lo contiene + `saveService.load(slot).isEmpty()`. Tempdir gestito via `@TempDir` JUnit5. (4) `client/ui/rules/RulesScreenE2ETest` (1 test, A3.10 `openRulesAndNavigateSections`) — `@SpringBootTest classes=ClientApplication.class webEnvironment=NONE` + `@TestPropertySource("dama.client.saves-dir=/tmp/test-saves-rules-e2e")`; `Platform.startup` con guard `Assumptions.assumeTrue(fxToolkitReady)` per skip headless; `FXMLLoader.load("/fxml/rules.fxml")` con `setControllerFactory(springContext::getBean)` su FX thread via `runOnFxThread(Callable)` + CountDownLatch; verifica default `currentSection==SETUP` poi itera su `RuleSection.ALL` (7 sezioni in display order), `selectSection` su FX thread, asserta `currentSection==section` + `contentChildren().get(0)` è Label il cui text == `i18n.t(section.titleKey())` + `contentChildren.size>=2` (header + ≥1 paragrafo). **Decisione di packaging**: la prima passata aveva i test sotto `client.e2e/`, ma rompeva la visibility package-private dei VISIBLE-FOR-TESTING accessor (`SinglePlayerController.state/onCellClicked/selectedSquare`, `RulesController.currentSection/selectSection/contentChildren`, `SaveSlotMetadata.slot()` — `slug()` era una mia assunzione errata). Soluzione: spostare ogni test nel package del modulo SUT mirando alla convenzione esistente (`AutosaveE2ETest` in `client.persistence` etc.) e **non** modificare la production API. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **264 test totali** (+10 vs Task 3.20: 4+3+2+1 nuovi), 0 skipped, JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.20 — Accessibility (accessibleText, keyboard nav, scaling globale). 4 cambi principali. (1) **Scaling globale**: nuovo `client/app/UiScalingService.java` `@Component` con `applyTo(Scene)` (legge `PreferencesService.load().uiScalePercent()`) e `applyTo(Scene, int)` (preview esplicito); whitelist `{100, 125, 150}` con fallback 100 + log WARN; `BASE_FONT_SIZE_PX = 14.0` × percent / 100. `SceneRouter.show` ora invoca `uiScalingService.applyTo(scene)` dopo `themeService.applyTheme(scene)` → la preferenza scaling è riapplicata su **ogni** navigazione (chiude la limitazione documentata in Task 3.17 — non solo scena Settings). `SettingsController` ridisegnato: ctor +`UiScalingService` arg, `onScalingChanged` delega a `uiScalingService.applyTo(scene, percent)` (rimossa la copia statica `applyScalingToScene` + `BASE_FONT_SIZE_PX`). (2) **`accessibleText` su BoardCellNode**: nuovo helper `client/ui/board/CellAccessibleText.java` con format inglese-only stile `PieceAccessibleText` (Fase 3 — localizzazione a11y in F11): light → "Light square (not playable)", dark vuota → "Dark square <FID>, empty", dark con pezzo → "Dark square <FID>, <PieceAccessibleText.describe>". `BoardCellNode.setAccessibleText` chiamato al ctor e ad ogni `setPiece(null|piece)`. (3) **Keyboard navigation board** (NFR-U-03): `BoardRenderer` ora `setFocusTraversable(true)` + `accessibleText="Italian draughts board, 8 by 8 grid"`. Tracking di una `Square keyboardFocus` con CSS class `cell-focused-keyboard` (border accent 2px in `components.css`). Handler `KeyEvent.KEY_PRESSED`: **arrow keys** muovono il focus di **due passi** sull'asse (next dark cell, step 2 conserva la parità del FID), **Enter** dispatcha il click handler sulla cella focalizzata, **Esc** invoca un `Runnable escapeHandler` opzionale. `focusedProperty` listener auto-imposta il focus iniziale a `(0,0)` quando il renderer riceve focus per la prima volta. `setKeyboardFocus(Square)` package-private rimuove la classe CSS dalla cella precedente e la aggiunge alla nuova. (4) **Wiring controller/view**: `SinglePlayerController.start` ora wira `renderer.setOnEscape(this::deselect)` → Esc deseleziona pezzi+highlights; `BoardViewController.initialize` chiama `Platform.runLater(boardRenderer::requestFocus)` dopo `gameController.start` per dare focus alla board appena entrati. **16 nuovi test**: `UiScalingServiceTest` 5 (null-scene no-op, applies 125%=`-fx-font-size: 17.5px` su root, unknown percent fallback 100=14px, `applyTo(scene)` legge prefs 150=21px, `ALLOWED_SCALES==[100,125,150]`); `CellAccessibleTextTest` 4 (light, dark empty, white man, black king — formati riproducibili senza JavaFX runtime); `BoardKeyboardNavigationTest` 7 (focus traversable, initial keyboardFocus=null, RIGHT da (0,0)→(2,0), UP→(0,2), edge clamp su LEFT da (0,0), ENTER→onCellClicked handler, ESC→escape handler — tutti su JavaFX thread con `Platform.startup` + `Assumptions.assumeTrue` per skip headless). `SettingsControllerTest` -1 (rimossa `applyScalingToSceneIsNullSafe` ora coperta da `UiScalingServiceTest#nullSceneIsNoOp`); ctor signature +`UiScalingService` mock. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **254 test totali** (+15 vs Task 3.19), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.19 — Mini-animazioni regole (FR-RUL-04, opzionale §7.6 → incluso). Nuovo `client/ui/rules/RulesAnimations.java` `@Component`: catalogo statico di 3 animazioni dimostrative con `enum Kind { SIMPLE_CAPTURE, MULTI_CAPTURE, PROMOTION }`. API: `startingPosition(Kind) → SerializedGameState`, `move(Kind) → Move`, `captionKey(Kind) → String`, `animation(Kind, Function<Square,Node>, double cellSize) → Animation`. Mosse FID legali secondo regole italiane: SIMPLE_CAPTURE = `22x15` saltando `18` (whiteMen [22], blackMen [18], CaptureSequence); MULTI_CAPTURE = `26x17x10` saltando `22` poi `14` (whiteMen [26], blackMen [22, 14], CaptureSequence con `captureCount==2`); PROMOTION = SimpleMove `5→1` (whiteMen [5], rank 6 → 7). Animation riusa `AnimationOrchestrator.animateMove` per le slide+fade legs; per PROMOTION wrappa in `SequentialTransition(slide, MoveAnimator.promotion(piece, flashOverlay))` (rotate 360° su asse Y + flash dorato `Rectangle(cellSize, cellSize, "#FFD700")` 500 ms, SPEC §13.3 NFR-U-04). `RulesController` esteso: ctor +`RulesAnimations` arg; per `CAPTURE` (2 widget) e `PROMOTION` (1 widget) `renderSection` aggiunge un `VBox` "rule-animation-widget" con `BoardRenderer` 280×280 inizializzato sulla starting position + caption + button "Riproduci" `button-secondary`; click → reset board, costruisce animation via `board::pieceAt` + `board.currentCellSize()`, disabilita button durante play, riattiva su `setOnFinished`. `animationKindsFor(RuleSection)` static visible-for-test: CAPTURE→[SIMPLE_CAPTURE, MULTI_CAPTURE], PROMOTION→[PROMOTION], altre→`List.of()`. **i18n** IT/EN: `rules.animation.play`, `rules.animation.{simple_capture,multi_capture,promotion}.caption`. **9 nuovi test**: `RulesAnimationsTest` 8 (SIMPLE_CAPTURE describes forward jump + correct path/captured Squares, MULTI_CAPTURE chains 2 jumps, PROMOTION moves to rank 7, caption keys unique scoped `rules.animation.*`, `cellSize<=0` throws IAE, simpleCapture animation plays without errors via `setRate(50)`+latch su JavaFX thread, MULTI_CAPTURE ritorna `SequentialTransition` con 2 children, PROMOTION ha `SequentialTransition(slide, promo)` con `promo.totalDuration == MoveAnimator.PROMOTION_DURATION == 500ms`); `RulesControllerTest` +1 (`animationKindsFor` mapping). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **239 test totali** (+9 vs Task 3.18), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.18 — Rules screen (sezioni statiche). Nuovo pkg `client/ui/rules` + `resources/fxml/rules.fxml` + 4 file `resources/rules/*.json`. `RulesController` `@Component @Scope("prototype")` con ctor `(SceneRouter, I18n, RuleDiagramLoader, MiniatureRenderer)`: `initialize()` popola la `ListView` con `RuleSection.ALL` (record con id+diagramResource, 7 istanze statiche `SETUP/MOVEMENT/CAPTURE/PRECEDENCE/PROMOTION/ENDGAME/NOTATION` in ordine SPEC §3), cell factory mappa via `i18n.t(section.titleKey())`, listener su `selectedItemProperty` chiama `renderSection(...)` che ricostruisce nel `VBox#contentBox` (destra dello SplitPane): header `label-subtitle` + paragrafi (split su `\n\n` da `body` i18n) + per ogni `RuleDiagram` (record `(captionKey, SerializedGameState position)`) un `VBox` con `ImageView` (rendered via `MiniatureRenderer.render(position, 220px)` riusando il bean prod `CanvasMiniatureRenderer` di Task 3.15, styleClass `rule-diagram`) + caption `label-secondary`. Default selection: `RuleSection.SETUP`. `selectSection(RuleSection)` e `currentSection()` e `contentChildren()` visible-for-test. `RuleDiagramLoader` (`@Component`): `loadFor(RuleSection)` legge classpath JSON via Jackson `TypeReference<List<RuleDiagram>>`; ritorna `List.of()` per sezioni senza diagram resource. **6 diagrammi totali su 4 sezioni** (setup 1 + movement 1 + capture 2 + promotion 2) — supera FR-RUL-03 (≥ 5 across ≥ 4). `SceneRouter.fxmlPathFor`: `RULES -> "/fxml/rules.fxml"` (rimosso placeholder null). `BoardViewController.onShowRules()` + nuovo `MenuItem rulesMenuItem` in `board-view.fxml` realizzano FR-RUL-01 ("Regole accessibile dal menu principale e dall'icona ⓘ in-game"): stop dell'AI + navigation a `SceneId.RULES`. **i18n** IT/EN: `rules.title`, `rules.section.<id>.{title,body}` × 7 (paragrafi separati da `\n\n`, bodies tradotti dal SPEC §3.1-3.6 + §3.8), `rules.diagram.<id>.caption` × 6, `board.menu.rules`. **16 nuovi test**: `RuleDiagramLoaderTest` 4 (setup loads initial, capture has 2, sezioni vuote tornano `List.of()`, coverage ≥ 5/4), `RulesControllerTest` 5 (display order, IT+EN title+body parity via Properties bundle read, hasDiagrams flags, selectSection tracks currentSection, onBack→MAIN_MENU), `RulesSpringContextTest` 3 (`@SpringBootTest` resolve prototype + isPrototypeScoped + RuleDiagramLoader bean — lezione `feedback_spring_ui_tests`), `RulesFxmlSmokeTest` 3 (default SETUP, clicking section updates currentSection, CAPTURE renders 2 ImageView con `.rule-diagram` — tutti su JavaFX thread con `Platform.startup` + `Assumptions.assumeTrue` per skip headless), `BoardViewControllerTest` +1 (`onShowRules` stops AI poi naviga RULES). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **230 test totali** (+16 vs Task 3.17), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.17 — Settings screen (lingua, scaling UI). Nuovo pkg `client/ui/settings` + `resources/fxml/settings.fxml`. `SettingsController` `@Component @Scope("prototype")` con ctor `(SceneRouter, I18n, PreferencesService, UserPromptService)`: `initialize()` legge `UserPreferences` e popola `ChoiceBox<Locale>` Italiano/Inglese (display via `StringConverter` su chiavi `locale.italian/english`), `ToggleGroup` scaling 100/125/150 (3 `RadioButton`), `ChoiceBox<String>` tema disabilitato con singolo item "Light" + label nota; `onScalingChanged()` applica subito `-fx-font-size` al root della scena (preview live; il global re-apply su scene switch è Task 3.20); `onSave()` → `saveSelections(Locale, int)` (visible-for-test, typed enum `SaveOutcome` SAVED_RESTART_REQUIRED/SAVED_NO_RESTART_NEEDED) che merge `UserPreferences.withLocale().withUiScalePercent()`, persiste via `PreferencesService.save`, e se locale cambiato mostra `prompt.info("settings.toast.locale.restart.*")` (§7.10 opzione A: riavvio richiesto), poi naviga a `SceneId.MAIN_MENU`; `onBack()` naviga senza salvare; `applyScalingToScene(Scene, int)` static + null-safe; scaling validato contro la whitelist {100,125,150} con fallback a 100. `UserPreferences.withUiScalePercent(int)` e `withThemeId(String)` aggiunti come helper compatibili con `withLocale`/`withFirstLaunch`. `SceneRouter.fxmlPathFor`: `SETTINGS -> "/fxml/settings.fxml"` (rimosso placeholder `RULES, SETTINGS -> null`; `RULES` resta `null` fino a Task 3.18). **i18n** IT/EN: `settings.title`, `settings.section.{language,scaling,theme}`, `settings.scaling.{100,125,150}`, `settings.theme.light`, `settings.theme.disabled.note`, `settings.button.save`, `settings.toast.locale.restart.{title,content}`. **9 nuovi test**: `SettingsControllerTest` 6 (locale change persists+prompts restart, locale unchanged no prompt, scaling change persists, invalid scaling fallback 100, onBack non salva, applyScalingToScene null-safe), `SettingsSpringContextTest` 2 (`@SpringBootTest` resolve prototype + isPrototypeScoped — lezione `feedback_spring_ui_tests`), `SettingsFxmlSmokeTest` 1 (`FXMLLoader.load` su JavaFX thread con `Platform.startup` + `Assumptions.assumeTrue` per skip headless, controller wired via `springContext::getBean`). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **214 test totali** (+9 vs Task 3.16), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.16 — Autosave + recovery prompt. Nuovo `AutosaveService` `@Component` (`client/persistence/`) wrap di `SaveService` vincolato a `AUTOSAVE_SLOT`: API `writeAutosave(SinglePlayerGame)` (build `SavedGame.of(game, now, now)` con `Clock` injectable per test), `readAutosave()` → `Optional<SinglePlayerGame>` via `SinglePlayerGame.fromSaved` con `Supplier<RandomGenerator>` injectable (default `() -> new SplittableRandom(System.nanoTime())`), `clearAutosave()` (delegato a `saveService.delete`), `autosaveExists()` (file-existence check su `Path.of(savesDir, "_autosave.json")`). Nuovo `SinglePlayerAutosaveTrigger` `@Component` (impl di `AutosaveTrigger`) che inoltra `onMoveApplied` a `autosaveService.writeAutosave` e swallows `UncheckedIOException` con log WARN (failure-tolerant: la mossa successiva ritenta naturalmente, FR-SP-08). Spring ora popola automaticamente `Optional<AutosaveTrigger>` di `SinglePlayerController` con questo bean → autosave attivo dopo ogni `applyMove` (umana e IA). `SplashController` refactor: rimossa `Path`/`Files`/`AUTOSAVE_FILENAME` hardcoded, ora dipende da `AutosaveService` e `bootstrap()` chiama `autosaveService.autosaveExists()`. `BoardViewController.terminate()` migrato da `saveService.delete(SaveService.AUTOSAVE_SLOT)` a `autosaveService.clearAutosave()` (rimosse `SaveService` e `AUTOSAVE_FILENAME`). `MainMenuController` ridisegnato: nuova firma ctor `(SceneRouter, I18n, AutosaveService, GameSession, UserPromptService)`; `handleAutosavePrompt()` ritorna typed enum `PromptResult` (RESUMED/DISCARDED/SCHEMA_MISMATCH/IO_ERROR/NO_AUTOSAVE); ramo "yes" → `autosaveService.readAutosave()` → `gameSession.setCurrentGame` → `sceneRouter.show(BOARD)`; ramo "no" → `autosaveService.clearAutosave()`; `UnknownSchemaVersionException` → toast `autosave.toast.error.schema.*` + clear; `UncheckedIOException` → toast `autosave.toast.error.io.*` + clear; `Optional.empty()` (file svanito tra splash e prompt) → `NO_AUTOSAVE` senza navigazione né cancellazione. **i18n** keys IT/EN aggiunte: `autosave.toast.error.schema.title/content`, `autosave.toast.error.io.title/content`. **22 test totali nuovi/modificati**: AutosaveServiceTest 8 (write reserved slot, overwrite, read empty, round-trip, schema propagation, clear+exists, idempotent clear, exists reflects FS), SinglePlayerAutosaveTriggerTest 2 (delegate, swallow IO), AutosaveE2ETest 3 (prompt on restart A3.5, clear on terminate, write failure tolerated quando savesDir è un file), SaveDialogSpringContextTest +2 (resolve `AutosaveService` + `AutosaveTrigger` come `SinglePlayerAutosaveTrigger` — context-resolution per la lezione `feedback_spring_ui_tests`), MainMenuControllerTest riscritto (3 click + 5 prompt branch), SplashControllerTest semplificato (mock `AutosaveService`), BoardViewControllerTest aggiornato. `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **205 test totali** (+22 vs Task 3.15), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.

- **Task precedente (uno indietro)**: Task 3.15 — Save dialog + Load screen. Nuovo pkg `client.ui.save` + integrazione menu sul board. `SceneId.LOAD` aggiunto al router (mapping `/fxml/load-screen.fxml`). `SinglePlayerController.currentSnapshot()` esposto pubblico per il save dialog; `SinglePlayerGame.fromSaved(SavedGame, RandomGenerator)` static factory per il reverse. `UserPromptService.confirm` esteso con `Object... formatArgs` (default no-arg overload preserva i caller esistenti) + nuovo `info(titleKey, contentKey, Object... contentArgs)` per i toast (Alert.INFORMATION); JavaFx impl applica i format args via `i18n.t(key, args)` su title/header/content. `SaveDialogController` `@Component @Scope("prototype")` con `show(SinglePlayerGame, Window)` che carica `save-dialog.fxml` via FXMLLoader+`setController(this)`, modal stage, blocking `showAndWait`; `confirm(name)` (visible-for-test) restituisce `ConfirmResult` enum (SAVED/NAME_EMPTY/NAME_INVALID/OVERWRITE_DECLINED/IO_ERROR); slugify via `SaveService.slugify`; overwrite prompt se slot esiste; toast success/error via `UserPromptService.info`; Clock injectable per timestamp deterministici nei test. `LoadScreenController` `@Component @Scope("prototype")` su `load-screen.fxml`: `TableView<SaveSlotMetadata>` con 6 colonne (Nome / Data / Livello / Colore / Mossa / Anteprima — sortable nelle prime 5; miniatura non-sortable); cell factory dell'anteprima istanzia `MiniatureCell` che on-demand carica il `SavedGame` completo e chiama `MiniatureRenderer.render(state, 64)`; pulsanti Carica/Elimina disabled via Bindings.isNull selezione; `loadSelected`/`deleteSelected` (visible-for-test) ritornano typed enum (LoadResult/DeleteResult); typed handling `UnknownSchemaVersionException` → toast `load.toast.error.schema.*`, `UncheckedIOException` → toast `load.toast.error.io.*` (entrambi con `selected.name()` come arg); `refresh()` null-tolerant su `slotsTable` (test harness skip UI side-effects, popola solo `currentSlots`); `Supplier<RandomGenerator>` injectable per RNG deterministico nei test. `MiniatureRenderer` `@FunctionalInterface` con prod impl `CanvasMiniatureRenderer` `@Component` che disegna su `Canvas` (8x8 grid + dischi pezzi + ring oro per dame) e `canvas.snapshot()` → `Image`. `BoardViewController` ridisegnato: `board-view.fxml` ora ha `MenuBar` con menu "Partita" (Salva con nome / Carica / Termina partita) sopra il titolo; `onSaveAs()` → `openSaveDialog()` (visible-for-test) prende `gameController.currentSnapshot()` e apre il dialog modal (owner Window dalla scene); `onLoad()` chiama `gameController.stop()` e naviga a `SceneId.LOAD`; `onTerminate()` → `terminate()` (visible-for-test) confirm prompt + `saveService.delete(AUTOSAVE_SLOT)` + `gameSession.clear()` + `sceneRouter.show(MAIN_MENU)`. **i18n** keys IT/EN: `board.menu.*`, `board.terminate.*`, `save.dialog.*`, `save.toast.*`, `load.title`, `load.column.*`, `load.button.*`, `load.empty`, `load.confirm.*` (con `{0}` per nome), `load.toast.error.*`. **22 nuovi test**: SaveDialogControllerTest 6 (persists, empty, unsluggable, overwriteDeclined, overwriteConfirmed, ioError), LoadScreenControllerTest 10 (lists 3 saves, click+load resumes, cancel, schema mismatch toast, IO error toast, no selection, delete, delete cancel, delete no selection, onBack), BoardViewControllerTest 4 (terminate success, terminate cancel, onLoad navigates+stops AI, openSaveDialog null guard), CanvasMiniatureRendererTest 2 (rejects non-positive size, miniatureRendersWithoutErrors via Platform.startup + runLater + CountDownLatch con `Assumptions.assumeTrue` per skip su headless). `mvn -pl client verify -DexcludedGroups=slow,performance` BUILD SUCCESS, **183 test totali** (+22 vs Task 3.14), JaCoCo client gate ✅, SpotBugs 0 High, Spotless OK.
- **Ultimo task in REVIEW**: chiusura REVIEW Fase 3 (commit `6eeb332`, file `reviews/REVIEW-fase-3.md`). Pre-fix: Task 3.24 (commit `74de2af`) ha implementato Undo/redo con semantica "coppia umana+IA" (Deque<GameState> stacks, BiConsumer-pattern UndoState observable, Ctrl+Z/Y, MoveHistoryViewModel.replaceWithHistory, 16 nuovi test su 4 file). Doc fixes (commit `59bedc7`): TRACEABILITY allineato ai metodi reali, ADR-031 chiarisce `kind="SINGLE_PLAYER_GAME"` univoco con manual↔autosave codificato nel nome del file, BoardRenderer.setAccessibleText commentato come deferral F11.
- **Prossimo task**: **Closure F3.5** — tag `v0.3.5` su main + merge `develop → main` con `--no-ff` per CLAUDE.md §4.4. Sequenza: (1) commit di chiusura del TEST-PLAN, (2) merge `feature/3.5-visual-polish-and-audio → develop` con `--no-ff`, (3) `git checkout main && git merge --no-ff develop -m "release: chiusura Fase 3.5"`, (4) `git tag -a v0.3.5 -m "Fase 3.5 — Visual polish + Audio + Demo release completata"`, (5) `git checkout develop` per future fasi. **Stop point** utente per conferma chiusura prima di applicare il tag. **NON aprire Fase 4** in questa sessione: F4 verrà aperta in nuova sessione completamente fresca dove riapplicare i 7 passi di "Avvio sessione" CLAUDE.md §7 (vedi memoria `project_phase_transition_protocol`).

- **Task originalmente previsto (ora storico)**: **Task 3.5.17** — TEST sotto-fase Fase 3.5. Output: completare `tests/TEST-PLAN-fase-3.5.md` (sezioni 1-6 e 8 da estendere; §7 manual demo già finalizzato). Eseguire regression `mvn clean verify` (root) con `slow`+`performance` inclusi (~16 min) per confermare che (a) il corpus regole F1 + gating IA F2 continuano a passare invariati (rework F3.5 è puramente client/visual/audio, niente codice `shared` toccato) e (b) il modulo `client` post-F3.5 build success con full suite. Closure check finale (coverage target, traceability, no @Disabled). Solo dopo OK utente: Task 3.5.18 = closure (tag `v0.3.5` su main + merge develop→main `--no-ff` per CLAUDE.md §4.4).

- **Task originalmente previsto (ora storico)**: **Task 3.5.16** — REVIEW Fase 3.5 (PLAN-fase-3.5 §3.5.16). Output: `reviews/REVIEW-fase-3.5.md`. Mappare ogni A3.5.x al codice che lo realizza, mappare ogni FR/NFR coinvolto, eseguire code-by-code review dei file modificati nella fase, identificare findings categorizzati per CLAUDE.md §2.3.1 (BLOCKER/REQUIREMENT_GAP/BUG/SECURITY/PERFORMANCE/CODE_QUALITY/DOC_GAP). Stop point con utente per fix concordati. F-001 (save-dialog non wood-themed) già RESOLVED in Task 3.5.14a — entrerà nella review come trace storica. Inizio TEST sotto-fase (Task 3.5.17) solo dopo chiusura della REVIEW.

- **Task originalmente previsto (ora storico)**: **Task 3.5.15** — Documentation + ADR + CHANGELOG + TRACEABILITY (PLAN-fase-3.5 §3.5.15). Aggiungere ADR per visual rework rationale (ADR-034), AudioService architecture (ADR-035), jpackage Windows packaging (ADR-036), asset licensing strategy (ADR-037). Aggiornare README sezione "Eseguire il client" con istruzioni installer + nota cliente demo. Aggiornare CHANGELOG `[Unreleased]` con voci F3.5. Aggiornare TRACEABILITY (sezione "Fase 3.5") con mappatura A3.5.x → test. Moduli toccati: `ARCHITECTURE.md`, `README.md`, `CHANGELOG.md`, `tests/TRACEABILITY.md`.

- **Task originalmente previsto (ora storico)**: **Task 3.5.12** — `jpackage` Windows MSI (PLAN-fase-3.5 §3.5.12). Aggiungere `org.panteleyev:jpackage-maven-plugin` al `client/pom.xml`. Configurazione: `--name "Dama Italiana"`, `--app-version 0.3.5`, `--vendor "Dama Italiana"`, `--type msi`, `--input target/jpackage-input/`, `--main-jar dama-italiana-client-0.3.5-SNAPSHOT.jar`, `--main-class com.damaitaliana.client.JavaFxApp`, `--win-shortcut`, `--win-menu`, `--win-menu-group "Dama Italiana"`, `--icon assets/app-icon.ico`. `--runtime-image` con `jlink` modules custom. Prerequisito: WiX Toolset 3.x installato sulla macchina build. Output: `target/jpackage/dama-italiana-0.3.5.msi`. Moduli toccati: `client/pom.xml`, `assets/app-icon.ico` (nuovo), `README.md` sezione "Build installer Windows".
- **Stato test (baseline pre-Fase 3)**: `mvn clean verify` (root) BUILD SUCCESS in ~2 min — `shared` 391 test (387 default + 1 slow + 3 performance), JaCoCo 97.3% modulo + 96.2% package `rules` + 97.7% package `ai`, SpotBugs 0 High, Spotless OK. `core-server`/`client`/`server` ancora con il singolo smoke test ciascuno (lo smoke `client` verrà rimosso in Task 3.1).
- **Test gating A2.2 (slow)**: ✅ PASSED il 2026-04-28 in 16:02 min (Campione ≥ 95/100 vs Principiante). Da rieseguire on demand a chiusura Fase 3 (regression).
- **Piano di riferimento Fase 3**: [`plans/PLAN-fase-3.md`](plans/PLAN-fase-3.md).
- **Piani precedenti**: [`plans/PLAN-fase-2.md`](plans/PLAN-fase-2.md), [`reviews/REVIEW-fase-2.md`](reviews/REVIEW-fase-2.md), [`tests/TEST-PLAN-fase-2.md`](tests/TEST-PLAN-fase-2.md).

## Decisioni recenti

- Approvate dall'utente in data 2026-04-27 (Task PIANIFICA Fase 0):
  - Spotless: indentazione **2 spazi** (Google standard, conforme a NFR-M-04).
  - Maven `groupId`: `com.damaitaliana`.
  - Tag chiusura Fase 0: `v0.0.0`.
- Revisione post-implementazione (2026-04-27, dopo feedback utente):
  - **ADR-018**: Docker Compose rimosso. L'ambiente di sviluppo usa il MySQL locale dell'utente (porta 3306, gestito via Workbench/DBeaver).
  - **ADR-019**: workflow GitHub Actions rinominato in `ci.yml.disabled` (non eseguibile). Verrà riattivato quando un repository remoto sarà disponibile.
- Adozione **GitFlow leggero** (2026-04-28): `main` = production / tag, `develop` = integrazione e default branch GitHub, branch effimeri `feature/<fase>-<topic>` e `fix/review-N-F-<id>` staccati da `develop`, mergiati `--no-ff`. Tag delle fasi (`v0.<fase>.0`) sul commit di merge in `main`.
- Approvati dall'utente in data 2026-04-28 (Task PIANIFICA Fase 2): tutti gli stop point §7.1-§7.10 in opzione A in blocco. Task 2.14 (Zobrist-based threefold repetition) deferred-F4. Si è applicata in F2 una hardening fix non-Zobrist (catch `IllegalMoveException` → `false`) per supportare il search su stati hand-built (REVIEW finding F-001/F-002, ACKNOWLEDGED).
- ADR-024 ÷ ADR-028 registrati durante Fase 2 (architettura `AiEngine` sealed + 3 livelli, Evaluator modulare, Zobrist + TT, cancellation cooperativa, rumore Principiante deterministico).
- Approvati dall'utente in data 2026-04-28 (Task PIANIFICA Fase 3): tutti gli stop point §7.1-§7.14 in opzione A in blocco. Riepilogo decisioni:
  - §7.1 Bootstrap: `ClientApplication` Spring Boot non-web + `JavaFxApp.launch` + `JavaFxAppContextHolder` (ADR-029).
  - §7.2 FXML per layout schermate, `setControllerFactory(context::getBean)` (ADR-030); board view custom programmatica.
  - §7.3 Tema F3: solo light selezionabile; `theme-dark.css` come stub presente non collegato; toggle dark in F11.
  - §7.4 Schema saves v1: JSON con `currentState` materializzato + lista `moves` FID; rifiuto schemaVersion ignota (ADR-031).
  - §7.5 Autosave atomico: tmp + `Files.move(ATOMIC_MOVE, REPLACE_EXISTING)` con fallback log WARN (ADR-032).
  - §7.6 Mini-animazioni regole: incluse (Task 3.19), candidate primo slip se la fase si gonfia.
  - §7.7 Audio: escluso da F3, riservato a F11.
  - §7.8 Coverage gate `client` 60% line+branch su pkg non-view, esclusioni esplicite tracciate nel POM.
  - §7.9 Chiavi i18n hierarchiche `screen.element.role`, bundle unico `messages_*.properties` (ADR-033).
  - §7.10 Cambio lingua richiede riavvio in F3; runtime dynamic in F11.
  - §7.11 Card LAN/Online visibili e disabilitate con tooltip "Disponibile in Fase X".
  - §7.12 Solo click-to-select; drag-and-drop valutabile in F11.
  - §7.13 Undo/redo annulla la coppia `(mossa umana + risposta IA)` come unità.
  - §7.14 Branch unico `feature/3-ui-singleplayer`, merge `--no-ff` su `develop`, tag `v0.3.0` sul commit di merge in `main`.

## SPEC clarifications needed

Nessuna al momento.

## Note operative

- **Fast loop di sviluppo F3**: durante l'IMPLEMENTA usare `mvn -pl client verify -DexcludedGroups=slow,performance` (~30s). Il gating slow di F2 (`AiTournamentSimulationTest`, ~16 min) si esegue solo a chiusura fase prima della REVIEW, oppure on demand. Prerequisito una tantum: `mvn install -pl .,shared,core-server -DskipTests -Dspotless.check.skip=true -Dspotbugs.skip=true -Djacoco.skip=true` per popolare il local Maven repo con parent + shared + core-server jar.
- Inter TTF binaries non committati (vedi `client/src/main/resources/fonts/README.md`). `ThemeService` carica i font se presenti, altrimenti log INFO + fallback su font-family chain di SPEC §13.2 ("Segoe UI", "Helvetica Neue", sans-serif). Il developer/asset pipeline aggiunge i file in `resources/fonts/` se vuole l'estetica Inter. Decisione documentata nel commit di Task 3.2.
- Rappresentazione del board per il corpus regole **decisa in Fase 1** (ADR-022): JSON con quattro liste disgiunte `whiteMen`/`whiteKings`/`blackMen`/`blackKings` indicizzate in FID 1-32 (ADR-020).
- Smoke test `SharedSmokeTest` **rimosso** in Task 1.11. Restano `CoreServerSmokeTest`, `ClientSmokeTest`, `ServerSmokeTest`: da rimuovere quando i rispettivi moduli ricevono test reali (Fase 4 / 3 / 5 rispettivamente).
- JaCoCo `haltOnFailure=true` su `shared` con soglie 90% bundle + 90% package `rules` (Fase 1) + 85% package `ai` (Fase 2). Per `client`/`core-server`/`server` il gate resta lasco fino alle rispettive fasi.
- `isThreefoldRepetition` (ADR-021) replay-based con hardening fix Fase 2 (catch `IllegalMoveException` → `false`). Limite documentato per stati hand-built. Ottimizzazione Zobrist-incremental deferred a Fase 4 (REVIEW-fase-2 finding F-001/F-002).
- Tag JUnit attivi nel modulo `shared`: `slow` (1 test, gating A2.2 ~16 min) + `performance` (3 test, NFR-P-02). Default `mvn verify` include tutto; loop di sviluppo veloce: `-DexcludedGroups=slow,performance`.
- Determinismo Principiante (ADR-028): `RandomGenerator` iniettato; in test `SplittableRandom(42L + gameIndex)` per simulazioni riproducibili. Default produzione: `SplittableRandom(System.nanoTime())`.
- Determinismo Zobrist (ADR-026): seed costante `0xDA4A172L`. Stesso jar = stessi hash = stesso comportamento TT.
