/**
 * Pure-Java, immutable domain model of the Italian Draughts board.
 *
 * <p>Records and sealed types only — no framework dependencies. SPEC §3.1 (board orientation), §3.2
 * (movement), §3.3 (capture), §3.5 (promotion), §3.6 (game termination), §8.1 (entities) are all
 * materialised here.
 */
package com.damaitaliana.shared.domain;
