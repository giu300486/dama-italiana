/**
 * Pure domain model of the Italian Draughts game.
 *
 * <p>Sub-packages added in Fase 1+: {@code domain} (Square, Piece, Board, Move, GameState), {@code
 * rules} (RuleEngine), {@code ai} (Minimax + alpha-beta), {@code dto}.
 *
 * <p>Constraint (CLAUDE.md §8.7): this module MUST NOT depend on Spring, JavaFX, JPA, or any
 * WebSocket library.
 */
package com.damaitaliana.shared;
